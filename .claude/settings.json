{
  "hooks": {
    "SessionStart": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd \"$CLAUDE_PROJECT_DIR\" && if [ ! -d node_modules ]; then echo 'weekly-audit hook: installing dependencies (npm ci)...'; npm ci --no-audit --no-fund || npm install --no-audit --no-fund; else echo 'weekly-audit hook: node_modules present, skipping install'; fi"
          }
        ]
      }
    ]
  }
}
