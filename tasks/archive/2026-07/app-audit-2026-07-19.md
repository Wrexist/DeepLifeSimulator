# Full App Audit — 2026-07-19

Multi-agent audit of all 20 in-game apps (phone + computer): functionality, feature completeness, visual polish. 31 agents; every critical/major finding was adversarially verified before inclusion (refuted claims dropped). Totals: 0 critical, 5 major, 20 minor, 7 polish.

**Status legend:** ✅ fixed · 🟡 reviewed & intentionally kept

## SparkApp (dating)

The app fundamentally works end-to-end. Swipe deck, matching, chat with delayed NPC replies, the Ultra-gated Likes-you inbox, promote-to-relationship, cash-based premium subscription (overdraft-reject via applyMoneyDelta), gem Boost, catfish expose/scam, and jealousy resolution all wire to real SparkActions that mutate and read back state live. Defensive throughout: double-tap dedup guards, anti-bigamy rechecks, deterministic sparkTick handling, legacy-save optional-chaining, empty states on every screen. On-DNA visuals with lucide icons and no raw emoji. Computer mounts the identical component, so no divergence. Findings are minor feedback/consistency gaps, not blockers.</works>
</invoke>

- ✅ **[MINOR · feature-gap]** Promote-to-dating from the chat header fails silently when the player already has a partner/spouse\n  `components/mobile/Spark/screens/ChatScreen.tsx:80`\n  ChatScreen.handlePromote (lines 80-89) calls promoteMatchToRelationship and on failure only runs sparkHaptics.error(), discarding result.message. promoteMatchToRelationship (SparkActions.ts:574-577) rejects when findRomanticPartner returns a partner. A player already dating/married taps the header Heart (ChatScreen.tsx:119-131) and nothing visible happens; ChatScreen does not import useToast, so the gated CTA reads as a dead button.
- ✅ **[MINOR · feature-gap]** Rewind silently spends 20 gems with no cost shown, no confirmation, and no feedback\n  `components/mobile/Spark/screens/SwipeScreen.tsx:267`\n  SwipeScreen.handleRewind (267-276) calls rewindLastSwipe directly; for free-tier players it debits 20 gems (SparkActions.ts:268-310). Unlike Boost's confirming modal, the Rewind button (SwipeScreen.tsx:353-360) shows no cost and no confirm step, so a tap immediately drains 20 gems. On success or failure only a haptic fires; useToast is imported and used elsewhere in the file but never for rewind.
- ✅ **[POLISH · feature-gap]** Player Spark profile photos never populated, so ~25 of 100 profile-strength points are unreachable\n  `lib/dating/sparkLogic.ts:251`\n  scorePlayerProfile (sparkLogic.ts:251) awards up to 25 points from sp.profile.photos.length and calculateMatchProbability adds +0.08 for photos, but sparkApp.profile.photos is only ever initialized to [] and no code writes to it; the edit modal omits photos (SparkProfileEditModal.tsx:8) though no such wiring exists. The Profile-strength meter can only move via bio+interests and never reaches its top band.

## PulseApp (social media)

PulseApp is a large, genuinely well-built social-media suite that works end-to-end for the overwhelming majority of flows. Every primary action I traced — inline + modal composing, like/repost/comment, follow/unfollow via the NPC sheet, brand-deal accept/decline/deliver/breach, live streaming, scandal recovery, Verified Pro subscribe/cancel, gem boosting, rewarded-ad follower boosts, notifications, profile editing, insights gating — routes into real PulseActions handlers that mutate state through the canonical money/stat paths, with careful anti-exploit re-checks inside the setState updaters and correct empty-states throughout. The design is consistent with the game's dark language (slate cards, hairline borders, theme-driven colors), and Pulse's magenta→indigo gradient is its intentional brand identity (confirmed in computer.tsx), not a violation. The one real defect a player will hit is that the Boost affordance is dead when reached from the Profile screen, because BoostPostModal is not mounted in the profile-overlay branch. A few actions also skip the explicit saveGame that every sibling uses, and one ad-reward label is hardcoded. Nothing crashes.

- ✅ **[MAJOR · broken-functionality]** Boost button on Profile-screen posts does nothing — BoostPostModal is never mounted while the profile overlay is active.\n  `components/mobile/Pulse/PulseApp.tsx:133`\n  In PulseApp.tsx the profile overlay is an early return (lines ~114-143) that renders ProfileScreen with onBoostPost={(postId) => setBoostPostId(postId)} plus VerifiedProUpsellModal and ProfileEditModal — but it does NOT render BoostPostModal. BoostPostModal is only mounted in the main return (line 321), which is not rendered when overlay==='profile'. PostCard shows the Boost affordance for the player's own posts (ProfileScreen.tsx line 328 passes onBoost + isPlayerPost), so tapping Boost on a post from your own profile sets boostPostId but no modal ever appears. Boost works from the Home feed (FeedScreen is in the main return) but is silently dead from the Profile screen — a primary place a player views and boosts their own posts. detailPostId avoids this because it is checked at the very top (line 100) before the overlay branches; boostPostId has no such top-level host.
- ✅ **[MINOR · broken-functionality]** Gem/money/reward-spending actions in three Pulse surfaces never call saveGame, unlike every sibling, so a reload inside the 2-minute autosave window silently reverts the spend/reward.\n  `components/mobile/Pulse/modals/BoostPostModal.tsx:35`\n  BoostPostModal.handleBoost (boostPostWithGems, -200 gems) does not destructure or call saveGame at all (lines 28-44). ScandalRecoveryModal.handleChoice (recoverFromScandal — up to -$5,000 lawsuit fee or -500 gems) never calls saveGame (lines 65-76). LiveStreamScreen never calls saveGame after startLiveStream (-30 energy), endLiveStream (tips credited via updateMoney + followers), or the accumulating tickLiveStream donations. Every other Pulse mutation persists explicitly (ComposeModal, BrandDealsScreen, NpcProfileSheet, ProfileEditModal, VerifiedProUpsellModal, RewardedAdModal, PostCard, FeedScreen, PostDetailScreen all call saveGame). Persistence therefore relies solely on the 2-minute periodic autosave (GameActionsContext.tsx ~line 3368) or the next weekly tick; a hard reload before that fires reverts the gem/cash spend and any live-stream tips, which is both a data-loss risk and an inconsistency with the established post-commit save pattern.
- ✅ **[POLISH · visual]** Rewarded-ad CTA on the Notifications screen hardcodes '+50 followers' even though Verified Pro members actually get +150.\n  `components/mobile/Pulse/screens/NotificationsScreen.tsx:165`\n  NotificationsScreen.tsx renders '▶ Watch ad for +50 followers' in both the empty-state CTA (line 165) and the header CTA (line 192) as a literal string. RewardedAdModal computes expectedFollowers = proActive ? 150 : 50 (line 32) and watchAdForFollowerBoost grants 150 to Pro members. So a Verified Pro player is shown '+50' but receives +150 — a misleading label that never reflects the Pro multiplier the rest of the app advertises.

## HustleApp (companies)

Yes — the suite works end-to-end. Founding, hiring (generic + named), campaigns, scandals, IPO, acquisitions, R&D labs/patents/competitions, upgrades, and family-business conversion all trace into canonical action functions that mutate state atomically (money always flows through applyMoneyDelta with same-updater debits, so no double-tap/low-cash race grants free actions). UI gating (canHire, canLaunch, affordability, IPO eligibility) is honest and matches each action's real preconditions, so buttons only fire when they can succeed. Empty states, feedback messages, and stale-state re-reads are handled in most flows. Mobile and computer both mount the identical HustleApp component, so there is no mobile/computer feature divergence. Two genuine gaps remain, one a missing core feature and one a modal feedback omission.

- ✅ **[MINOR · feature-gap]** No way to sell or divest a company anywhere in the app, and the Dashboard's 'Sold' milestone is a permanently-zero dead stat.\n  `components/mobile/Hustle/screens/DashboardScreen.tsx:250`\n  The canonical sellCompany() (contexts/game/company.ts:314) has zero callers in the entire app — a grep across contexts/ and components/ finds it referenced only in a code comment. The Hustle suite lets a player found, staff, IPO, and acquire, but never exit/close/sell a company. Compounding this, DashboardScreen's Milestones tab renders a 'Sold' tile (DashboardScreen.tsx:250) bound to lifetime.totalCompaniesSold — and totalCompaniesSold is the ONLY lifetime stat with no writer anywhere (every other counter is incremented in HustleActions/hustleTick; totalCompaniesSold appears only as `: 0` initializers in HustleActions.ts:92, CompanyActions.ts:137, initialState.ts:1622). So a player who sinks capital into a company can never divest it, and the surfaced 'Sold' record always reads 0.
- ✅ **[MINOR · feature-gap]** ResolveScandalModal gives no feedback and no affordability gating when the chosen response is unaffordable.\n  `components/mobile/Hustle/modals/ResolveScandalModal.tsx:51`\n  The five response cards (e.g. Restructure $200,000, Lawsuit $100,000) are always pressable with no disabled/opacity state and no cash check. When the player can't afford a choice, resolveScandal() returns {success:false} and handleChoice (ResolveScandalModal.tsx:51-60) only fires hustleHaptics.error() — the component has no resultMsg state and renders no message, so the tap produces a silent buzz and nothing else while the scandal banner persists. Every other Hustle modal (Hire/Campaign/IPO) surfaces a resultMsg on failure; this one leaves the player with no explanation of why the response didn't apply.

## ContactsApp

ContactsApp fundamentally works end-to-end. All four tabs (Personal, Network, Favors, Attention) plus the network detail page render correctly and every user-facing action is wired to a real state-mutating handler in contexts/game/actions and lib/contacts. Call/Hang Out/Ask $/Lend $ route through ContactsActions helpers with atomic prev-rechecking updaters; favor Redeem/Repay go through redeemFavor/repayFavor with overdraft-safe applyMoneyDelta credits and double-tap guards; dates, gifts, propose, wedding, and divorce route through the canonical DatingActions with modal confirmation flows; aggregateContacts safely walks 5 game systems with null-guards. Empty states, per-card and global feedback toasts, stable list keys, and dark-theme tokens are all present and consistent. The two issues found are minor tracking/polish items, not broken functionality.

- ✅ **[MINOR · broken-functionality]** Date and gift spending bypasses the canonical money path, so it is invisible to the daily-summary money tracking that every other Contacts money action feeds\n  `contexts/game/actions/DatingActions.ts:211`\n  goOnDate (DatingActions.ts:211) and giveGift (DatingActions.ts:330) deduct the cost by mutating stats.money inline (money: Math.max(0, prevMoney - config.cost)) inside their setGameState updaters, ignoring the deps.updateMoney passed in from ContactsApp handleDate/handleGift. The canonical updateMoney/applyMoneyDelta path (MoneyActions.ts:47-64,119-124) additionally accumulates dailySummary.moneyChange. Every other money action reachable from ContactsApp — Hang Out cost, Lend $, Ask $, favor Redeem/Repay — routes through applyMoneyDelta and is tracked. As a result a player who spends hundreds/thousands on dates and gifts sees that spend omitted from the daily summary's net money change. Money is still correctly deducted (with an in-updater affordability re-check), so it is a tracking inconsistency, not a lost/duplicated funds bug.
- ✅ **[POLISH · visual]** Expanded personal-profile 'inner life' lines use raw platform emoji as glyphs while the rest of the app uses lucide icons\n  `components/mobile/ContactsApp.tsx:599`\n  The expanded personal card renders its inner-life readouts with raw emoji prefixes — 🤝 Trust / 💘 Attraction / 🎖️ Respect (ContactsApp.tsx:599-605), 🎯 Dreams of / 🎁 Loves / 🚫 Dislikes / 💵 Asked for money / 📌 life event / 💭 Right now / 🤰 Expecting / 💍 milestone (lines 610-646). Every actionable control and hero in the same screen uses lucide-react-native icons (Phone, Gift, Handshake, Heart, etc.), so these emoji read as a stylistic break from the game's lucide-based dark design language and render inconsistently across platforms. They are secondary text decorations (not primary tappable UI), so impact is cosmetic.

## BankApp (mobile)

Fundamentally yes. The app is a thorough, well-wired banking screen: every button (deposit, withdraw, close, open account, apply loan/card, prepay, pay card, add/toggle/remove bill, create/contribute savings goal, watch-ad bonus, credit report) traces into a real action in contexts/game/actions that mutates state through the canonical spend/earn paths, and queueSave() persists after each. Mirrored (checking-default/savings-default) accounts are correctly detected and rendered read-only so their deposit/withdraw/close never silently no-op. All list loops use stable keys, every section has an empty-state card, credit-report math is null-guarded (history/inquiries/points), and the visual language (deep navy surfaces, slate glass cards, blue #3B82F6/#60A5FA accents, lucide icons, rounded cards) is consistent with the game. The one substantive problem: the credit-card and loan payment flows draw from the checking-default account, whose balance is only synced to real cash on the weekly tick, while the modals advertise and cap against live cash — so mid-week cash gains cause silent failures/partial payments.

- ✅ **[MAJOR · broken-functionality]** Pay-credit-card and prepay-loan draw from checking-default (only synced on the weekly tick) while the modal caps against live cash, so mid-week cash gains cause silent full/partial payment failures\n  `components/mobile/BankApp.tsx:846`\n  The 'Pay credit card' modal (line 837-855) uses maxAmount={cash} and subtitle 'Cash on hand: {cash}', but onConfirm calls payDownCard(setGameState, payCardId, checking.id, amt) where checking = banking.accounts.find(a=>a.type==='checking') — always checking-default, the only checking account allowed. payDownCard -> payCreditCard -> withdrawFromAccount rejects with 'Insufficient funds' when account.balance < amt (lib/banking/operations.ts). checking-default.balance is ONLY written by mirrorAccountsFromLegacy on the weekly tick (lib/banking/weeklyTick.ts:146-148); every mid-week cash gain updates stats.money WITHOUT touching checking-default: e.g. the Watch-ad bonus on THIS SAME screen (updateMoney at BankApp.tsx:574), savings withdrawals (withdrawCashFromAccount), account closes, stock/crypto sales, quest rewards. Repro: last tick checking-default=$1000; player watches ad / sells stock so cash=$5000; opens 'Pay credit card', sees 'Cash on hand: $5000', enters $2000 -> withdrawFromAccount sees stale $1000 < $2000 -> ok:false -> payDownCard returns prev, logs a warning, NO state change and NO user feedback. The player plainly has the cash but the payment silently does nothing. The identical Prepay-loan modal (line 815-834 -> prepayLoan, LoanActions.ts:271) instead silently PARTIAL-pays: payAmount=Math.min(account.balance[stale], amount, remaining), so only the stale checking balance is applied and stats.money debited by that smaller amount, even though maxAmount={cash} let the player enter (and afford) more. Both are core banking actions that quietly under-perform against the live cash the UI shows; they only 'self-heal' after the next Next-Week press re-mirrors checking-default.

## AdvancedBankApp (computer)

The app is fundamentally solid and works end-to-end. It is a deliberate superset of the mobile BankApp: statement overview, accounts (deposit/withdraw/close/transfer), savings goals, borrow (loans + credit cards with charge/pay/redeem), and budget. Every user-facing action I traced dispatches to a real BankingActions/LoanActions function that mutates gameState through the canonical applyMoneyDelta / operations helpers with finite/ceiling/death guards, and state re-renders from gameState.banking so views stay fresh. Empty-states, disabled states, and post-action feedback are consistently present, and the design follows the game's dark language (theme colors, lucide icons, no raw emoji). Two defects worth fixing: one broken documented action (clearing a budget cap) and cosmetic mojibake in shared banking strings.

- ✅ **[MINOR · broken-functionality]** Budget cap cannot be cleared — the modal's "Enter 0 to clear" instruction is impossible to execute\n  `components/computer/AdvancedBankApp.tsx:1304`\n  The Budget tab's 'Set weekly budget' modal is rendered with subtitle 'Weekly cap for {cat}. Enter 0 to clear.' (AdvancedBankApp.tsx:1301-1316). But AmountInputModal computes valid = amount > 0 && (maxAmount==null || amount<=maxAmount) (AmountInputModal.tsx:47) and disables the Confirm button when invalid, so entering 0 leaves Confirm greyed out and onConfirm never fires. The underlying setBudgetTarget op (lib/banking/operations.ts:598-602) explicitly deletes the target when amount<=0, and BudgetBreakdown (the only cap UI) exposes no other way to remove a cap — so once a player sets a weekly cap they can change it but can never clear it, despite the app telling them how.
- ✅ **[MINOR · visual]** Mojibake middot ('Â·') renders in user-facing banking strings\n  `components/banking/BudgetBreakdown.tsx:89`\n  The Budget tab header renders the literal string `Last {weeks} weeks Â· {formatMoney(grandTotal)}` (BudgetBreakdown.tsx:89) — the 'Â·' is a double-encoded U+00B7 middot (bytes C2 B7) that displays as 'Â·' instead of '·'. The same corrupted separator appears in OpenAccountModal.tsx:140 (` Â· Locked {n}w`), which AdvancedBankApp renders in its Open Account flow. The rest of the app uses the correct '·' (e.g. AdvancedBankApp.tsx:514), so these two shared components look inconsistent and visibly garbled.

## StocksApp (mobile)

StocksApp is fundamentally solid and functions end-to-end. All user-facing actions are wired to real context mutations: tab switching, sector filtering, sort chips, watchlist toggle (toggleStockWatchlist), order cancel (cancelStockOrder with confirm dialog), and trades (buyStockMarket/sellStockMarket/placeStockLimitOrder/placeStockStopOrder). Money flows correctly through the canonical applyMoneyDelta helper with fee, NaN, overdraft and death-popup guards. Empty states exist for every list, prices are read live from getStockInfo keyed on weeksLived, and symbol casing is consistent (all uppercase) so watchlist/holdings joins resolve. Design matches the dark language (slate glass cards, purple accent identity, lucide icons, rounded radii). The one meaningful issue is a silent no-op on affordable-looking buys due to the commission not being reflected in the modal's validity gate.

- ✅ **[MINOR · broken-functionality]** Buy for an amount between ~98% and 100% of cash passes the modal's enabled Confirm button but is silently rejected by the action (2% fee not accounted for in validation), with no error feedback.\n  `components/stocks/StockTradeModal.tsx:61`\n  StockTradeModal validity (StockTradeModal.tsx:61) gates a buy on `amount > cash`, so the Confirm button is enabled for any amount up to the full cash balance. But buyStockMarket (StockActions.ts:86-90) computes `grossCost = amountUSD * (1 + STOCK_FEE)` (STOCK_FEE = 0.02) and rejects when `grossCost > cash`. So any buy amount in the range (cash/1.02, cash] — e.g. tapping to spend your whole balance — passes the modal, the user taps Execute Buy, handleTrade (StocksApp.tsx:157-158) calls buyStockMarket which returns `prev` unchanged, then handleTrade unconditionally runs queueSave() and setTradeTarget(null) (StocksApp.tsx:165-166). The modal closes as if the trade succeeded, but nothing was bought and no Alert/toast is shown. The same class of silent rejection applies to limit/stop buys: the modal only checks `amount > cash` (line 61) while canPlaceStockOrder (StockActions.ts:200-204) also requires fee headroom plus reserves against existing open buy orders. Players attempting to go 'all in' will repeatedly see the trade dialog dismiss with no effect and no explanation.

## EducationApp (mobile/computer shared)

Yes, the app fundamentally works end-to-end. All three tabs (Catalog / Enrolled / Earned) render correctly, the enroll flow routes through EnrollModal into enrollInProgram with proper cash-vs-loan handling (loans wired through the real banking system), and Study / Pause / Withdraw / Study-group buttons all call real transformers that mutate state and re-render fresh (computeStudyState and findStudentLoan are recomputed from live gameState each render, so no staleness). Money flows use the canonical applyMoneyDelta path for study-group joins and the enroll quote for tuition. Empty states, disabled/gated study buttons, and detail pages are all present. The one genuine gap is that the campus-event mechanic has been reduced to a consequence-free banner.

- ✅ **[MAJOR · feature-gap]** Campus events fire recurringly but resolve to a hollow, consequence-free dismiss banner — the decision/effect the mechanic exists for was dropped in this rewrite.\n  `components/mobile/EducationApp.tsx:572`\n  The weekly tick (contexts/game/actions/weekly/applyEducationProgression.ts:209-213) triggers a campus event every 4-8 weeks per enrolled program and sets pendingCampusEventEducationId. In EducationApp.tsx:572-607 the only consumer is a banner reading 'Something happened on campus in <course>.' with a Dismiss button that calls clearCampusEvent (EducationActions.ts:343-347), which merely sets the flag to undefined — no stat change, no choice, no outcome. clearCampusEvent's own doc comment claims 'Choice effects are applied by the legacy modal in EducationApp,' but no such modal exists in this file or anywhere in the codebase (grep for CampusEventModal/applyCampusEvent/resolveCampusEvent finds only docs). A player sees a vague recurring toast with zero gameplay effect.
- ✅ **[MINOR · visual]** Raw platform emoji (🎓) used inline in the scholarship-eligibility copy, against the app's lucide-icon design language.\n  `components/education/EnrollModal.tsx:145`\n  EnrollModal.tsx:145-147 prefixes the scholarship eligibility line with a literal '🎓' emoji ('🎓 Full ride — your GPA earned you this', etc.). The rest of the app consistently uses lucide-react-native icons with tinted bubbles; a raw platform emoji renders inconsistently across iOS/Android and clashes with the Slate Glass visual system. A lucide GraduationCap/Award glyph would match.

## PetApp (pets / "paw")

The app is fundamentally solid and works end-to-end. Every user-facing action (adopt, feed, buy food/toy, play, sleep, vet, compete) routes through the pure lib actions in contexts/game/actions/PetActions.ts, which correctly go through the canonical applyMoneyDelta spend path, apply atomic state updates with race re-checks, and give toast/alert feedback. Tabs, drill-down profiles, empty states, disabled states, and the dark "Slate Glass" design are all coherent and consistent with the game's design language. One real defect exists in the care-pad Feed handler that will mis-route a player who owns only the higher-tier foods.

- ✅ **[MAJOR · broken-functionality]** Care-pad 'Feed' button ignores 3 of the 6 purchasable foods, falsely reporting 'Out of food'\n  `components/mobile/PetApp.tsx:197`\n  handleFeedFromInventory (PetApp.tsx line 197) only searches for owned food among ['basic','premium','luxury']: `const ownedFoodId = ['basic','premium','luxury'].find((id) => (inventory[id] ?? 0) > 0)`. But PET_FOODS (lib/pets/catalog.ts lines 59-66) defines six foods — it also includes 'organic', 'gourmet', and 'treats', all of which are buyable in the Shop and written into gameState.petFood by buyFood(). If a player stocks only organic/gourmet/treats (owning zero basic/premium/luxury), tapping the primary Feed button on the Pets stage / profile care pad flashes 'Out of food — buy some from the shop.' and switches to the Shop tab even though the player owns food and can afford to feed. The pet is never fed. Workaround exists only via the per-tile Feed button in the Shop tab.
- ✅ **[MINOR · feature-gap]** Compete 'Enter' button stays enabled after a pet has already competed this week\n  `components/mobile/PetApp.tsx:971`\n  In renderCompete (PetApp.tsx ~line 971), the Enter button is disabled only by `!eligible` (the stat requirement). After a pet competes, enterCompetition() rejects re-entry for that week (PetActions.ts line 302), and the arena hero shows a 'Competed this week' chip (line 924), but the per-competition Enter buttons remain gold and tappable. Tapping re-rolls Math.random() and then just flashes 'already competed this week'. Functionally safe (the action rejects), but the button should be visibly disabled for the week to match the sleep/ownership disabled-state pattern used elsewhere.

## BitcoinMiningApp (computer)

The app is fundamentally solid and unusually complete. All four tabs (Trade, Mine, Upgrades, Portfolio) plus the two detail sub-views (rig / coin) render real game state and dispatch real, atomic, double-tap-safe actions. Trading routes through the canonical CryptoTradingActions (executeMarketOrder + applyMoneyDelta money guard); mining upgrades/pools/staking/energy/automation each round-trip a real MiningActions call with honest disabled states; the mining yield estimate is the same shared estimator the weekly tick uses, so switching target coin / crossing a halving visibly updates projections. Empty states, feedback alerts, and the dark Slate-Glass/amber design language are all present and consistent. I found one reachable broken-action bug in the Staking picker plus one money-path inconsistency; everything else traced clean.

- ✅ **[MAJOR · broken-functionality]** Staking Max/25%/50% buttons stake the raw stakeCoinId ('btc' default) instead of the displayed effectiveStakeCoinId, so players who don't hold BTC get an enabled button that fails with 'Invalid stake amount'.\n  `components/computer/BitcoinMiningApp.tsx:467`\n  The Staking render derives everything the player sees from effectiveStakeCoinId (lines 837-841): the highlighted coin chip, stakeOwned (line 841), and the enabled-state of the stake buttons (disabled={stakeOwned <= 0}, line 1061). But handleStake (lines 466-478) reads the raw stakeCoinId state: `const coin = cryptos.find(c => c.id === stakeCoinId)` and passes `stakeCrypto(..., stakeCoinId, amount, ...)`. effectiveStakeCoinId falls back to the first owned coin only when stakeCoinId isn't owned. Concrete failure: a player buys only ETH (never BTC) and never taps a coin chip. stakeCoinId stays 'btc' (0 owned); effectiveStakeCoinId becomes 'eth'. The UI shows ETH selected with its balance and an enabled Max button. Tapping Max computes amount from stakeCoinId='btc' (owned 0) -> amount 0 -> stakeCrypto rejects with 'Invalid stake amount' (MiningActions.ts:534). The staking feature is completely dead for any non-BTC-only holder until they happen to tap a chip, with a misleading enabled control and a confusing error. Fix: use effectiveStakeCoinId inside handleStake.
- ✅ **[MINOR · broken-functionality]** Miner purchases and repairs write stats.money directly instead of the canonical applyMoneyDelta path the trading side deliberately uses.\n  `components/computer/BitcoinMiningApp.tsx:384`\n  handleBuyMiner (lines 371-389) does `money: (prev.stats?.money ?? 0) - price` directly, and repairRig/buyMinerUpgrade/energy/automation in MiningActions.ts do the same (e.g. MiningActions.ts:78, 407, 724, 782). By contrast buyCryptoMarket in the same app routes the debit through applyMoneyDelta specifically for the MONEY_CEILING clamp and NaN/overdraft guard (CryptoTradingActions.ts:84-91, with a comment calling this out). The atomic affordability re-check prevents double-spend, so a player won't normally hit a bug, but a corrupt/NaN balance that the trading guard would reject can still be written through the mining buy paths, and these spends bypass whatever expense/net-worth accounting the canonical helper centralizes. Low impact but a genuine inconsistency for the money-flow dimension.

## RealEstateApp (computer)

RealEstateApp is fundamentally solid and works end-to-end. All three tabs (Portfolio, Browse, Activity) render, the local detail sub-page routing is null-safe (a sold property falls back to the list), and every user-facing action is wired to a real state mutator: buy → buyPropertyWithMortgage (with a full quote/preflight/DTI gate and success/reject Alert), and Manage → setPropertyRentMode / stopRenting / evictTenant / maintainProperty / install-decor / add-room / upgrade-tier / toggleLaunderingFront / sellOwnedProperty. All optional-state accesses (p.tenant, p.rooms, p.interior, mortgageId) are guarded, image lookups have an id→keyword→default fallback so no listing can crash on a missing asset, empty-states exist for all three tabs, and results re-render fresh because the manage modal is fed the live resolved property. Design matches the game's dark/glass language (emerald identity accent used as translucent tints, lucide icons throughout, rounded glass cards, theme-aware colors). Findings below are minor consistency/logic gaps, not blockers.

- ✅ **[MINOR · broken-functionality]** Sell/maintain/improve money flows write stats.money directly, bypassing the canonical applyMoneyDelta path the buy flow uses\n  `contexts/game/actions/RealEstateActions.ts:304`\n  buyPropertyWithMortgage deliberately routes its debit through applyMoneyDelta (NaN/overdraft guard + MONEY_CEILING clamp, per the M-7 comment on lines 170-179). But sellOwnedProperty (line 304: `money: cash + result.saleProceeds`), maintainProperty (line 390), installPropertyDecor (line 436), addPropertyRoom (line 480), and upgradePropertyTier (line 522) all write `stats.money` arithmetically. The debit paths are protected by explicit `cash < cost` checks, but the sell CREDIT has no MONEY_CEILING clamp, so a large sale near the money cap can push the balance past the ceiling the rest of the game enforces. Inconsistent with the app's own canonical spend/earn path.
- ✅ **[MINOR · feature-gap]** Commercial listings (office tower, warehouse) can be bought as a 'primary residence' in the buy modal\n  `components/realEstate/BuyPropertyModal.tsx:133`\n  BuyPropertyModal always renders the 'Make this my primary residence' toggle (lines 133-145) regardless of property type, and buyPropertyWithMortgage honors asResidence for any property. Yet RealEstateApp itself treats commercial properties as non-residential everywhere else — ListingCard/detail suppress the comfort/energy amenities via isCommercialCatalogId and show 'Commercial · rent & fronts' instead. A player can therefore designate a warehouse as their home, which yields no comfort/energy (those are gated off for commercial) — an inconsistent, dead outcome. The residence toggle should be hidden for commercial catalog ids.
- ✅ **[POLISH · visual]** A vacant rented unit still shows an asking-rent '/wk' income figure in the earning-green identity color\n  `components/computer/RealEstateApp.tsx:434`\n  In PortfolioRow, weeklyIncomeRow = p.status === 'rented' ? (p.tenant?.weeklyRent ?? p.rent ?? 0) : 0 (line 434). When a unit is in rented mode but has no tenant (vacant), it falls back to p.rent and renders it as `{money}/wk` in the IDENTITY green (lines 480-482), implying active income even though a vacant unit earns nothing. The 'Vacant' badge in OwnedBadges partially mitigates this, but the green per-week figure is misleading.

## OnionApp

The OnionApp is fundamentally solid. Every action traces to a real context/lib function and money flows through the canonical setBtcOwned path. Defects are feedback/no-op gaps and one unguarded-access inconsistency, not structural breakage.

- ✅ **[MINOR · broken-functionality]** Starting an already-active job is a silent no-op\n  `components/darkweb/StartJobModal.tsx:60`\n  StartJobModal only disables cards failing skill reqs (line 60), never templates already active. Selecting an active template and tapping Start closes the modal (OnionApp lines 1439-1443); beginDarkWebJob via startJob (operations.ts line 230) returns ok:false and the updater returns prev unchanged. beginDarkWebJob returns void so no Alert can surface.
- ✅ **[MINOR · feature-gap]** No feedback after a purchase; scam silently debits BTC\n  `components/computer/OnionApp.tsx:409`\n  confirmBuy (OnionApp lines 401-416) calls buyMarketListing with no result handler. attemptPurchase can return outcome scam which debits full cost but grants nothing. Only money action with no post-action Alert (runJobStage Alerts at 421-427). Player only sees a passive forum-log line.
- ✅ **[MINOR · broken-functionality]** Cash-out modal reads dw.cleanBtc/dw.dirtyBtc unguarded\n  `components/computer/OnionApp.tsx:1463`\n  Always-mounted AmountInputModal evaluates dw.cleanBtc.toFixed(4) (line 1463) and maxAmount dw.cleanBtc (1465), plus LaunderModal dirtyBtc (1448), while every other read uses ?? 0. On a partial darkWeb save cleanBtc undefined throws in render.

## GamingApp (YouVideo — computer)

GamingApp works completely end-to-end. Every user-facing action traces cleanly into ContentActions: video publishing (handlePublish → publishVideo), accessory buys (buyAccessory), and PC upgrades (upgradePCComponent) all route money through the canonical applyMoneyDelta path, re-validate caps/energy/ownership atomically inside their state updaters to prevent double-spend, and provide Alert/toast feedback. Tab switching, list→detail routing, and sort chips all mutate real state and re-render. Null-safety is thorough (channel?. with defaults everywhere; computeQuality/monetizationSummary/trend helpers all tolerate undefined), and the projected-reach band guards against NaN/Infinity. Empty states, disabled states, and weekly-cap visibility are all present. Design is consistent with the dark Slate-Glass language (violet accent, deep-navy thumbnails, glass cards, lucide icons, theme-aware). I found no broken handlers, dead gates, out-of-band money flows, crash paths, or unjustified feature gaps — the missing live-streaming UI is an intentional video-vs-stream split with the mobile app. No defects worth a player's attention.

_No findings._

## GamingStreamingApp (Streamly — computer)

This app works completely end-to-end. Every user-facing action traces cleanly into canonical, atomic handlers: Go Live (startLiveStream reserves a weekly slot without charging energy), the real-time drain loop (tickLiveStream drains energy + accrues viewers each second, auto-stops at 0 energy), Stop (finalizeLiveStream is idempotent and pays out via applyMoneyDelta), and the Shop (buyAccessory / upgradePCComponent both gate→debit→grant inside a single atomic setGameState updater, re-checking cost/tier from fresh state so a double-tap can't double-charge). All money flows go through applyMoneyDelta, not ad-hoc mutations. State is null-guarded throughout (channel?., ensureChannel creates a channel on first purchase, computeQuality tolerates undefined gear), so there is no crash path on old/empty saves. The detail sub-views use non-null assertions (detailCategory!, detailBroadcast!) but only render behind explicit inCategory/inBroadcast truthiness gates, so they are safe. Refs (gameStateRef/weekRef) are synced in a post-commit effect and read by the drain interval and the ref-stable Stop handler; saveGame, flash, and setGameState are all stable useCallbacks, so the drain interval subscribes once per live session and the ~10s checkpoint save works as intended. Empty history shows a proper empty-state with a Go Live CTA; disabled states exist for weekly cap, insufficient energy, owned accessories, and maxed PC tiers. paidMembers/membershipRate shown in the Revenue grid are populated by the weekly membership system, not dead. The fuchsia identity accent over a dark slate base is consistent with the game's per-app accent convention (sibling GamingApp uses violet #8B5CF6), and all light-text pills sit over scrimmed box art. No broken handlers, stale renders, always-true/false gates, or list-key issues were found.

_No findings._

## TravelApp (computer)

TravelApp is fundamentally solid and works end-to-end. Every user-facing action traces into a real, well-guarded context action: handleBook→travelTo, handleReturn→returnFromTrip, handlePassport→purchasePassport, handleInvest→investInBusinessOpportunity, and handleActivity→doTravelActivity. All money flows go through the canonical applyMoneyDelta path (overdraft-reject, daily-summary, budget tracking), and each mutation is folded into a single guarded setGameState updater with same-batch double-tap protection. State reads live off gameState so the UI re-renders correctly after each action (activity "Done" chips, invested state, trip progress, passport unlock). Every tab has a proper empty state with a CTA, blocked-action reasons are surfaced (needs-money/needs-energy/passport), CTAs disable correctly, and the return modal gives clear post-action feedback. Invested business opportunities genuinely pay out weekly via lib/economy/passiveIncome.ts. List keys are unique. No no-op handlers, no null-access crashes, no always-true/false gates found. The only observation is a stylistic one about emoji usage.

- 🟡 (kept as-is — deliberate content flavor, same emoji-forward convention as the luxury catalog; converting ~20 destination identities to generic lucide glyphs would erase their distinctiveness) **[POLISH · visual]** Destination identity relies on raw platform emoji as the primary visual glyph on tiles, route headers, and passport stamps\n  `components/computer/TravelApp.tsx:136`\n  DEST_META maps each destination to a platform emoji (🗼🏯🏝️🗽 etc.) rendered at large sizes as the dominant visual on the departures grid tiles (styles.tileEmoji fontSize scale(26), line ~610), the detail/boarding-pass route headers (routeEmoji scale(22), lines ~706, ~933), and passport stamps (stampEmoji, line ~1236). The game's design language favors lucide icons; the rubric flags raw platform emoji used as primary UI icons because they render inconsistently across iOS/Android/web and don't match the teal/slate icon system. This is clearly a deliberate flavor choice (all functional/interactive controls correctly use lucide icons), so it's cosmetic, not broken — but it is the one place the app departs from the icon convention.

## PoliticalApp (computer)

PoliticalApp works end-to-end. All three tabs (Office, Policies, Influence) and every sub-view (career ladder, bill detail, lobbyist detail, hire-lobbyist picker, form-alliance picker) render real PoliticsState data, and every user action routes into a verified game action: runForOffice / campaign / joinParty / enactPolicy / hireLobbyist / formAlliance in PoliticalActions.ts, and raisePACClean / raisePACDirty / spendPACOnCampaign / suppressPoliticalScandal via lib/politics/operations. Money flows all go through Math.max(0, prev.stats.money - cost) inside atomic setGameState updaters with same-batch double-tap guards, elections/rewards are re-checked against fresh prev, and state re-derives through useMemo so views refresh after each action. Empty states, disabled/affordability gates, keys, and the dark design language are all handled correctly. The one substantive problem is the reuse of the USD-only AmountInputModal for the BTC-denominated dirty-funnel flow.

- ✅ **[MINOR · broken-functionality]** The BTC dirty-funnel reuses the USD-only AmountInputModal, so its unit prefix, presets and Max button are all wrong for a Bitcoin quantity\n  `components/computer/PoliticalApp.tsx:1100`\n  showRaiseDirty passes a BTC quantity to AmountInputModal (maxAmount={btcOwned}, presets={[0.1, 0.5, 1]}). But AmountInputModal is hardcoded for dollars: it renders a literal '$' prefix (AmountInputModal.tsx:66), labels the presets '$0.1'/'$0.5'/'$1' via `${p.toLocaleString()}` (line 94), shows 'Exceeds available {formatMoney(maxAmount)}' i.e. a dollar-formatted BTC count (line 109), and — most damaging — the Max chip sets `String(Math.floor(maxAmount))` (line 99). For any holding under 1 BTC (e.g. 0.3 BTC) Math.floor rounds to 0, so Max produces an invalid amount=0 and does nothing; for e.g. 1.6 BTC it silently truncates to 1. Combined with presets 0.5/1 exceeding a sub-1-BTC balance, a player with a fractional BTC holding can effectively only use the 0.1 preset or manual entry, and every label tells them they are spending dollars rather than Bitcoin. The other four modals in this file (clean raise, PAC spend, suppress, campaign) all pass genuine USD amounts and are fine.

## StatisticsApp (computer)

StatisticsApp is a solid, crash-hardened stats dashboard. It's overwhelmingly a read-only presentation of existing state (net worth, vitals rings, trend sparklines, records, career, systems summary, milestones timeline, life-expectancy/FIRE/retirement planning, past lives), and every value it reads is defended: `s` is spread over `getDefaultStatistics()` so the history/career arrays are never undefined, `safeNum`/`clampPct`/`metricRange` filter non-finite values, and all optional state (`stats?.*`, `previousLives?.[i]`, `pl.*`) is accessed with guards or falls back to a sensible render (renderSystemDetail/renderLifeDetail return a valid view when the id is missing). Tab switching and local list→detail routing via useState are correct, headerTitle covers every DetailView kind, list keys are stable, and empty states exist for Systems, Milestones, Records-detail and Career-detail. The one real action — claiming a milestone's one-time gem reward — routes through `claimMilestoneReward`, which is pure/idempotent (returns the same state and granted:0 when already-claimed or unreached, so double-taps can't double-grant), records the id in the additive `claimedMilestoneRewards` set, and adds to `stats.gems` via the exact clamped `Math.min(CEILING, base + reward)` path used by lib/ambitions/progress — the canonical gems flow, not a bypass. The chip flips to a "Claimed" state from gameState and fires haptic.success, so there is post-action feedback and no stale render. All player stat fields the vitals rings read (health/happiness/fitness/energy/reputation) exist on the type. I found no broken handlers, no-ops, crashes, or economy leaks — only a minor design-consistency nit.

- ✅ **[POLISH · visual]** Raw 💎 platform emoji used as the icon inside the primary Claim button and the Claimed tag, inconsistent with the app's otherwise all-lucide iconography\n  `components/computer/StatisticsApp.tsx:1238`\n  In TimelineItem the claim affordance renders `Claim +{reward}💎` (line 1238) and the claimed state renders `Claimed +{reward}💎` (line 1229) using a raw diamond emoji. Every other icon in this app is a lucide-react-native glyph (Sparkles, Trophy, Award, etc.); the emoji renders with platform-specific styling/color that ignores the accent.gold theming around it and reads inconsistently against the dark Slate Glass design language. A lucide Gem/Sparkles glyph tinted with accent.gold would match the rest of the UI.

## VehicleApp (computer)

This app is fundamentally solid and works end-to-end. Every user-facing action traces cleanly into a real state mutation: refuel/repair/sell/set-active/buy-insurance/cancel-insurance all call the corresponding VehicleActions functions, which perform atomic functional setGameState updates with double-tap re-check guards (sellVehicle H-9 ownership re-check, purchaseInsurance/cancelInsurance batch re-checks). Money flows go through the canonical paths (applyMoneyDelta for the auto-loan down payment, trackBudgetSpend for budget accounting, clamped stats.money writes). The car-buy path (BuyVehicleModal -> purchaseVehicleWithAutoLoan) correctly originates an auto loan linked by vehicleId, grants reputation, and shows a success/failure Alert. Empty states exist for every list (garage, dealership category, insurance), the dealership filters cover all four template types (car/luxury/sports/motorcycle) so no vehicle is stranded, all templates have images, and the detail sub-page, license gate, and insurance-active state-hiding are all handled. Design is internally consistent with the intended orange identity accent, glassmorphism cards, lucide icons, and rounded radii; the orange (not blue) accent is a deliberate, documented per-app identity choice. I found no crashes, no-op handlers, broken gates, or stale-render bugs — only one minor CTA-affordance inconsistency.

- ✅ **[MINOR · visual]** Refuel/Repair/insurance-Buy action chips never show a disabled or unaffordable state, unlike the license and dealership-buy buttons which do\n  `components/computer/VehicleApp.tsx:469`\n  handleRefuel/handleRepair/handleBuyInsurance are wired to ActionChip/TouchableOpacity that are always rendered as fully-styled active CTAs regardless of the player's cash (e.g. lines 469, 472-473, 633-635, 875-887). The underlying actions guard affordability and return {success:false} with a message, so tapping 'Refuel $50' at $0 just fires an Alert('You need $50...') rather than being prevented. This is inconsistent with renderLicensePrompt (line 257, disabled={cash < cost} with a greyed style) and the dealer buy card (line 771, disabled + opacity 0.55 + 'License needed'/'Locked' label). A player with no cash sees inviting orange buttons that only error on tap. Feedback works but the visual affordance is inconsistent across the same screen.

## LuxuryApp

Solid end-to-end. Buy/Sell route through canonical applyMoneyDelta with in-updater ownership re-checks. Weekly upkeep/happiness/prestige applied. Null-safe, good empty states and feedback. No defects found.

_No findings._

## Phone & Computer shells (app/(tabs)/mobile.tsx, app/(tabs)/computer.tsx)

Both launcher shells work end-to-end. Each guards device ownership with null-safe lookups before rendering the grid, shows a proper "no phone/no computer" empty state, and routes taps through setActiveApp into a component map that is defensively guarded: an unknown/stale activeApp id resets to the grid instead of hard-crashing with "Element type is invalid". Every app id in both grids maps to a real, eagerly-imported component (no unlocked-but-unimplemented tiles), and all 18 computer apps are covered by the desktop/mobile category split with no orphans. The id-to-alias plumbing (Spark=tinder, Pulse=social, Crypto=bitcoin, DarkWeb=onion, Pets=pet on phone / paw on computer) is handled consistently in both getAppBadgeCounts and getAppIconAsset, so badges and custom PNG icons resolve correctly and fall back to Lucide glyphs when a PNG is missing. Feedback (haptic+sound via useFeedback.buttonPress) is wired and non-trivial. Jail and device-sold redirects are correctly skipped when embedded. Only minor cosmetic/parity nits remain.

- ✅ **[MINOR · visual]** Mobile grid card width computed with a larger gap (md) than the layout actually renders (sm), so cards are sized for spacing they don't get\n  `app/(tabs)/mobile.tsx:268`\n  mobile.tsx line 268 sets cardGap = responsiveSpacing.md and derives cardWidth from it, but the appsGrid style (line 401) renders gap: responsiveSpacing.sm with justifyContent: 'space-evenly'. Because cards are sized for the larger md gap, the leftover width is redistributed by space-evenly, producing slightly inset and uneven column spacing that doesn't align with the header's horizontal padding. computer.tsx does this correctly (line 414 cardGap = responsiveSpacing.sm, matching its grid gap). No overflow — purely a spacing inconsistency between the two shells.
- ✅ **[POLISH · broken-functionality]** Computer app-close (onBack) gives no haptic/sound feedback, unlike the mobile shell\n  `app/(tabs)/computer.tsx:405`\n  In mobile.tsx the onBack handler (lines 258-262) calls buttonPress() and haptic('light') when closing an app. In computer.tsx the equivalent handler is onBack={() => setActiveApp(null)} (line 405) with no feedback call, even though opening an app on the computer does fire buttonPress() (line 453). The result is asymmetric feedback: opening buzzes/clicks, closing is silent, and it diverges from the phone shell's behavior. Not a functional break, just an inconsistency a player could notice.

