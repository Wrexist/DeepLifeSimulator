# Signed candidate inventory — 10 September 2026

Read-only App Store Connect observation for DeepLife, app `6749675615`.

- Latest uploaded version: **2.14.0 (186)**, uploaded September 6 at 22:03 in
  the dashboard's displayed time. Processing: **Validated**; testing listing:
  **Ready to Submit**. This does not mean approved for the App Store.
- Original file: `b83b44ec-ac32-4910-8b3a-bdf429485cc9.ipa`.
- Bundle: `com.deeplife.simulator`; SDK `23F81a`; symbols present;
  non-exempt encryption: No. Minimum iOS 15.1; iPhone and iPad; arm64.
- Internal groups: Team Expo and Tester. One install shown; session/crash
  columns show dashes, not measured zero crashes. What to Test is empty.
- Previous uploads: 2.13.0 (185), September 4; 2.12.0 (184), August 30.

The inspected September 6 source `cc60c2e` predates PR #209 fixes. Current local
package version is still 2.13.0. Reconcile next binary version, remote build
number, exact source and environment before creating a candidate. Do not reuse
old instructions that call 2.13.0 the next upload.

Live store version is 1.5.5, confirmed by read-only workflow `34513497670`.
The 1.5.6 plan in `34513608084` was successful and proposed three writes, but
performed none. It does not choose or create the next release. Binary and
store-version metadata need deliberate reconciliation.

Apple uses the bundle's version number to associate an upload with the store
version record. A successful standalone 1.5.6 metadata plan is **not** proof
that a 2.x binary can be selected for it. Check the actual exported
`CFBundleShortVersionString` and eligible build list before preparing the final
record. See [Apple upload guidance](https://developer.apple.com/help/app-store-connect/manage-builds/upload-builds)
and [build selection](https://developer.apple.com/help/app-store-connect/manage-builds/choose-a-build-to-submit).

Native purchases, ads, consent, save upgrades, termination/relaunch,
accessibility and compact iPhone/iPad acceptance remain unverified for the
current candidate. A browser preview and the older upload cannot close them.
