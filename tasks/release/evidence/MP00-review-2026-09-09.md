# MP00 review corrections

Inspected PR #203 head `4b1aa7be9e2464c1db06515ffd2ab789205174ad`.
Main advanced during this review to merge `c32f2b9a395ce9f617c5f80f08a0a59d2c0f6883`;
these corrections therefore belong in a follow-up PR, not the already merged branch.

## Reproduced findings

- The real mounted GameProvider rejected both saveGame and loadGame promises when
  another operation retained the mutex. Both regression cases failed before the fix.
- The release status command exited successfully and printed verified packages
  referencing unpublished hashes. Git could not resolve those objects.

## Changes and acceptance criteria

- Acquisition now runs inside existing save/load error handling. Failed saves return
  false; failed loads return null. Only the operation that acquired the lock releases
  it and clears its loading indicator. Future-version load errors still rethrow.
- CLI commands resolve every evidence commit and require candidate ancestry.
  Missing/shallow history, unknown hashes and unrelated branches fail closed.
- Four original local hashes are explicitly labeled unpublished; their evidence now
  points to published consolidated commit `13fe9f315dd53ea141f98eaf1bde76db3845b16b`,
  whose diff contains the save, research, workflow and privacy changes. Historical
  command reports are retained as historical reports, not newly executed proof.

## Verification

- Mounted save/load and workflow/provenance tests: 3 suites, 18 tests pass.
- Full save and stress suites: 81 suites, 1,235 tests pass.
- Release status validates actual published ancestry and continues to report HOLD
  for seven external release packages. None has been promoted to verified.
- Latest source/test types, lint and remote CI must pass before merge.

Review boundary: these fixes do not establish signed candidate, native purchases,
device acceptance, provider privacy settings, or App Store approval.
