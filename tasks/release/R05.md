# R05: Verify economy, continuity and performance

Priority: P1. Dependencies: R02.

## Master prompt

Run existing stress, save/load, retention and reproducibility harnesses against the changed candidate. Measure money conservation, finite stats, replay identity, bounded reward claims and long-life tick behavior. Separate wall-clock performance in this environment from device performance. Fix reproducible regressions without adjusting budgets simply to pass.

## Acceptance criteria

- Existing stress and applicable simulations pass
- Known warnings individually triaged
- Measurements identify commit and environment
- No new player-facing P0/P1 or unresolved in-scope P2

Apply `tasks/release/CONTRACT.md`. Re-check current source before implementing.

## Completed cleanup continuation

### Bounded cleanup investigation

Inspect candidate `b5aa2a8` (application/test tree unchanged from `13fe9f3`).
Split the normal suite by domain and use a temporary async-resource tracer at
Jest environment teardown, without cancelling resources or forcing process exit.
Render/startup/e2e and save/services/stress/integration splits have exited cleanly;
the tracer identified orphaned 500ms storage-readiness race timers. Reproduce their
ownership with fake-timer assertions in `__tests__/utils/storageWrapper.test.ts`,
then clear only the readiness deadline when its attempt settles in
`utils/storageWrapper.ts`. Preserve the timeout, retry and fallback behavior.
Continue isolating any longer-lived resource before attributing the full-run hang
to these short timers. Run affected storage and render tests, type checks and the
normal candidate gate; record actual exits. No fixture-wide timer purge, changed
budget, schema change, gameplay change or UI work is in scope.

The actions split also exposed provider intervals left pending at teardown by
`__tests__/actions/jailActivityMessage.test.ts`. Await unmount in `act` from
`afterEach` and assert every interval created by the fixture was cleared. The
monetization split traced a 90-second adapter timeout to the subscription routing
test in `__tests__/monetization/expoIapAdapter.test.ts`: it starts a purchase but
never settles it. Complete that mock StoreKit request through its real cancellation
listener, await its result and assert deadline cancellation. Do not change native
purchase behavior or shorten its production timeout.

Implementation `591f372345d0be784169da7a96ac69827945f4ce` closes this local package.
The full serial suite and affected open-handle diagnostic now exit cleanly, and
120-week save/load, five-persona replay and retention reruns pass. Exact commands,
counts, performance caveats and remaining external gates are in
`tasks/release/evidence/R05-cleanup-2026-09-09.md`. Inspect CI on each new pushed
head separately under R08. Reopen R05 only if new evidence invalidates its acceptance;
do not repeat the unchanged cleanup investigation.
