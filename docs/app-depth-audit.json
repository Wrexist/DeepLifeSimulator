[
 {
  "app": "BankApp",
  "inventory": "Phone banking (Apple-Wallet card-deck DNA) reading gameState.banking + gameState.loans[]. Player CAN: view overview hero (cash/bank/invested/debt + lifetime ledger chips) and a cross-app Invested tile (stocks+crypto); open self-managed accounts (savings 2%, HYSA 4.5% $1k min, 52-wk CD 5.5% locked, money-market 3.5% $2.5k min) via OpenAccountModal; deposit-from-cash / withdraw-to-cash / close (residual->cash); checking-default & savings-default are read-only mirrors of stats.money/bankSavings (MIRRORED_ACCOUNT_IDS). Loans: apply (LoanQuoteModal->acceptLoan, gated by credit-score min + 0.43 DTI + politics/Private-Banking APR mods), weekly autopay (applyLoanAutopay.ts), prepay. Credit cards: apply (ApplyCardModal, score-gated tiers). Savings goals: create (category) + contribute. Auto-pay bills: add/toggle/delete (weekly tickBillPay debits account, $35 late fee on fail). Credit report sub-page: real history sparkline, 5-factor weighted breakdown, inquiries. Key state: banking.{accounts,creditCards,billPayRules,budgetSpend,creditScore{score,band,componentBreakdown,history,inquiries},savingsGoals,totalLateFeesPaid,totalInterestEarned,totalInterestPaid,taxDueThisYear,lastEconomyState}.",
  "broken": [
   {
    "what": "'Interest earned' lifetime stat is permanently $0 \u2014 banking.totalInterestEarned has no writer anywhere. accrueAccountInterest() computes totalInterest but weeklyTick discards it (only takes .banking), and the legacy savings-interest result is never added either. The chip is shown prominently and also feeds crossSystemSummary.",
    "evidence": "lib/banking/weeklyTick.ts:147 (accrueAccountInterest(banking).banking \u2014 totalInterest dropped); shown BankApp.tsx:543",
    "severity": "unread-state"
   },
   {
    "what": "'Interest paid' lifetime stat is ~permanently $0 in normal play. It is written only by applyLoanPayment (operations.ts:654), whose sole caller payLoanWeekly (LoanActions.ts:221) is never invoked in gameplay. The actual weekly loan payment path (applyLoanAutopay) computes interest via remainingWithInterest but never records it to totalInterestPaid.",
    "evidence": "contexts/game/actions/weekly/applyLoanAutopay.ts:99-107 (real path, no interest tracking); dead writer LoanActions.ts:221; shown BankApp.tsx:544",
    "severity": "unread-state"
   },
   {
    "what": "Savings-goal 'Contribute' spends no money and pays no reward. contributeToGoal only increments goal.currentAmount; it never debits stats.money or any account, yet the modal presents it as a cash spend ('Cash on hand: $X', maxAmount=cash). currentAmount is read only for the progress bar, has no cap at target, and hitting target does nothing \u2014 goals are free-to-fill cosmetic bars.",
    "evidence": "lib/banking/operations.ts:546-559 (no money debit); misleading modal BankApp.tsx:788-804; display-only read SavingsGoalCard.tsx:40",
    "severity": "no-op"
   },
   {
    "what": "Credit-card spending is unreachable, so the entire card loop past 'Apply' is dead. chargeCreditCard's only caller spendOnCard has no gameplay callsite (only a dead-player-guard test), so card.balance never rises: utilization stays 0%, payDownCard always rejects ('zero balance'), and no rewards ever accrue. Applying still helps creditMix/inquiries, but a card is otherwise inert.",
    "evidence": "lib/banking/operations.ts:286 (chargeCreditCard) called only by BankingActions.ts:221 (spendOnCard) which has no non-test caller",
    "severity": "dead-feature"
   },
   {
    "what": "Credit-card rewards can never be redeemed. redeemCardRewards/redeemRewards are fully implemented (with a $10k/redeem cap) but no UI surfaces a Redeem button; CreditCardRow displays pendingRewards with no way to cash it out (doubly dead because charging is also unreachable).",
    "evidence": "lib/banking/operations.ts:380 + BankingActions.ts:290 (redeemRewards) \u2014 zero UI callers; pendingRewards shown CreditCardRow.tsx:67-70",
    "severity": "dead-feature"
   },
   {
    "what": "Advertised credit-card annual fee is never charged. card.annualFee ($95 gold / $495 platinum) is stored and shown in the apply sheet but no tick ever deducts it, making higher tiers strictly better (more cashback, zero downside).",
    "evidence": "lib/banking/operations.ts:269 (annualFee stored), ApplyCardModal.tsx:136 ('$95/yr'); no reader/deduction anywhere",
    "severity": "no-op"
   },
   {
    "what": "taxDueThisYear is never accrued \u2014 always 0. The type comment promises 'Capital-gains/tax accrual \u2014 debited yearly' but no code path increments it, so the 'Tax due' ledger chip (guarded >0) never renders.",
    "evidence": "contexts/game/initialState.ts:68 (=0), only other write utils/saveMigrations.ts:254; permanently-hidden chip BankApp.tsx:546-548",
    "severity": "dead-feature"
   },
   {
    "what": "Economy 'rates changed' notifications are cosmetic \u2014 no banking rate ever moves. The weekly tick emits 'Loan rates rising; savings yields falling' on economyState change, but accrueAccountInterest uses the fixed acct.baseAPR and quoteLoan uses a fixed baseByType table with no economy input.",
    "evidence": "lib/banking/weeklyTick.ts:181-191 (notification) vs fixed rates operations.ts:186-206 & operations.ts:591-596",
    "severity": "dead-feature"
   },
   {
    "what": "Account-to-account transfer is fully built and unit-tested but has no UI in the phone app. Moving savings->CD requires a two-step withdraw-to-cash then deposit. (Also refreshCreditScore, recordCategorizedSpend are defined with no callers.)",
    "evidence": "contexts/game/actions/BankingActions.ts:114 (transferBetweenOwnAccounts) + operations.ts:124 \u2014 no BankApp button",
    "severity": "dead-feature"
   }
  ],
  "proposals": [
   {
    "name": "Live rate environment (finish the cosmetic notification)",
    "pitch": "Recession/boom/crash actually shift deposit APY and new-loan APR, so the existing 'rates changed' notification finally has teeth.",
    "fitsBecause": "The notification, lastEconomyState tracking, and (on computer) EconomyEventBanner already exist \u2014 only the rate math is missing. Overview already surfaces per-account APR and a debt figure to react to.",
    "stateChanges": "Optional banking.rateEnvironment?: { depositMult: number; loanDelta: number } read via ?? {depositMult:1,loanDelta:0}; init in initialState + STATE_VERSION 22 migration (pattern of v14 banking).",
    "actions": "accrueAccountInterest gains an optional depositMult param; quoteLoan gains optional loanDelta (LoanActions already has prevState.economy in scope).",
    "uiSurface": "A small 'Rate outlook' chip in the overview ledger strip next to the existing APR/earned/paid chips (Wallet DNA).",
    "tests": "weeklyTick + quoteLoan unit tests per economyState; invariant test: max effective deposit APY < min loan APR (no borrow-low/save-high arbitrage).",
    "effort": "M",
    "risk": "Arbitrage if a boosted deposit rate exceeds the loan floor \u2014 mitigate by clamping deposit boost below SAVINGS_APR_HARD_CAP and asserting the invariant in a test.",
    "tickHook": "Extend runWeeklyBankingTick: derive depositMult/loanDelta from input.economyState, apply in the interest-accrual step.",
    "content": "A small economyState->{depositMult,loanDelta} table in lib/banking/ (data module)."
   },
   {
    "name": "Real savings goals: fund from an account, cap, celebrate",
    "pitch": "Contributing actually moves owned money into the goal, progress can't exceed target, and completing a goal fires a bounded reward.",
    "fitsBecause": "savingsGoals + the (currently dead) linkedAccountId/targetWeek fields already exist; fixes the no-op Contribute at the same time. Wallet card DNA already renders each goal as a card.",
    "stateChanges": "Reuse linkedAccountId (now read) + targetWeek (pace); add optional completedWeek?: number and autoContribute?: number, defaulted via ?? ; STATE_VERSION 22 migration backfills nothing (all optional).",
    "actions": "Fix contributeToSavingsGoal to debit the linked account (or cash) so assets are conserved; add setGoalAutoContribute(goalId, weeklyAmount).",
    "uiSurface": "SavingsGoalCard gains a linked-account picker, an on-track/behind chip from targetWeek, and a completion ribbon (on-DNA).",
    "tests": "contributeToGoal debits source and conserves total assets; completion sets completedWeek once (idempotent); pace calc.",
    "effort": "M",
    "risk": "Double-counting money (goal + account both counted) \u2014 mitigate by pulling FROM an account balance rather than minting a new pool; completion reward is happiness-only or <=1% of target capped at $500.",
    "tickHook": "New contexts/game/actions/weekly/applySavingsGoals.ts: sweep autoContribute from linkedAccountId, mark completion, grant bounded reward.",
    "content": "Category->default target-week suggestions in lib/banking/."
   },
   {
    "name": "Living credit card: charge, carry, redeem, pay the fee",
    "pitch": "Route opt-in discretionary spend to a card so it carries a balance (interest cost), earns cashback you can finally redeem, and pays its real annual fee.",
    "fitsBecause": "chargeCreditCard, payCreditCard, redeemCardRewards and card.annualFee are ALL already implemented and tested \u2014 only callers/UI and a fee tick are missing.",
    "stateChanges": "None required (uses existing card.balance/pendingRewards/annualFee); optional card.lastFeeWeek?: number to make the anniversary fee idempotent, defaulted via ??.",
    "actions": "Wire spendOnCard behind a 'Charge to card' affordance and redeemRewards behind a Redeem button; both already guard dead players.",
    "uiSurface": "CreditCardRow gets a Redeem chip (shows pendingRewards) and the card detail a 'Put weekly lifestyle spend on this card' toggle (Wallet card face).",
    "tests": "weekly card tick accrues interest on carried balance, deducts annualFee once per 52 weeks, increments totalInterestPaid; redeem caps at $10k; rewards<interest when carrying.",
    "effort": "L",
    "risk": "Rewards-vs-interest arbitrage \u2014 already mitigated (rewards accrue only on repaid principal); carrying a balance now costs interest so it's never net-positive.",
    "tickHook": "New contexts/game/actions/weekly/applyCardInterestAndFees.ts wired beside applyLoanAutopay.",
    "content": "None beyond existing CARD_TIER_DEFAULTS."
   },
   {
    "name": "CD maturity moments + rollover",
    "pitch": "When a CD crosses its lock week, fire a notification and offer roll-over (relock at current rates) or sweep-to-cash, and drop the post-maturity rate to base savings so a matured CD stops strictly dominating HYSA.",
    "fitsBecause": "CDs, lockUntilWeek, and the account-detail sub-page already exist in the phone app; pairs naturally with the live-rate proposal.",
    "stateChanges": "Optional account.maturedNotified?: boolean (defaulted via ??); no migration needed (optional).",
    "actions": "rolloverCD(accountId, termWeeks) relocks at the current CD rate; sweepMaturedCD(accountId) moves balance to cash.",
    "uiSurface": "Account-detail hero shows a 'Matured \u2014 roll over / sweep' CTA when weeksLived>=lockUntilWeek (reuses existing detail page).",
    "tests": "maturity detection idempotent (notifies once); post-maturity accrual uses base savings APR until rolled; rollover relocks lockUntilWeek.",
    "effort": "M",
    "risk": "Low \u2014 money is conserved; removes an existing mild imbalance rather than adding one.",
    "tickHook": "Extend runWeeklyBankingTick to detect lock-week crossings and emit the notification + rate reversion."
   }
  ],
  "flawlessChecklist": [
   "Make banking.totalInterestEarned actually accumulate: in weeklyTick capture accrueAccountInterest's totalInterest (+ the legacy savings interest) into totalInterestEarned so the 'Earned' chip and crossSystemSummary stop reading $0 (lib/banking/weeklyTick.ts:147).",
   "Make banking.totalInterestPaid actually accumulate on the REAL weekly loan path: have applyLoanAutopay surface interest (remainingWithInterest\u2212remaining) and add it to totalInterestPaid, so the 'Paid' chip works (applyLoanAutopay.ts:99-107).",
   "Fix savings-goal Contribute to debit real money (cash or a linked account) so it isn't a free no-op while the modal says 'Cash on hand' (operations.ts:546-559; BankApp.tsx:788-804).",
   "Resolve the credit-card dead loop: either wire spendOnCard + redeemRewards + charge the annualFee, or hide the unreachable affordances (rewards display, advertised fee) so the UI stops promising unimplemented behavior (operations.ts:269,286,380).",
   "Reconcile the economy 'rates rising/falling' notification with reality: either apply economyState to deposit/loan APRs or soften the copy so it doesn't claim rate moves that never occur (weeklyTick.ts:181-191).",
   "Either implement taxDueThisYear accrual or remove the permanently-hidden 'Tax due' ledger chip so the field isn't dead (initialState.ts:68; BankApp.tsx:546-548)."
  ]
 },
 {
  "app": "AdvancedBankApp",
  "inventory": "Computer 'Bank Pro' (desktop-statement DNA) over the SAME gameState.banking slice + BankingActions/LoanActions. Four tabs: Statement (assets/liabilities/net-worth columns, net-worth COMPOSITION ledger incl. stocks/crypto/real-estate, Activity summary with interest/fees/tax, credit gauge->report), Accounts (deposit summary + blended APY, AccountRow list, savings goals), Borrow (debt summary, loans with prepay/REFINANCE via openLoanActions, credit cards), Budget (per-week spend bars + BudgetBreakdown per-category from banking.budgetSpend ring buffer, auto-pay rules). Account statement + credit-report sub-pages mirror the phone app. Unique vs phone: refinanceLoan (re-price at current score + hard inquiry), the Budget tab, blended-APY strip. Same underlying loans/cards/goals/bills state as BankApp.",
  "broken": [
   {
    "what": "'Interest earned' activity row is permanently $0 (banking.totalInterestEarned never written; accrueAccountInterest's totalInterest is discarded in the weekly tick).",
    "evidence": "lib/banking/weeklyTick.ts:147; row rendered AdvancedBankApp.tsx:314",
    "severity": "unread-state"
   },
   {
    "what": "'Interest paid' activity row is ~permanently $0: only applyLoanPayment writes totalInterestPaid and its caller payLoanWeekly is never used; the real weekly path applyLoanAutopay never records interest.",
    "evidence": "contexts/game/actions/weekly/applyLoanAutopay.ts:99-107; row rendered AdvancedBankApp.tsx:315",
    "severity": "unread-state"
   },
   {
    "what": "Borrow tab advertises '$X rewards' but rewards are unreachable \u2014 spendOnCard (charging) has no gameplay caller so pendingRewards stays 0, and redeemRewards has no UI, so even accrued rewards can't be cashed out.",
    "evidence": "totalRewards meta AdvancedBankApp.tsx:581,605; unreachable chargeCreditCard operations.ts:286 / spendOnCard BankingActions.ts:221; no redeem UI (redeemRewards BankingActions.ts:290)",
    "severity": "dead-feature"
   },
   {
    "what": "Credit-card annual fee is never charged, so Card 'usage' has no cost of ownership and gold/platinum are strictly better than standard.",
    "evidence": "lib/banking/operations.ts:269 (annualFee) advertised ApplyCardModal.tsx:136; never deducted",
    "severity": "no-op"
   },
   {
    "what": "Savings-goal 'Contribute' (Accounts tab) debits no cash and rewards nothing on completion; the goals meta ('$X of $Y saved') is a free-to-fill cosmetic tracker.",
    "evidence": "lib/banking/operations.ts:546-559; modal AdvancedBankApp.tsx:1142-1158; meta AdvancedBankApp.tsx:516-517",
    "severity": "no-op"
   },
   {
    "what": "'Tax accrued this year' activity row never appears \u2014 taxDueThisYear is never incremented (always 0) and the row is guarded >0.",
    "evidence": "contexts/game/initialState.ts:68; guarded row AdvancedBankApp.tsx:318-320",
    "severity": "dead-feature"
   },
   {
    "what": "The Budget tab tracks spend but offers no targets/limits \u2014 categories can't be capped and overspend is never flagged, so the tab is read-only reporting with no goal to act against. (Also economy 'rates changed' notifications + the banking EconomyEventBanner promise rate moves that never happen.)",
    "evidence": "Budget tab AdvancedBankApp.tsx:623-707 (no target state); cosmetic banner AdvancedBankApp.tsx:382 vs fixed rates operations.ts:591-596",
    "severity": "dead-end"
   }
  ],
  "proposals": [
   {
    "name": "Budget targets + overspend alerts (Budget tab)",
    "pitch": "Let the player set a weekly cap per BudgetCategory; the Budget tab flags over-cap categories and the weekly tick raises an overspend notification.",
    "fitsBecause": "The budgetSpend ring buffer, BudgetBreakdown, and per-week bars are computer-only and already built \u2014 only per-category targets are missing, turning passive reporting into a loop.",
    "stateChanges": "Optional banking.budgetTargets?: Partial<Record<BudgetCategory, number>> read via ?? {}; init [] -> {} in initialState + STATE_VERSION 22 migration (v14 pattern).",
    "actions": "setBudgetTarget(category, amount) in BankingActions following the setGameState/updater convention.",
    "uiSurface": "BudgetBreakdown rows gain a target bar + over/under chip; a 'Set budget' affordance on the Budget tab (statement DNA).",
    "tests": "setBudgetTarget persistence; weekly tick pushes an overspend notification exactly once when a category's weekly total exceeds its target.",
    "effort": "S",
    "risk": "None \u2014 informational only (no money effect), so no economy exposure.",
    "tickHook": "Extend runWeeklyBankingTick to compare the current week's byCategory totals to budgetTargets and emit a notification.",
    "content": "Default per-category target suggestions in lib/banking/."
   },
   {
    "name": "Live rate environment on the statement",
    "pitch": "Economy state shifts deposit APY and new-loan APR so the Bank Pro statement's rate figures and the EconomyEventBanner reflect real conditions.",
    "fitsBecause": "Shared banking core; the computer app already renders EconomyEventBanner context='banking' and a blended-APY strip that would visibly respond.",
    "stateChanges": "Same optional banking.rateEnvironment as the phone proposal (shared implementation) \u2014 read via ??, STATE_VERSION 22 migration.",
    "actions": "accrueAccountInterest depositMult param; quoteLoan loanDelta param (economy already in scope in LoanActions).",
    "uiSurface": "A 'Rate outlook' line in the Statement masthead / Accounts blended-APY strip; refinance quotes automatically reflect the new environment.",
    "tests": "per-economyState rate deltas; arbitrage invariant (max deposit APY < min loan APR); refinance uses the shifted rate.",
    "effort": "M",
    "risk": "Borrow-low/save-high arbitrage \u2014 clamp deposit boost below the loan floor and assert it in a test.",
    "tickHook": "Same extended runWeeklyBankingTick rate step as the phone proposal (build once, both apps benefit).",
    "content": "economyState->rate-delta table in lib/banking/."
   },
   {
    "name": "Living credit card loop (Borrow tab)",
    "pitch": "Wire card charging, cashback redemption, carried-balance interest, and the real annual fee so the Borrow tab's cards and '$X rewards' become a genuine loop.",
    "fitsBecause": "chargeCreditCard/payCreditCard/redeemCardRewards/annualFee are already implemented and tested; the Borrow tab already surfaces card usage % and rewards totals \u2014 only wiring + a fee tick are missing.",
    "stateChanges": "None new (uses card.balance/pendingRewards/annualFee); optional card.lastFeeWeek?: number for idempotent anniversary fee, defaulted via ??.",
    "actions": "Wire spendOnCard behind a 'Charge to card' control and redeemRewards behind a Redeem button on CreditCardRow.",
    "uiSurface": "Borrow tab: Redeem chip on each card + a card-detail toggle to route weekly lifestyle spend onto the card (statement DNA).",
    "tests": "card weekly tick: interest on carried balance, annualFee once/52 weeks, totalInterestPaid increments; redeem cap $10k; carrying never nets positive.",
    "effort": "L",
    "risk": "Rewards arbitrage \u2014 already mitigated (rewards only on repaid principal) + interest on carried balance.",
    "tickHook": "New applyCardInterestAndFees.ts weekly module (shared with phone).",
    "content": "Uses existing CARD_TIER_DEFAULTS."
   },
   {
    "name": "Account transfers + real savings-goal funding",
    "pitch": "Expose the already-built transferBetweenOwnAccounts as a Transfer control, and route savings-goal contributions through a linked account so they move real money.",
    "fitsBecause": "transferBetweenAccounts is complete + tested but UI-less, and the goal's linkedAccountId field already exists but is dead; both are pure wiring on the Accounts tab.",
    "stateChanges": "Reuse SavingsGoal.linkedAccountId (now read) + add optional completedWeek?: number; no required migration (optional fields).",
    "actions": "Add a transferBetweenOwnAccounts UI path (source/dest picker + amount); fix contributeToSavingsGoal to debit the linked account so total assets are conserved.",
    "uiSurface": "Accounts tab: a Transfer action between accounts, and a linked-account picker + completion ribbon on SavingsGoalCard.",
    "tests": "transfer conserves total balance and respects locks/min-balance; goal contribution debits source; completion idempotent.",
    "effort": "M",
    "risk": "Money double-count if a goal mints a new pool \u2014 mitigate by pulling strictly from an existing account balance.",
    "tickHook": "Optional: applySavingsGoals.ts sweeps auto-contributions (shared with phone proposal).",
    "content": "None."
   }
  ],
  "flawlessChecklist": [
   "Fix the Statement 'Interest earned' / 'Interest paid' activity rows to reflect real accrual: accumulate totalInterestEarned from accrueAccountInterest and totalInterestPaid from the applyLoanAutopay weekly path (weeklyTick.ts:147; applyLoanAutopay.ts:99-107; rows AdvancedBankApp.tsx:314-315).",
   "Resolve the Borrow tab's rewards promise: wire redeemRewards (and card charging) or stop displaying a '$X rewards' meta that can never be earned or redeemed (AdvancedBankApp.tsx:581,605).",
   "Charge the advertised annual fee (or drop it from ApplyCardModal) so gold/platinum cards carry a real cost of ownership (operations.ts:269; ApplyCardModal.tsx:136).",
   "Make Accounts-tab savings-goal Contribute debit real money and reward completion, instead of free-filling a cosmetic bar (operations.ts:546-559; AdvancedBankApp.tsx:1142-1158).",
   "Either implement taxDueThisYear accrual or remove the permanently-hidden 'Tax accrued this year' row (initialState.ts:68; AdvancedBankApp.tsx:318-320).",
   "Reconcile the banking EconomyEventBanner + 'rates rising/falling' notification with actual APR behavior (make rates move, or soften the claim) (AdvancedBankApp.tsx:382; weeklyTick.ts:181-191)."
  ]
 },
 {
  "app": "StocksApp",
  "inventory": "Mobile Apple-Stocks app (components/mobile/StocksApp.tsx). Tabs: Market (breadth strip, tappable sector-rotation board, sortable grouped list of 20 stocks), Portfolio (value hero + week change, sector allocation bar, 6-stat grid, holdings, watchlist), Orders (open/filled/closed summary + cancel), plus per-symbol Detail (quote/sparkline, position, market data, Trade CTA). Player actions (StockActions.ts): buyStockMarket, sellStockMarket, placeStockLimitOrder, placeStockStopOrder, cancelStockOrder \u2014 via StockTradeModal; 2% commission (STOCK_FEE). Weekly tick (lib/stocks/weeklyTick.ts): sector momentum rotation (strong/neutral/weak), per-sector tilt on holdings, macro drift by economyState, quarterly dividends every 13w, limit/stop order matching gated on cash. State (types.ts:2080 stocks): holdings, watchlist, realizedGains, savedMarketPrices, lastWeekPrices, sectorSnapshots, openOrders, orderHistory, totalDividends, dividendsThisYear. 20 stocks / 6 sectors (sectors.ts). Authoritative prices live in module lib/economy/stockMarket.ts (seeded weekly Box-Muller walk).",
  "broken": [
   {
    "what": "Watchlist is UI-unreachable \u2014 the player can never add or remove a stock. toggleStockWatchlist exists but is imported by ZERO components (StocksApp imports only buy/sell/limit/stop/cancel). The rendered 'Watchlist' sections can only ever be empty; the Detail page's sole CTA is Trade (no star/watch button).",
    "evidence": "contexts/game/actions/StockActions.ts:267 (defined, never imported); components/mobile/StocksApp.tsx:43-49 (import list omits it), :463-483 (watchlist render), :650-660 (detail CTA)",
    "severity": "dead-feature"
   },
   {
    "what": "Sector rotation + macro drift never reach the tradeable/displayed price, so economy events and sector momentum have no capturable effect on equities \u2014 contradicting the code's stated 'real teeth on equities'. The tick applies tilt+drift only to a LOCAL prices copy (used for holdings.currentPrice display + limit/stop fills). The module price (getStockInfo) that drives the market board, market-order buy/sell price, and the persisted savedMarketPrices snapshot is never adjusted (adjustStockPrice is not called by the tick), and holdings.currentPrice is rebuilt from the untilted module price each week, so tilt never compounds.",
    "evidence": "lib/stocks/weeklyTick.ts:118-133 (local copy, tilt/drift), :44-53 (stated intent); contexts/game/GameActionsContext.tsx:1438 (holdings rebuilt untilted), :1628 (snapshot is untilted module prices); components/mobile/StocksApp.tsx:124-133 & 157-162 (board + trade price read untilted getStockInfo)",
    "severity": "no-op"
   },
   {
    "what": "dividendsThisYear is never reset at the year boundary despite its type saying 'resets at year boundary'. Its only writer always accumulates, so the 'Div. this yr' stat grows forever and converges to lifetime totalDividends (crypto correctly resets its realizedGainsThisYear at week%52).",
    "evidence": "contexts/game/types.ts:2133-2134 (contract); contexts/game/GameActionsContext.tsx:1635 (only writer, always +=); components/mobile/StocksApp.tsx:428 (displayed); contrast lib/crypto/weeklyTick.ts:276",
    "severity": "no-op"
   },
   {
    "what": "The Energy sector is an empty content pool and Healthcare has a single stock. ALL_SECTORS includes 'energy' (so the board always renders an 'Energy \u00b7 0 stocks' tile that filters to an empty list), but no symbol maps to energy and no energy stock exists in the price catalog; healthcare = JNJ only.",
    "evidence": "lib/stocks/sectors.ts:113 (ALL_SECTORS has energy) vs :18-44 (STOCK_SECTORS has no energy, healthcare=JNJ); lib/economy/stockMarket.ts:6-27 (20 stocks, no energy names); components/mobile/StocksApp.tsx:207-220 & 267 (renders empty tile)",
    "severity": "content-starved"
   }
  ],
  "proposals": [
   {
    "name": "Wire up the watchlist (finish half-built system)",
    "pitch": "Add a star toggle on stock rows and the detail header so players can actually populate the Watchlist sections that already render.",
    "fitsBecause": "State (stocks.watchlist), the toggle action (toggleStockWatchlist), and both Watchlist render blocks already exist \u2014 only the button is missing.",
    "stateChanges": "None new \u2014 uses existing stocks.watchlist[] and toggleStockWatchlist.",
    "actions": "Call existing toggleStockWatchlist(setGameState, symbol); no new action.",
    "uiSurface": "Star icon in StockRow (grouped list) and a 'Watch/Unwatch' pill beside the Detail Trade CTA (StocksApp.tsx:650). On-DNA (existing chip styles).",
    "tests": "Action test asserting toggle adds then removes a symbol; snapshot that a watched symbol appears in watchItems.",
    "effort": "S",
    "risk": "Minimal \u2014 additive UI over an existing, idempotent action.",
    "tickHook": "None."
   },
   {
    "name": "Persist sector tilt + macro drift to the tradeable price (make economy events real)",
    "pitch": "Fold the weekly sector tilt and macro drift into the authoritative module price so a strong sector or a crash actually moves the market board, market-order fills, and portfolio value \u2014 and compounds week over week.",
    "fitsBecause": "Tilt/drift are already computed in the tick and already deterministic (seeded rollFor). This converts an existing no-op into the intended mechanic and finally makes the sector board, Movers sort, and EconomyEventBanner meaningful.",
    "stateChanges": "None new \u2014 writes existing module prices, captured by the existing savedMarketPrices snapshot; holdings.currentPrice then derives from the persisted price.",
    "actions": "None (mechanic lives in the tick + wiring).",
    "uiSurface": "No new surface \u2014 Market board, Movers sort, and Detail auto-reflect the moved price since they read getStockInfo.",
    "tests": "weeklyTick test: a strong-sector symbol's persisted price exceeds the untilted baseline and compounds across two weeks; a crash economyState lowers the persisted price; determinism preserved across a save/reload of the seed.",
    "effort": "M",
    "risk": "Economy/balance \u2014 mitigate by keeping the existing small modifiers (+0.8%/-0.5%/wk) and the existing per-week return clamps; mean-reverting sector states + the $1M price ceiling bound growth. Preserve seeded determinism (already threaded).",
    "tickHook": "runStocksWeeklyTick returns per-symbol tilted/drifted prices; GameActionsContext applies adjustStockPrice(sym, factor) for each symbol before getStockPricesSnapshot() (factor = tiltedPrice/baseModulePrice)."
   },
   {
    "name": "Add an Energy sector + thicken Healthcare (content is data)",
    "pitch": "Add real energy names (XOM, CVX, SLB) and a couple of healthcare names (PFE, UNH) so the sector board has no dead 0-stock tile and rotation covers all six sectors.",
    "fitsBecause": "The sector board + rotation already iterate ALL_SECTORS; dividends and rotation auto-cover any new symbol. Pure data additions to existing catalogs.",
    "stateChanges": "None on the save shape \u2014 additive keys in DEFAULT_PRICES, STOCK_SECTORS, and volatilityMap. restoreStockPrices only overwrites known keys, so new symbols initialize from defaults with no migration needed.",
    "actions": "None.",
    "uiSurface": "None new \u2014 new stocks auto-render in the grouped list and Energy tile.",
    "tests": "sectors.test: energy has >=3 symbols and sectorForSymbol('XOM')==='energy'; dividends.test: new yield-paying names pay quarterly.",
    "effort": "S",
    "risk": "Minimal \u2014 content only; pick realistic prices/yields consistent with neighbors.",
    "content": "~3 energy + ~2 healthcare listings with prices, dividend yields, and volatility band tags."
   },
   {
    "name": "Reset dividendsThisYear yearly + a dividend-calendar readout",
    "pitch": "Reset the YTD dividend stat at the 52-week boundary (matching the crypto precedent) and show weeks-to-next-dividend with a projected payout so dividends feel scheduled.",
    "fitsBecause": "isDividendWeek/DIVIDEND_INTERVAL_WEEKS already exist; crypto already resets its YTD stat at week%52 \u2014 this closes a stated-but-unimplemented contract.",
    "stateChanges": "None new \u2014 fixes the reset of the existing dividendsThisYear field in the tick wiring.",
    "actions": "None.",
    "uiSurface": "Portfolio adds a 'Next dividend in Nw \u00b7 est $X' line using DIVIDEND_INTERVAL_WEEKS + computePayouts; Detail already shows Est. annual income.",
    "tests": "Tick/wiring test: dividendsThisYear accumulates within a year and resets to 0 at week 52; projection calc test.",
    "effort": "S",
    "risk": "Minimal.",
    "tickHook": "In the stocks tick wiring, when nextWeeksLived % 52 === 0, set dividendsThisYear back to 0 (mirrors crypto weeklyTick.ts:276)."
   },
   {
    "name": "Trailing-stop orders (deepen the existing order book)",
    "pitch": "Add a trailing-stop order type whose stop ratchets up as the price rises, letting players lock in gains automatically.",
    "fitsBecause": "Stop orders, the fill loop (processOpenOrders), and the solvency guard already exist; a trailing stop is a small extension of stopOrderShouldTrigger.",
    "stateChanges": "Additive optional fields on the stock order: trailPercent?: number and highWaterMark?: number (both optional \u2192 no migration; safe-default via ??).",
    "actions": "New placeStockTrailingStop(setGameState, symbol, side, amount, trailPercent) mirroring placeStockStopOrder + canPlaceStockOrder guard.",
    "uiSurface": "Add a 'Trailing' mode + % field to StockTradeModal; open orders show 'trail X%'.",
    "tests": "orderBook/operations test: highWaterMark rises with price and the order fills after a drop of trailPercent from the high; solvency guard still applies.",
    "effort": "M",
    "risk": "Order-book complexity \u2014 reuse the existing placement solvency guard and per-week clamp; default missing fields to 0.",
    "tickHook": "In processOpenOrders, before the trigger check, update highWaterMark=max(prev,mid) and stopPrice=max(stopPrice, highWaterMark*(1-trailPercent))."
   }
  ],
  "flawlessChecklist": [
   "Wire toggleStockWatchlist to a star button (row + detail) so the Watchlist sections are not permanently empty.",
   "Persist the weekly sector tilt + macro drift into the tradeable module price (adjustStockPrice) so sector momentum and economy events actually move the market and compound, instead of being a one-week cosmetic on holdings.",
   "Reset dividendsThisYear at the 52-week boundary to honor its type contract and match the crypto realizedGainsThisYear precedent.",
   "Give the Energy sector real listings (or drop 'energy' from ALL_SECTORS) so the board has no dead 0-stock tile; add at least one more healthcare name.",
   "Detail 'Previous close' is reverse-derived from changePct (StocksApp.tsx:564) \u2014 read the actual lastWeekPrices[symbol].price for an accurate prior close."
  ]
 },
 {
  "app": "BitcoinMiningApp",
  "inventory": "Desktop mining+trading dashboard (components/computer/BitcoinMiningApp.tsx). Tabs: Trade (8 coins btc/eth/xrp/ada/sol/dot/matic/link, regime banner, place market/limit/stop via PlaceOrderModal\u2192CryptoTradingActions, open orders, fills), Mine (hashrate gauge = fleet health, mining-target picker = warehouse.selectedCrypto, 8 buyable rig tiers via inline handleBuyMiner, difficulty/durability readouts), Portfolio (wallet hero + BTC sparkline, dirty-BTC notice, halving countdown, cost-basis/realized stats, holdings, DCA schedules add/remove). Weekly ticks: runCryptoWeeklyTick (regime evolution, seeded price walk writing coin.price+priceHistory, BTC halving @208w, limit/stop fills, DCA debits from bank accounts, 25% yearly cap-gains tax), applyMiningCryptos (mint selectedCrypto = per-tier crypto \u00d7 per-coin multiplier \u00d7 0.5^halving \u2212 electricity, capped $100K/wk), applyMiningWarehouse (durability \u22122-5%/wk, difficulty +10%/10w cap 2\u00d7, auto-repair). State: cryptos[8], cryptoMarket (coinMarkets/openOrders/orderHistory/dcaRules/costBasis/realizedGainsThisYear/totalRealizedGains/lastHalvingWeek/halvingCount), warehouse (miners/minerDurability/selectedCrypto/difficultyMultiplier + a large set of unused depth fields).",
  "broken": [
   {
    "what": "The entire enhanced-mining depth system is UI-unreachable. buyMinerUpgrade, joinMiningPool/leaveMiningPool, stakeCrypto/claimStakingRewards, upgradeEnergySystem, upgradeAutomation are imported by ZERO components (only tests/task docs). So warehouse.upgrades/pools/activePool/stakingPositions/energyType/energyEfficiency/automationLevel are never written by the player, and calculateMiningEarnings' pool-bonus / automation / efficiency / energy branches can never fire. MINING_POOLS (6), ENERGY_TYPES (4), MINER_UPGRADE_DEFINITIONS (8) and staking rewardRates are all dead content.",
    "evidence": "contexts/game/actions/MiningActions.ts:180,270,332,362,449,538,588 (functions never imported by any component); calculateMiningEarnings reads the fields at MiningActions.ts:122-149; MINING_POOLS:41-51, ENERGY_TYPES:54-59",
    "severity": "dead-feature"
   },
   {
    "what": "Rig durability decays every week with no in-app way to repair it. The rig-detail 'Repair now \u00b7 $X' is a readout with no button, there is no manual-repair action, and no component sets autoRepairEnabled/autoRepairCryptoId \u2014 so the fully-implemented auto-repair tick can never be turned on. The only response to decay is buying replacement rigs.",
    "evidence": "components/computer/BitcoinMiningApp.tsx:796-799 (repair readout, no CTA), :274-300 (handleBuyMiner only adds); degrade at contexts/game/actions/weekly/applyMiningWarehouse.ts:90-94; auto-repair implemented at :96-129 but no component writes autoRepairEnabled",
    "severity": "dead-end"
   },
   {
    "what": "Displayed weekly yield is misleading across coins and after halvings. The hero 'MINING RIG $X/wk' and rig 'Yield $X/wk' use static USD figures that assume BTC at difficulty 1.0, but actual minted value applies per-coin multipliers (btc 1.0 \u2026 xrp 0.1), network difficulty, BTC halving, electricity, and the $100K cap. Mining XRP shows the same $/wk as BTC while yielding ~10% of the USD value.",
    "evidence": "components/computer/BitcoinMiningApp.tsx:109-118 & 202-208 (static weeklyEarnings) vs contexts/game/actions/weekly/applyMiningCryptos.ts:94-143 (real multipliers/halving/electricity/cap); cryptoMiningMultipliers at MiningActions.ts:94-103",
    "severity": "unread-state"
   },
   {
    "what": "MiningStatistics is never populated or shown. updateMiningStatistics is never called from any tick or component, so warehouse.statistics stays undefined \u2014 lifetime crypto mined, power spent, and per-week history are computed each week (applyMiningCryptos) but discarded.",
    "evidence": "contexts/game/actions/MiningActions.ts:645 (updateMiningStatistics never called); type at contexts/game/types.ts:622-638; per-week earnings computed at applyMiningCryptos.ts:106-117 then dropped",
    "severity": "dead-feature"
   },
   {
    "what": "Stale UI comment claims halving is deferred, though the reward-halving mechanism is fully wired. The Portfolio comment says the halving 'mechanism itself is deferred; this UI lights up the countdown', but the crypto tick fires the halving at 208w and applyMiningCryptos scales mining reward by 0.5^halvingCount.",
    "evidence": "components/computer/BitcoinMiningApp.tsx:556-557 (stale comment) vs lib/crypto/weeklyTick.ts:161-187 (halving fires) and applyMiningCryptos.ts:113-117 (reward halved)",
    "severity": "no-op"
   }
  ],
  "proposals": [
   {
    "name": "Surface the mining-depth system (finish the biggest half-built system)",
    "pitch": "Add an 'Optimize' panel exposing miner upgrades, mining pools, energy source, and automation \u2014 all already implemented, tested, and honored by the tick, just unreachable.",
    "fitsBecause": "MiningActions (buyMinerUpgrade/joinMiningPool/upgradeEnergySystem/upgradeAutomation) plus the MINING_POOLS/ENERGY_TYPES/MINER_UPGRADE_DEFINITIONS catalogs already exist and already feed calculateMiningEarnings; only the UI is missing.",
    "stateChanges": "None new \u2014 warehouse.upgrades[], pools[], activePool, energyType, energyEfficiency, automationLevel are already typed (types.ts:640-661) and already read by the tick.",
    "actions": "Call the existing MiningActions (each returns {success,message} and already has same-batch double-tap guards).",
    "uiSurface": "Mine tab 'Optimize' section + per-rig upgrade rows in rig-detail: pools chip row (filtered to selectedCrypto with net-yield = bonus\u00d7(1\u2212fee)), energy-source selector, automation-upgrade CTA. Reuse StatPill / rig-card DNA + amber tokens.",
    "tests": "Action tests exist; add a smoke test that joining a pool / buying an upgrade changes calculateMiningEarnings output; assert pool net-fee tradeoff can dip below solo.",
    "effort": "L",
    "risk": "Economy \u2014 bonuses stack into yield; mitigate via the existing network-difficulty, halving, electricity charge, and the $100K/wk cap (already bound total), plus the already-tuned pool fee tradeoff (MiningActions.ts:45-51).",
    "tickHook": "None new \u2014 the tick already consumes these fields."
   },
   {
    "name": "Repair loop: manual repair CTA + auto-repair toggle",
    "pitch": "Turn the rig-detail 'Repair now \u00b7 $X' readout into a real button and add an auto-repair toggle, making durability a managed resource instead of one-way decay.",
    "fitsBecause": "Repair-cost math (MINER_REPAIR_COSTS, repairNow) is already computed and displayed, and the auto-repair tick is already implemented \u2014 only the enable path and the manual action are missing.",
    "stateChanges": "Reuse warehouse.minerDurability + autoRepairEnabled/autoRepairCryptoId/autoRepairWeeklyCost (all typed). New action restores durability to 100 and debits cash/crypto.",
    "actions": "New repairRig(setGameState, tierId) with an atomic affordability re-check (mirror handleBuyMiner at BitcoinMiningApp.tsx:280-298); setAutoRepair(setGameState, {enabled, cryptoId}).",
    "uiSurface": "Rig-detail 'Repair now \u00b7 $X' becomes a PrimaryCTA; Mine hero gets an 'Auto-repair' toggle chip + repair-crypto picker.",
    "tests": "repairRig restores durability and debits atomically (no double-spend on double-tap); enabling auto-repair persists and the existing tick then repairs <50% rigs.",
    "effort": "M",
    "risk": "Money \u2014 reuse the atomic prev-state re-check pattern; cap repair spend at the displayed cost.",
    "tickHook": "None new \u2014 applyMiningWarehouse/applyMiningCryptos already perform auto-repair once autoRepairEnabled is set."
   },
   {
    "name": "Honest per-coin yield estimate (accuracy + shared estimator)",
    "pitch": "Replace the static $/wk with a live estimate for the selected coin (per-coin multiplier \u00d7 0.5^halving \u00d7 price \u2212 electricity) so switching the mining target visibly changes projected yield.",
    "fitsBecause": "Every input already exists \u2014 calculateMiningEarnings computes exactly this; the UI just isn't calling it and instead shows a BTC-at-difficulty-1 constant.",
    "stateChanges": "None new.",
    "actions": "None \u2014 pure derive. Prefer extracting a pure estimateWeeklyMining(warehouse, cryptos, selectedCrypto) into lib so UI and tick share one source of truth.",
    "uiSurface": "Mine hero + rig cards show the real estimate for the selected coin, with a small 'vs BTC' hint; updates when the target changes.",
    "tests": "Estimator test across coins/halvings asserting XRP < BTC and post-halving < pre-halving; parity with applyMiningCryptos output.",
    "effort": "S",
    "risk": "Minimal (display only); keep the estimator and tick in sync by sharing one function.",
    "tickHook": "None (or reuse the extracted estimator inside applyMiningCryptos)."
   },
   {
    "name": "Staking surface (finish half-built system)",
    "pitch": "Let players lock idle coins for weekly yield via the already-implemented, race-tested stakeCrypto/claimStakingRewards.",
    "fitsBecause": "stakeCrypto/claimStakingRewards are fully implemented with exploit-tuned rates and even have a dedicated race test \u2014 they just have no UI.",
    "stateChanges": "None new \u2014 warehouse.stakingPositions[] already typed (types.ts:612).",
    "actions": "Existing stakeCrypto(setGameState, cryptoId, amount, lockWeeks) and claimStakingRewards.",
    "uiSurface": "Portfolio 'Staking' section: amount + 1-4 week lock picker modal (reuse DCAModal patterns), positions list with a Claim CTA and maturity countdown.",
    "tests": "Existing action + race tests; add a UI-dispatch smoke and a claim-after-maturity assertion.",
    "effort": "M",
    "risk": "Economy \u2014 rates already bounded to 0.10-0.25%/wk with 1-4 week integer locks; keep those guards.",
    "tickHook": "Optional: a weekly 'position matured' notification; rewards already accrue via weeksLived on claim."
   },
   {
    "name": "Populate + show MiningStatistics (finish the stats system)",
    "pitch": "Record each week's mined amount / power cost into warehouse.statistics and surface a lifetime-mined card with a history sparkline.",
    "fitsBecause": "The MiningStatistics struct and updateMiningStatistics reducer already exist, and applyMiningCryptos already computes the weekly numbers \u2014 they are just thrown away.",
    "stateChanges": "None new \u2014 warehouse.statistics already typed; append to statistics.miningHistory (100-cap logic already in updateMiningStatistics).",
    "actions": "None new for the player \u2014 wire the existing recorder into the tick.",
    "uiSurface": "Mine tab 'Lifetime' card (total mined per coin, best coin, total power cost) + a Sparkline of weekly mined value (reuse the existing Sparkline component).",
    "tests": "Tick test: one history entry appended per week, capped at 100; totals accumulate; best-performing coin derived.",
    "effort": "M",
    "risk": "Minimal \u2014 pure accumulator; guard NaN like neighbors.",
    "tickHook": "In applyMiningCryptos, return the week's cryptoEarned/totalPowerCost and append to warehouse.statistics in the wiring (idempotent per week via weeksLived)."
   }
  ],
  "flawlessChecklist": [
   "Give durability an in-app repair path (manual CTA + auto-repair toggle) so 'Repair now \u00b7 $X' (BitcoinMiningApp.tsx:796) is actionable \u2014 today rigs only decay toward 0 with no fix but replacement.",
   "Make the hero/rig $/wk yield reflect the selected coin's difficulty multiplier + halving + electricity so mining XRP no longer shows BTC-equivalent yield.",
   "Either surface the mining-depth actions (upgrades/pools/energy/automation/staking) or gate their now-dead code paths in calculateMiningEarnings \u2014 they branch on warehouse state no UI can set.",
   "Populate warehouse.statistics (updateMiningStatistics is never called) so the mining history/lifetime totals exist, or drop the struct.",
   "Correct the stale 'reward-halving mechanism is deferred' comment (BitcoinMiningApp.tsx:556) \u2014 halving is fully wired (crypto weeklyTick.ts:161, applyMiningCryptos.ts:113)."
  ]
 },
 {
  "app": "Spark (dating app \u2014 components/mobile/Spark/**, SparkActions, lib/dating/*)",
  "inventory": "Player actions: swipe left/right/super (swipeOnProfile \u2192 deterministic rollMatch, weekly quota + super-like caps), rewindLastSwipe (20 gems / free on premium), boostProfile (50 gems, 1 wk), view matches + chat (sendSparkMessage + generateNpcReply), promoteMatchToRelationship (match\u2192relationships[]), unmatch, reportProfile, subscribeSparkPremium (plus/ultra), ProfileTab lifetime-stats readout. State: sparkApp (SparkAppState): profile, swipes[], matches[], messages{}, swipeQuota, swipesUsedThisWeek, superLikesUsedThisWeek, premium{tier,perks}, likedYou[], catfishRecords[], activeJealousy, jealousyHistory[], boost, dismissedCatfishIds[], reportedIds[], lifetimeStats; plus top-level datingMatches[]. Weekly tick processSparkWeeklyTick: quota/super reset, boost expiry, premium renewal, jealousy spawn, likedYou top-up. DatingActions (dates/gifts/propose/wedding/divorce) live separately and are reached from Contacts/Family, not from inside Spark.",
  "broken": [
   {
    "what": "likedYou is populated every week by the tick but NO UI ever reads it \u2014 the Ultra headline perk 'See who liked you' (advertised in SparkPremiumUpsellModal ULTRA_PERKS) has no surface. Grep: no component references likedYou; only sparkTick writes it.",
    "evidence": "lib/dating/sparkTick.ts:137",
    "severity": "dead-feature"
   },
   {
    "what": "Jealousy is a write-only dead-end: the tick spawns activeJealousy and pushes a notification, but resolveJealousy has zero UI callers so the event can never be resolved \u2014 and because the tick only spawns when !activeJealousy, the first unresolved event permanently blocks all future jealousy.",
    "evidence": "contexts/game/actions/SparkActions.ts:717",
    "severity": "dead-end"
   },
   {
    "what": "Catfish loop is unreachable: exposeCatfish/fallForCatfish/dismissCatfishSuspicion have zero callers. swipeOnProfile returns catfishSuspected but SwipeScreen ignores it and never passes it to the pre-swipe ProfileCard (which already renders a 'Suspicious profile' chip). Only reportProfile is wired.",
    "evidence": "components/mobile/Spark/screens/SwipeScreen.tsx:233",
    "severity": "dead-feature"
   },
   {
    "what": "updateMyProfile has zero UI callers \u2014 the player cannot edit their Spark bio/photos/interests. ProfileTab shows 'Add a bio to attract more matches' but offers no editor, so hasBio/hasPhotos/attractiveness bumps in calculateMatchProbability are permanently unearnable (profile stays empty).",
    "evidence": "contexts/game/actions/SparkActions.ts:103",
    "severity": "dead-end"
   },
   {
    "what": "Five SparkLifetimeStats fields \u2014 totalDatesGoneOn, totalGiftsGiven, totalProposals, totalMarriages, totalDivorces \u2014 are only ever initialized/migrated, never incremented (dating milestones happen in DatingActions which never touches sparkApp), and ProfileTab does not even display them.",
    "evidence": "contexts/game/initialState.ts:1589",
    "severity": "unread-state"
   },
   {
    "what": "attractivenessScore is READ in calculateMatchProbability but never written; scorePlayerProfile (the function that would compute it) is only called from a stress test, never from the tick or an action \u2014 so the match-rate bump is dead.",
    "evidence": "lib/dating/sparkLogic.ts:41",
    "severity": "unread-state"
   },
   {
    "what": "sparkPulseBridge.ts (milestoneToPulsePost + shouldAutoPostMilestone) is an entire unused module \u2014 not imported by any context/component; its intended consumer composePost exists in PulseActions but nothing wires the two. A half-built cross-app hook.",
    "evidence": "lib/dating/sparkPulseBridge.ts:30",
    "severity": "dead-feature"
   },
   {
    "what": "generateNpcReply's reply POOL is keyed adventurous/ambitious/romantic/creative/introverted/extroverted/friendly/professional, but the 51-profile catalog uses active/analytical/artistic/caring/... \u2014 only 3 keys (adventurous/ambitious/creative) match, so nearly every NPC falls through to the generic 3-line 'friendly' pool. (calculateMatchProbability also branches on personality==='romantic', of which there are zero profiles.)",
    "evidence": "contexts/game/actions/SparkActions.ts:386",
    "severity": "content-starved"
   },
   {
    "what": "swipeQuota field is stored (=30) but never read \u2014 the cap uses hardcoded maxSwipesPerWeek(); travelMode perk is read nowhere; sparkApp.lastOpenedTimestamp is never written or read; cancelSparkPremium has no UI caller.",
    "evidence": "lib/dating/sparkLogic.ts:161",
    "severity": "unread-state"
   }
  ],
  "proposals": [
   {
    "name": "Likes-You inbox (deliver the Ultra perk)",
    "pitch": "A 'Likes' surface that reveals the profiles already accumulating in sparkApp.likedYou \u2014 full reveal for Ultra, blurred count teaser for Plus/free \u2014 with tap-to-instant-match.",
    "fitsBecause": "The tick already tops up likedYou weekly and premium.perks.seeWhoLikedYou already gates it; SparkPremiumUpsellModal already sells this exact perk. Only the read surface is missing.",
    "stateChanges": "None required (reads existing sparkApp.likedYou). Optional additive SparkLikedYouEntry.dismissed?:boolean via v22 migration to let players clear entries.",
    "actions": "New likeBackFromLikedYou(setGameState, gameState, profileId) reusing swipeOnProfile's SparkMatch-append + datingMatches dedupe; dismissLikedYou(setGameState, profileId).",
    "uiSurface": "New 'Likes' tab or a rose rail on MatchesScreen reusing the existing fresh-match avatar-rail DNA; reveal gated by premium.perks.seeWhoLikedYou, else a blurred count + upsell CTA.",
    "content": "None (reuses DATING_PROFILES + Face images).",
    "tickHook": "None (likedYou top-up already exists in processSparkWeeklyTick).",
    "tests": "likeBackFromLikedYou creates one match + dedupes datingMatches; free/Plus tier hides identities; Ultra reveals; dismiss removes entry.",
    "effort": "M",
    "risk": "Low \u2014 reuses the tested match-creation path; no money/economy surface."
   },
   {
    "name": "Jealousy confrontation modal (finish the mechanic)",
    "pitch": "Surface activeJealousy as a confrontation sheet on Spark open (plus a partner banner in Contacts) with the five outcome choices, dispatching the already-built resolveJealousy.",
    "fitsBecause": "The tick already spawns events + notifies, resolveJealousy already computes reputation/relationship deltas and clears the event; wiring it also un-sticks the permanent-block bug where one unresolved event stops all future spawns.",
    "stateChanges": "None (activeJealousy, jealousyHistory, SparkJealousyOutcome all exist).",
    "actions": "None new \u2014 wire the existing resolveJealousy(setGameState, gameState, outcome).",
    "uiSurface": "New modal reading sparkApp.activeJealousy, one button per SparkJealousyOutcome, on-DNA with the BoostModal sheet pattern; severity/triggerType drive the copy.",
    "content": "Per-triggerType flavor lines (data) so the four trigger types read distinctly.",
    "tickHook": "None (spawn already exists); resolving simply frees the next spawn.",
    "tests": "Modal renders per triggerType; each outcome clears activeJealousy, appends jealousyHistory, applies rep/rel deltas (action is already unit-tested \u2014 add the wire/render test).",
    "effort": "S",
    "risk": "Low."
   },
   {
    "name": "Catfish exposure loop",
    "pitch": "Light up the dormant catfish system: show the suspicion chip pre-swipe, and on a suspicious match offer Verify / Expose (+reputation) / Block, with fallForCatfish money loss if you date an unexposed catfish.",
    "fitsBecause": "isCatfish/calculateCatfishProbability + exposeCatfish/fallForCatfish/dismissCatfishSuspicion + catfishRecords all already exist and are tested; ProfileCard already renders the chip \u2014 only the wiring and a few scammy profiles are missing.",
    "stateChanges": "None (catfishRecords, dismissedCatfishIds, reportedIds exist).",
    "actions": "None new \u2014 pass isCatfish(profile, lineageId) into ProfileCard from SwipeScreen and wire exposeCatfish/dismissCatfishSuspicion/fallForCatfish from PartnerProfileScreen.",
    "uiSurface": "catfishSuspected prop on the pre-swipe ProfileCard; Expose/Dismiss actions on PartnerProfileScreen for flagged matches, reusing its existing destructive-action row.",
    "content": "Add ~5 catfish-flavored DatingProfiles to lib/dating/datingProfiles.ts (bio red-flags, wealth/job mismatch) so detection actually fires beyond the current 3 red-flag bios.",
    "tickHook": "None.",
    "tests": "isCatfish deterministic per lineage; expose grants +5 rep exactly once (already tested); dismiss suppresses the chip; fallForCatfish debits bounded money.",
    "effort": "M",
    "risk": "Low \u2014 fallForCatfish money loss is already bounded/caller-supplied."
   },
   {
    "name": "Spark \u2192 Pulse milestone auto-posts",
    "pitch": "Auto-compose a Pulse post when an engagement / wedding / divorce / anniversary / new-baby happens, using the ready-made sparkPulseBridge.",
    "fitsBecause": "milestoneToPulsePost returns exactly composePost's arg shape and shouldAutoPostMilestone already guards for an active Pulse account; composePost exists in PulseActions. This is a cheap, prized cross-app hook currently 100% unwired.",
    "stateChanges": "Optional additive settings.disableAutoPostMilestones?:boolean (referenced in the bridge doc) via v22 migration for opt-out.",
    "actions": "Call sites in DatingActions proposeMarriage/executeWedding/fileDivorce/checkAnniversary \u2192 if shouldAutoPostMilestone(state), composePost(setGameState, state, milestoneToPulsePost(...)).",
    "uiSurface": "None required (posts land in the Pulse feed); optional settings toggle.",
    "content": "None (bridge copy already written for all six milestone kinds).",
    "tickHook": "None.",
    "tests": "Each milestone composes the correct content/hashtags; opt-out suppresses; no post when socialMedia.totalPosts===0.",
    "effort": "S",
    "risk": "Low \u2014 composePost is bounded and prints no money."
   },
   {
    "name": "Editable Spark profile + live attractiveness score",
    "pitch": "Wire updateMyProfile into a ProfileTab editor and recompute attractivenessScore each week via scorePlayerProfile, so a good bio/photos/interests actually raises match rate and incoming likes.",
    "fitsBecause": "updateMyProfile and scorePlayerProfile already exist, calculateMatchProbability already reads attractivenessScore + hasBio/hasPhotos, and the weekly tick already runs \u2014 the only gaps are the editor and the recompute call.",
    "stateChanges": "None new (writes existing profile fields + attractivenessScore).",
    "actions": "Wire updateMyProfile from an edit sheet; add attractivenessScore = scorePlayerProfile(state) inside processSparkWeeklyTick.",
    "uiSurface": "Edit sheet from ProfileTab (bio TextInput, interest chips, photo picker over the existing Face assets); on-DNA glass sheet.",
    "content": "None.",
    "tickHook": "processSparkWeeklyTick \u2014 set sp.profile.attractivenessScore weekly.",
    "tests": "updateMyProfile patch merges (already tested); tick sets attractivenessScore; scorePlayerProfile stays within [0,100] (already stress-tested); match prob rises with a filled profile.",
    "effort": "M",
    "risk": "Low."
   }
  ],
  "flawlessChecklist": [
   "Wire updateMyProfile into a ProfileTab editor so profile quality (bio/photos/interests) is earnable and the calculateMatchProbability bumps become reachable.",
   "Build a Likes-You screen reading sparkApp.likedYou, gated by premium.perks.seeWhoLikedYou, to deliver the advertised Ultra perk instead of accumulating invisible state.",
   "Add a jealousy-resolution modal reading activeJealousy \u2192 resolveJealousy, which also fixes the permanent-block bug that stops all future jealousy spawns.",
   "Pass catfishSuspected/isCatfish into the pre-swipe ProfileCard and wire exposeCatfish/dismissCatfishSuspicion/fallForCatfish so the whole catfish system stops being dead code.",
   "Increment the five dating lifetimeStats (dates/gifts/proposals/marriages/divorces) from DatingActions and display them in ProfileTab.",
   "Extend generateNpcReply's POOL to cover every catalog personality (or map catalog personalities to reply archetypes) so chats stop collapsing to the generic friendly pool."
  ]
 },
 {
  "app": "ContactsApp (components/mobile/ContactsApp.tsx, lib/contacts/*, ContactsActions, relationship actions)",
  "inventory": "Four tabs. Personal: expand a contact \u2192 Call / Hang Out / Ask $ (handleSimple, handleAskMoney with a 5-refusal pity grant); partner-only Coffee/Dinner/Luxury dates (goOnDate), Flowers/Jewelry gifts (giveGift), Move in (moveInTogether), Propose (RingSelectionModal \u2192 proposeMarriage), Plan Wedding (WeddingPlanningModal), Break up (breakUpWithPartner); spouse File for divorce (fileDivorce). Network: read-only tiles + detail for lobbyists/alliances/vendors/business/employees via aggregateContacts. Favors: IOU ledger with redeem (redeemFavor), net-money header, settled history. Attention: triage cards for stale/weak contacts (contactsNeedingAttention) with a Call-to-reconnect CTA. State: relationships[] (rich npcOpinion/npcGoals/giftPreferences/npcMemories/npcMood/datesCount/engagementWeek/moneyRequestAttempts...), favorLedger, plus read-only aggregated sources. Active weekly ticks: processWeeklyNPCDepth (opinion/goals/mood/memories/aging) and applyRelationshipHealth (breakup/disappointed).",
  "broken": [
   {
    "what": "The favor ledger is never populated: recordFavor has zero non-test callers, so no Favor is ever created in-game \u2014 the entire Favors tab shows 'No open favors' permanently, and redeemFavor / netMoneyPosition header / settled history are all unreachable. lib/contacts/favors.ts + the redeem UI are a complete, tested system with no producer.",
    "evidence": "contexts/game/actions/ContactsActions.ts:31",
    "severity": "dead-feature"
   },
   {
    "what": "tickFavors is never wired into any weekly tick (GameActionsContext), so favor expiry never runs \u2014 part of the same unfinished favor system.",
    "evidence": "contexts/game/actions/ContactsActions.ts:108",
    "severity": "dead-end"
   },
   {
    "what": "The recency/warmth signal is structurally dead: weeksSinceContact = lastInteractionWeek ?? lastCall, but the only writer of lastInteractionWeek (interactRelation) has zero callers and lastCall is never written anywhere \u2014 so weeksSinceContact is ALWAYS undefined. Every personal card's recency dot shows 'No recent contact', and contactsNeedingAttention returns [] for every contact (it returns false on undefined), leaving the entire Attention tab permanently empty and its 'At risk' stat pinned at 0.",
    "evidence": "lib/contacts/aggregator.ts:79",
    "severity": "dead-feature"
   },
   {
    "what": "The Contacts actions that SHOULD count as contact \u2014 Call/Hang Out (handleSimple), dates (goOnDate), gifts (giveGift) \u2014 never stamp lastInteractionWeek, so even active play never warms a recency dot or clears the Attention tab. It also means processWeeklyNPCDepth decays trust/attraction every single week (lastInteractionWeek !== weeksLived is always true), making its 'interact to prevent decay' design unreachable.",
    "evidence": "components/mobile/ContactsApp.tsx:567",
    "severity": "no-op"
   },
   {
    "what": "weeklyInteractions is read by the 'This wk' fact chip but is only written by the dead interactRelation, so the chip never renders.",
    "evidence": "components/mobile/ContactsApp.tsx:510",
    "severity": "unread-state"
   },
   {
    "what": "lastCall (Relationship) is never written by any code \u2014 it exists only as a read fallback in the aggregator. Dead field.",
    "evidence": "contexts/game/types.ts:444",
    "severity": "unread-state"
   }
  ],
  "proposals": [
   {
    "name": "Turn 'Ask $' loans into real IOUs (finish the favor system)",
    "pitch": "When a contact lends the player money, record an owed-by-player money Favor; let the player repay it from the Favors tab \u2014 giving the whole built-but-empty favor ledger real content and stakes.",
    "fitsBecause": "favors.ts, recordFavor, redeemFavor and the entire Favors UI already exist and are tested \u2014 only a producer is missing. handleAskMoney already tracks moneyRequestAttempts/lastMoneyRequest, so a granted loan is the natural IOU origin.",
    "stateChanges": "Uses existing optional favorLedger (v14+ optional-slice precedent); no new fields. A v22 migration only if seeding existing debts.",
    "actions": "Call recordFavor from handleAskMoney on a granted loan (direction owed-by-player, kind money, value=amount, expiresWeek); add repayFavor(gameState, setGameState, favorId) in ContactsActions that debits via applyMoneyDelta and flips to redeemed with the same double-tap re-check pattern as redeemFavor; wire tickFavors into the weekly tick.",
    "uiSurface": "Favors tab already renders owed-by-player rows \u2014 add a 'Repay' button mirroring the existing 'Redeem' button (ContactsApp favor row).",
    "content": "None.",
    "tickHook": "Wire the existing tickFavors(setGameState, nextWeeksLived) beside applyRelationshipHealth for expiry.",
    "tests": "recordFavor fires on a granted loan; repay debits cash + closes exactly once (guards a same-batch double-tap); tickFavors expires past-due favors.",
    "effort": "M",
    "risk": "Low \u2014 repay is a pure money sink (the borrowed cash was already granted); no printing."
   },
   {
    "name": "Living contact recency + working Attention triage",
    "pitch": "Stamp lastInteractionWeek on every Contacts interaction so recency dots warm up and the Attention tab finally populates from real neglect, with a gentle weekly slip for long-ignored contacts to give the triage teeth.",
    "fitsBecause": "aggregator recencyMeta, contactsNeedingAttention, the triage cards and the 'Call to reconnect' CTA are all already built and merely starved of the signal; processWeeklyNPCDepth already keys its decay off lastInteractionWeek.",
    "stateChanges": "None new (lastInteractionWeek + weeklyInteractions already exist on Relationship).",
    "actions": "Set lastInteractionWeek = prevWs (and bump weeklyInteractions) inside the Call/Hang Out/Ask handlers and the goOnDate/giveGift updaters; to honor 'UI never mutates state inline', move handleSimple's inline write into a small ContactsActions helper (e.g. recordInteraction).",
    "uiSurface": "No new surface \u2014 lights up the existing recency dots, the 'This wk' fact chip, and the Attention tab/'At risk' portfolio stat.",
    "content": "None.",
    "tickHook": "Reuses processWeeklyNPCDepth (decay now becomes meaningful because interactions clear it).",
    "tests": "Each interaction stamps lastInteractionWeek; contactsNeedingAttention returns stale low-strength contacts; neglected non-partners slip over weeks.",
    "effort": "S",
    "risk": "Low."
   },
   {
    "name": "Weekly favors from the network (owed-to-player)",
    "pitch": "High-strength network contacts (trusted vendors, invested business partners, high-influence lobbyists/allies) occasionally offer the player a favor \u2014 a discount, intro, influence, or cash IOU \u2014 that accrues in the Favors tab and can be called in for a bounded benefit.",
    "fitsBecause": "aggregateContacts already classifies these kinds with a 0-100 strength, favors.ts already models discount/influence/intro/safety/money, and redeemFavor already handles both the money-credit and non-money-flip cases; the Favors UI already renders them. A prized network\u2192favors cross-system hook.",
    "stateChanges": "Existing favorLedger; no new relationship fields.",
    "actions": "Reuse recordFavor for generation; extend redeem to apply small capped per-kind effects (discount = one-time markdown flag, influence = small political capital, money = cash credit via applyMoneyDelta); wire tickFavors for expiry.",
    "uiSurface": "Favors tab already lists them; add per-kind redeem semantics + an expiry countdown (favor.expiresWeek is already rendered).",
    "content": "lib/contacts/favorTemplates.ts data module (typed favor templates keyed by contact kind/strength).",
    "tickHook": "New contexts/game/actions/weekly/applyContactFavors.ts, wired beside sparkTick/applyRelationshipHealth, deterministic via a seeded weekly roll (weeksLived-based, no Date.now), idempotent, with a per-week generation cap.",
    "tests": "Tick generates <=N/week deterministically and expires them; redeem applies each bounded effect exactly once; caps hold.",
    "effort": "L",
    "risk": "Medium (economy) \u2014 cap generation count, keep values small, and enforce expiry so favors can't be hoarded into a windfall."
   },
   {
    "name": "Host a get-together (inner-circle group action)",
    "pitch": "A 'Host a get-together' action from the Personal 'Relationship portfolio' hero that spends money + energy to boost several friends/family bonds at once, weekly-capped \u2014 a group loop on top of today's one-contact-at-a-time Call/Hang Out.",
    "fitsBecause": "The hero already computes topPersonal/inner-circle + avgStrength and the handleSimple money/energy/cap pattern already exists; this deepens the currently thin friend/family loop and stamps recency on several contacts (pairs with proposal C2).",
    "stateChanges": "Optional additive lastGatheringWeek?:number top-level for the weekly cap, via v22 migration.",
    "actions": "New hostGathering(gameState, setGameState, {tier}) in ContactsActions: cost + energy gate, +bond to the top N non-partner contacts, stamps lastInteractionWeek on each, once per week.",
    "uiSurface": "A CTA inside the existing 'Relationship portfolio' hero; reuse the flash toast for feedback; on-DNA amber CTA.",
    "content": "2-3 gathering tiers (cost/bond/energy) as a small data table.",
    "tickHook": "None.",
    "tests": "Cost/energy gating; capped once per week; boosts multiple contacts and warms their recency; ignores partners' romance-only actions.",
    "effort": "M",
    "risk": "Low-medium (economy) \u2014 flat cost + weekly cap + bounded bond keep it from farming."
   }
  ],
  "flawlessChecklist": [
   "Stamp lastInteractionWeek (and bump weeklyInteractions) in the Contacts Call/Hang Out/Ask/date/gift actions so recency dots, the 'This wk' chip, and the Attention tab come alive.",
   "Wire tickFavors(setGameState, nextWeeksLived) into the weekly tick so favor expiry actually runs.",
   "Create favors from existing money moments (record an owed-by-player IOU when handleAskMoney grants a loan) so the Favors tab has content and borrowing has a consequence.",
   "Route the Attention 'Call to reconnect' CTA through the same interaction path that writes recency, so triaging a contact actually clears them from the queue.",
   "Either wire or retire the dead interactRelation (SocialActions.ts) and the never-written lastCall field so the recency source of truth is unambiguous.",
   "Move handleSimple's inline relationship mutation into a ContactsActions helper to satisfy the 'UI never mutates state inline' ground rule while adding the recency write."
  ]
 },
 {
  "app": "Pulse social platform",
  "inventory": "WHAT THE PLAYER CAN DO TODAY: (1) Compose posts \u2014 inline text composer on the feed (FeedScreen.tsx:112) + full ComposeModal with content-type chips (text/photo/video/story), up to 5 hashtags, and a 'sponsor this post' picker when brand deals are active. Posting costs energy/health, grants happiness, may go viral, earns ad revenue, gains followers (composePost, PulseActions.ts:113). (2) Like/repost/bookmark and comment on the player's OWN posts (PostDetailScreen \u2192 likePost/repostPost/commentOnPost). (3) Feed = own persisted posts interleaved with ephemeral ambient NPC + random-profile posts; ambient like/repost is local-optimistic only. (4) Trending tab (read-only hashtag list). (5) Notifications tab (mark read/all read). (6) DMs (wraps legacy DMSystem). (7) Profile (Posts/Replies/Media/Bookmarks tabs, edit profile, InfluenceMeter). (8) Brand Deals inbox/active/history \u2014 accept/decline/deliver/breach (BrandDealsScreen). (9) Live streaming setup\u2192live\u2192summary (needs 100 followers + 30 energy; viewers/tips tick every 30s). (10) Monetization: gem post-boost (200), Verified Pro subscription (IAP), rewarded-ad follower boost (1/wk). (11) Scandal recovery modal (apology/silence/gems/lawsuit) \u2014 only if a scandal is active.\n\nKEY STATE (socialMedia slice, types.ts:2264): followers, influenceLevel, totalPosts, viralPosts, engagementRate, recentPosts, commentThreads, trendingHashtags, followGraph, activeScandal, scandalHistory, brandInbox, activeBrandDeals, verifiedPro(+welcomeClaimed), notifications, liveSession, pendingBoosts, lifetimeStats, lastViralBoostBySkill, lastAdBoostWeek, totalEarnings, live-stream totals.\n\nWHAT THE WEEKLY TICK ACTUALLY UPDATES (pulseTick.ts, wired at GameActionsContext.tsx:630): engagement-rate recompute, follower decay after 2+ idle weeks, trending rotation (+3 organic tags, +scandal tag), scandal progression (decay only \u2014 never spawns one), brand-offer generation gated at 10K followers, brand-deal weekly installments/expiry/breach, weekly impression earnings, Verified Pro expiry, lifetimeStats. pulseEarnings flows into weekly income (GameActionsContext.tsx:677); reputationDelta applied (GameActionsContext.tsx:1293). NPC feed generation is explicitly skipped in the tick ('skipping 7/8: NPC feed for now', pulseTick.ts:321) and done ephemerally in the UI instead.",
  "broken": [
   {
    "what": "The entire scandal system is UNREACHABLE \u2014 nothing ever creates an activeScandal in normal play. The only creator, applyFameEventToPulse, has zero callers, and no EventTemplate anywhere sets surface:'pulse_scandal'. So ScandalBanner, ScandalRecoveryModal (apology/silence/gems/lawsuit), the whole scandal progression in the tick, scandalHistory, scandal hashtags, and lifetime totalScandalsSurvived are all built but dead.",
    "evidence": "lib/events/fameEvents.ts:516 (applyFameEventToPulse, 0 callers) + engine.ts:87 defines surface but grep 'surface:' in lib/events returns nothing; tick only decays: pulseTick.ts:191",
    "severity": "dead-feature"
   },
   {
    "what": "Follow graph is UI-unreachable: followNpc/unfollowNpc have zero component callers, so the player can never follow or unfollow an NPC. The mutual-follow-back follower boost, the followBackGrantedNpcIds anti-farm guard, and 'follow' notifications can never fire.",
    "evidence": "contexts/game/actions/PulseActions.ts:381 (followNpc) and :433 \u2014 grep across components/ finds no caller",
    "severity": "dead-feature"
   },
   {
    "what": "like/comment/mention/repost notification types are never generated. They have icons, colors, group headers, and an elaborate 'liked by @a and 4 others' rollup (collapseLikeReposts), but no code path ever pushes one \u2014 only milestone/follow/brand_offer/scandal_update/verified_pro_renewal/system are created. The Likes & Reposts, Comments, and Mentions groups are permanently empty.",
    "evidence": "NotificationsScreen.tsx:50 (collapseLikeReposts, dead) vs pushNotification calls at PulseActions.ts:246/415/662/807/1121 + pulseTick.ts:213-349 (no like/comment/mention/repost)",
    "severity": "dead-feature"
   },
   {
    "what": "generateScandalPileOnComments \u2014 a full hostile-hater comment generator with 6 scandal-type template pools \u2014 has zero callers. Its docstring claims 'Used by the Pulse tick to seed commentThreads' but the tick never calls it.",
    "evidence": "lib/social/randomProfiles.ts:240 (0 non-test callers; pulseTick.ts never references it)",
    "severity": "dead-feature"
   },
   {
    "what": "Verified Pro sells 'Advanced analytics \u2014 See per-post performance trends' and sets perksUnlocked.analyticsUnlocked=true, but no analytics UI exists anywhere in Pulse and analyticsUnlocked is read nowhere. A paid perk delivers nothing (deceptive-value risk).",
    "evidence": "VerifiedProUpsellModal.tsx:23 advertises it; PulseActions.ts:1050 sets analyticsUnlocked; grep 'analyticsUnlocked' finds no reader",
    "severity": "dead-feature"
   },
   {
    "what": "lifetimeStats (peakFollowers, peakInfluenceLevel, totalScandalsSurvived, totalBrandDealsCompleted, totalGemsBoostsUsed, totalVerifiedProWeeks) is faithfully tracked by the tick and actions but never displayed in any Pulse component \u2014 a complete trophy set the player can never see.",
    "evidence": "written pulseTick.ts:362; grep 'lifetimeStats' in components/mobile/Pulse/ returns nothing",
    "severity": "unread-state"
   },
   {
    "what": "lastViralBoostBySkill is declared, seeded, and documented ('capped 1 boost/skill/week') but never read or written anywhere \u2014 an intended skill\u2192viral cross-app hook that was never implemented.",
    "evidence": "types.ts:2302 + initialState.ts:1547; no other references in contexts/lib/components",
    "severity": "unread-state"
   },
   {
    "what": "pendingBoosts is write-only: boostPostWithGems appends {type:'post',postId,appliedWeek} but nothing ever reads/consumes it (the boost effect is applied inline in the action). The lingering-boost ledger does nothing.",
    "evidence": "contexts/game/actions/PulseActions.ts:1016 (append); no reader in tick or UI",
    "severity": "unread-state"
   },
   {
    "what": "StoriesRail NPC bubbles are a dead-end \u2014 FeedScreen mounts StoriesRail without onTapNpc, so tapping any NPC story bubble no-ops. Only the 'go live' self bubble works.",
    "evidence": "components/mobile/Pulse/screens/FeedScreen.tsx:270 (<StoriesRail onGoLive={onGoLive} />, no onTapNpc) \u2192 StoriesRail.tsx onPress={()=>onTapNpc?.(npc.id)}",
    "severity": "dead-end"
   },
   {
    "what": "Profile 'Following' count is permanently static: ProfileScreen reads userProfile.following, but followNpc writes followGraph.followingNpcIds and nothing ever writes userProfile.following. Combined with the unreachable follow graph, Following never changes.",
    "evidence": "components/mobile/Pulse/screens/ProfileScreen.tsx:59 (following = profile.following ?? 0); no writer of userProfile.following",
    "severity": "unread-state"
   },
   {
    "what": "Photo/Video/Story posts can't attach media: ComposeModal offers those content-type chips but has no image picker; composePost reads args.photo/category/mentionedNpcIds but the modal never sets them. A 'photo' post is just a text post with a higher multiplier and no image. Mentions and post categories are never populated or displayed.",
    "evidence": "ComposeModal.tsx:33-38 (chips) vs :101 (only passes content/contentType/hashtags); PulseActions.ts:95-96,171 (photo/category/mentionedNpcIds args unused by UI)",
    "severity": "content-starved"
   },
   {
    "what": "Ambient NPC/random feed posts are ephemeral (regenerated client-side each week, never persisted) and non-interactive for detail/comment \u2014 only the player's own posts route to PostDetailScreen, so the comment icon on ambient posts leads nowhere. Legacy generateWeeklyNPCPosts is fully dead.",
    "evidence": "FeedScreen.tsx:149 (client-side gen) + :199 (onOpenDetail gated to isPlayerPost); npcPosts.ts:195 generateWeeklyNPCPosts has 0 callers",
    "severity": "content-starved"
   },
   {
    "what": "Trending 'Why is this trending?' never renders \u2014 TrendingScreen/TrendingChip read whyReason but no code path ever sets it (the tick injects organic/scandal tags without a reason).",
    "evidence": "TrendingScreen.tsx:56 reads t.whyReason; grep shows whyReason is only ever read, never assigned in tick/actions",
    "severity": "content-starved"
   },
   {
    "what": "Live streaming has a npcChatters field and the fantasy of live chat, but npcChatters is seeded [] and never appended, and LiveStreamScreen renders no chat feed \u2014 the live experience is only a viewer counter + tips number.",
    "evidence": "PulseActions.ts:857 (npcChatters:[]) never mutated; LiveStreamScreen.tsx has no chat UI",
    "severity": "content-starved"
   }
  ],
  "proposals": [
   {
    "name": "Bring scandals to life (finish the built scandal system)",
    "pitch": "Fame turns double-edged: bad takes, leaks and deepfakes can actually erupt into a scandal you must survive, with haters piling onto your posts in real time.",
    "fitsBecause": "Every piece is already built and wired except the spark: activeScandal, ScandalBanner, ScandalRecoveryModal, the tick's scandal decay/cascade, scandalHistory, scandal hashtags, generateScandalPileOnComments, and lifetimeStats.totalScandalsSurvived. This is finishing a half-built system, the top-priority category.",
    "stateChanges": "No new required fields \u2014 activeScandal/scandalHistory already exist. Optional additive: socialMedia.scandalRiskScore?:number (0-100, decays weekly) to make spawns feel earned; if added, bump STATE_VERSION 21\u219222 with a 22:(state)=>{} migration defaulting it.",
    "actions": "recoverFromScandal already exists. Add a pure spawnScandal helper (or inline in the tick) that mints a PulseActiveScandal; wire generateScandalPileOnComments to seed commentThreads on recent posts.",
    "uiSurface": "Reuses ScandalBanner (already mounted in PulseApp) + ScandalRecoveryModal \u2014 zero new surfaces. Hater comments render through the existing CommentThread/CommentItem hostile styling.",
    "tests": "Extend __tests__/social/pulseTick.test.ts: spawn probability gated by influence tier, at most one active scandal, pile-on comments bounded and idempotent per week, severity/rep/follower caps preserved.",
    "tickHook": "Extend processPulseWeeklyTick: after the existing scandal-progression block, roll a low weekly spawn chance scaled by influence tier / risk, and seed pile-on comments while a scandal is active.",
    "content": "Hater templates already exist (6 scandal types + generic). Add ~6 scandal headline strings so ScandalBanner reads better.",
    "effort": "M",
    "risk": "Economy/UX: scandals cost followers + reputation. Mitigation \u2014 reuse the existing caps (0.5%/wk follower cascade, rep floor at 0), keep spawn probability low and gate to popular+ tier so early players aren't punished; only ever one scandal at a time (guard already present in applyFameEventToPulse)."
   },
   {
    "name": "Make the follow graph playable + live Following count",
    "pitch": "Follow the people you know and the creators you discover; mutual follows boost your reach and fill your feed with accounts you chose to see.",
    "fitsBecause": "followNpc/unfollowNpc are fully built and already hardened against the follow/unfollow farm exploit (followBackGrantedNpcIds) \u2014 they just have no button. Reuses followGraph, StoriesRail (its onTapNpc prop already exists), and generateNpcPostsForFeed.",
    "stateChanges": "followGraph already exists. No new persisted state \u2014 derive the profile Following count from followGraph.followingNpcIds.length instead of the never-written userProfile.following (read-side only).",
    "actions": "Reuse followNpc/unfollowNpc unchanged. Prioritize followed NPCs inside generateNpcPostsForFeed so following visibly changes the feed.",
    "uiSurface": "Add a Follow/Following button to PostCard for ambient/NPC posts and wire StoriesRail onTapNpc to a small NPC sheet (avatar + relationship + Follow). On-DNA with existing card/bubble components.",
    "tests": "followNpc already has coverage; add: Following count derives from followGraph length; followed NPCs rank ahead in generateNpcPostsForFeed; re-follow after unfollow does not re-pay the boost (guard already tested).",
    "tickHook": "None required (feed generation already runs per week).",
    "content": "None \u2014 uses existing relationships + random profiles.",
    "effort": "M",
    "risk": "Low. The one economic concern (follow/unfollow follower farming) is already mitigated by followBackGrantedNpcIds. Deriving the count avoids any migration."
   },
   {
    "name": "Creator Studio: analytics + lifetime trophy case",
    "pitch": "A real insights screen \u2014 your top posts, weekly follower growth, engagement trend, and lifetime records \u2014 and it becomes the actual payoff for Verified Pro.",
    "fitsBecause": "Finishes two dead things at once: the never-shown lifetimeStats and the Verified Pro 'Advanced analytics' perk that currently delivers nothing. Reuses recentPosts metrics, lifetimeStats, InfluenceMeter, and perksUnlocked.analyticsUnlocked as the real gate.",
    "stateChanges": "Additive: socialMedia.followerHistory?:{week:number;followers:number}[] (cap ~52) appended each tick for the growth chart. Bump STATE_VERSION 21\u219222 with a 22:(state)=>{} migration seeding []. lifetimeStats already exists.",
    "actions": "None new \u2014 read-only screen. Reads existing state.",
    "uiSurface": "New 'Insights' overlay route in PulseApp (same pattern as profile/brandDeals overlays): top-posts cards, a lightweight follower sparkline, engagement-rate readout, and a trophy grid from lifetimeStats. Show a blurred teaser + upsell when analyticsUnlocked is false, finally making the perk real.",
    "tests": "Tick appends followerHistory and caps at 52; analytics gate hides advanced view when Pro inactive; trophy values read straight from lifetimeStats.",
    "tickHook": "Extend processPulseWeeklyTick to push one {week,followers} sample and slice to the cap.",
    "content": "None \u2014 data-driven from existing state.",
    "effort": "M",
    "risk": "Low. Save size bounded by the 52-entry cap; migration is additive and never touches existing fields."
   },
   {
    "name": "They actually react: NPC likes, reposts & replies",
    "pitch": "Posts stop echoing into a void \u2014 they collect likes, reposts, and NPC replies that light up your Alerts tab and thread under your posts.",
    "fitsBecause": "Finishes the dead like/comment/mention/repost notification types (icons, colors, groups, and the collapseLikeReposts rollup are all already built) and makes commenting two-way. Reuses commentThreads, the 100-entry notification ring buffer, and the 50/thread cap.",
    "stateChanges": "notifications + commentThreads already exist. Additive optional socialMedia.lastReactionWeekByPost?:Record<string,number> to avoid re-reacting to the same post; if added, bump STATE_VERSION 21\u219222 migration seeding {}.",
    "actions": "Add a pure generateReactionsForWeek helper (mirrors generateScandalPileOnComments) producing bounded like/repost/comment notifications with refPostId+fromHandle plus a few NPC comments into commentThreads.",
    "uiSurface": "Zero new surfaces \u2014 the Likes & Reposts / Comments / Mentions groups and the '@a and N others' rollup already exist in NotificationsScreen and finally populate; replies appear in existing CommentThread.",
    "tests": "Tick generates \u2264N reactions/week scaled by engagement, refPostId set so collapseLikeReposts fires, comments respect the 50/thread cap, idempotent per week.",
    "tickHook": "Extend processPulseWeeklyTick to fan out reactions across the player's recent/viral posts.",
    "content": "New lib/social/npcComments.ts with friendly/neutral reply template pools by post category (data module, not hardcoded in components).",
    "effort": "L",
    "risk": "Notification/save bloat. Mitigation \u2014 hard weekly cap, reuse existing 100-ring buffer + 50/thread trimming, deterministic seeding so StrictMode double-renders don't double-generate."
   },
   {
    "name": "Your skills feed your feed (implement lastViralBoostBySkill)",
    "pitch": "Leveling real skills \u2014 writing, video, marketing \u2014 makes matching posts pop harder, so your life progress visibly powers your Pulse growth.",
    "fitsBecause": "Implements the already-declared, already-seeded, fully-dead lastViralBoostBySkill field exactly as its comment intends ('capped 1 boost/skill/week'). A cheap, prized cross-app hook. Reuses checkViralChance, contentType, and composePost.",
    "stateChanges": "lastViralBoostBySkill already exists (Record<string,number>) \u2014 no new state, no migration.",
    "actions": "Extend composePost: map contentType\u2192a relevant skill from gameState.skills, and if that skill hasn't boosted this weeksLived, grant a modest viral-chance/follower bump and stamp lastViralBoostBySkill[skill]=weeksLived.",
    "uiSurface": "A one-line 'Skill boost applied' hint in the compose success toast/result; no new screen.",
    "tests": "Boost applies at most once per skill per week; higher skill \u2192 larger (bounded) bump; stamping enforces the cap; no boost when the skill is low/absent.",
    "tickHook": "None \u2014 happens at post time.",
    "content": "Small contentType\u2192skill map (data in the action or a tiny lib constant).",
    "effort": "S",
    "risk": "Economy: keep the bump small and strictly 1/skill/week (the field was designed for exactly this cap) so it can't compound into runaway follower/ad income."
   }
  ],
  "flawlessChecklist": [
   "Fix the SCANDAL_TYPE_MAP key mismatch in fameEvents.ts:508 \u2014 keys are 'tabloid_scandal' etc. but template ids are 'fame_tabloid_scandal', so once scandals are wired every type wrongly resolves to 'bad_take'.",
   "Derive the profile 'Following' count from followGraph.followingNpcIds.length (ProfileScreen.tsx:59) so it isn't frozen at 0 \u2014 a one-line read-side fix that makes the profile honest even before the follow graph gets a button.",
   "Resolve the Verified Pro 'Advanced analytics' claim (VerifiedProUpsellModal.tsx:23 / analyticsUnlocked): either ship the Insights screen (Proposal 3) or remove the bullet \u2014 today it sells a perk that does nothing, a deceptive-value risk.",
   "Decide pendingBoosts (PulseActions.ts:1016): either consume it in the tick as a lingering boost or stop writing it \u2014 it is currently a write-only ledger that misleads anyone reading the state.",
   "Ambient feed posts expose a comment affordance that goes nowhere (FeedScreen.tsx:199 gates detail/comment to player posts). Either allow opening them read-only or hide the comment icon on ambient posts so it doesn't look tappable.",
   "Trending 'Why?' (whyReason) never populates (TrendingScreen.tsx:56). Set whyReason when the tick injects scandal/event/player tags, or drop the always-absent UI branch so the screen doesn't imply missing content."
  ]
 },
 {
  "app": "PetApp",
  "inventory": "Four tabs. PETS: pet roster rail + active-pet STAGE (Health/Happiness ProgressRings, Hunger/Energy mini-meters, 0-5 bond stars derived from health+happiness, care pad Feed/Play/Sleep/Vet, 'Companion bonus' card, In-memoriam list) + per-pet profile drill-down (vitals, life-stage %, breed-trait chips, per-pet bond contribution, care log, toy chest). SHOP: buy food/toys/breeds. VET: 6 services (checkup, vaccination, treatment, surgery, dental, grooming). COMPETE: 5 shows with win-odds dials. Actions (contexts/game/actions/PetActions.ts): buyPet (atomic debit+grant), buyFood, feedPet (+nutrition/+3 happy/+healthBonus), buyToy, playWithPet (\u221220 pet energy, \u221210 player energy, gated \u226520 energy), petSleep (+50 energy/+5 health, once-per-week), payForVet, enterCompetition (once-per-week/pet, ~10x entry fee at \u226490% odds). Pet state (types.ts:68-93): age,hunger,happiness,health,energy,toys[],ownedToys[],vaccinated,isSick,sickness,weeksAtZeroHealth,isDead,competitionWins,lastVetVisit,lastSleepWeek,lastCompetitionWeek; plus gameState.petFood{}. Live weekly tick = tickPetsForWeek + applyPetDeathSideEffects + applyPetLivingSideEffects (contexts/game/actions/weekly/applyPets.ts): +1 age, \u22128 hunger flat, happiness \u22125 when hunger<40, health \u22123 when hunger<20, flat 2%/6% sickness roll, sick pets \u22125 health/wk, death at 3wk zero-health or age\u2265lifespan; player +2 happiness per happy&healthy pet, \u2212$15/pet food. NOTE: a second, richer, fully-tested decay engine exists in lib/pets/decay.ts (tickPet/tickAllPets) but is NOT wired to the game \u2014 GameActionsContext.tsx:1128 calls tickPetsForWeek instead.",
  "broken": [
   {
    "what": "Vaccination is a paid feature ($200 service sets vaccinated=true in payForVet) but the LIVE weekly tick never reads pet.vaccinated \u2014 the sickness roll is a flat 2%/6% with no vax adjustment. Only the DEAD lib/pets/decay.ts:78 honors vaccination. So the 'Vaccinated' shield and the vet purchase are purely cosmetic.",
    "evidence": "contexts/game/actions/weekly/applyPets.ts:93",
    "severity": "dead-feature"
   },
   {
    "what": "The sophisticated lib/pets/decay.ts (breed-specific hunger decay, life-stage bands, vaccination-halved illness, passive energy recovery, per-sickness healthDrain, death causes) is dead code in production \u2014 referenced only by its own test and by effectiveHungerDecay (UI). The wired tick is the cruder tickPetsForWeek.",
    "evidence": "contexts/game/GameActionsContext.tsx:1128",
    "severity": "dead-feature"
   },
   {
    "what": "Breed traits the profile advertises are fiction vs the live tick: 'Base hunger -12/wk', 'Now -Nwk' (effectiveHungerDecay), 'Rest +35/wk' energy recovery, 'Illness 2.5%/wk', 'Age illness x2.5' all come from breed/life-stage data the live tick ignores (it uses flat \u22128 hunger, flat 2/6% illness, and ZERO passive energy recovery). A turtle and a dog decay identically.",
    "evidence": "components/mobile/PetApp.tsx:596",
    "severity": "unread-state"
   },
   {
    "what": "Passive pet energy never recovers: tickPetsForWeek never modifies energy, so energy only drops via playWithPet (\u221220) and only rises via petSleep (+50, once/wk). The catalog energyRecoveryPerWeek and the 'Rest +N/wk' chip imply weekly regen that never happens \u2014 the Agility Race (energy\u226560) is only reachable via manual Sleep.",
    "evidence": "contexts/game/actions/weekly/applyPets.ts:48",
    "severity": "unread-state"
   },
   {
    "what": "'Companion bonus' and per-pet 'Bond with you' numbers are computed by bondingSummary (player happiness \u00b1, player health +0.6/healthy pet, neglect penalty) and shown in the UI, but the tick applies a DIFFERENT, cruder rule (+2 happiness per happy&healthy pet, no player health, \u2212$15/pet). The advertised player-health-from-pets is never applied and a miserable pet never actually saps the player.",
    "evidence": "components/mobile/PetApp.tsx:153",
    "severity": "unread-state"
   },
   {
    "what": "Sickness severity is cosmetic in the live tick: it drains a flat \u22125 health/wk for ANY illness and picks the type from a hardcoded ['cold','infection','parasite','injury'] array that duplicates PET_SICKNESSES instead of importing it \u2014 so the catalog's per-sickness healthDrain (2 vs 5) and severity are ignored; a mild Cold equals a moderate Infection.",
    "evidence": "contexts/game/actions/weekly/applyPets.ts:102",
    "severity": "content-starved"
   },
   {
    "what": "Pet.ownedToys is a dead duplicate of Pet.toys: actions only ever write `toys` (buyToy), the UI reads `toys ?? ownedToys` as a legacy fallback, and nothing writes ownedToys \u2014 a permanently-unread field.",
    "evidence": "contexts/game/types.ts:77",
    "severity": "unread-state"
   }
  ],
  "proposals": [
   {
    "name": "Wire the real care engine (vaccination + breed + life stage become true)",
    "pitch": "Route the weekly pet tick through the already-built, already-tested lib/pets/decay.ts (tickAllPets) so vaccination halves illness, breed hunger/illness rates and life-stage bands apply, per-sickness healthDrain matters, and passive energy recovery works \u2014 making every number the UI already displays honest.",
    "fitsBecause": "The engine is finished and unit-tested; only the wiring in GameActionsContext points at the simpler applyPets.ts. This is the charter's #1 priority (finish a half-built system) and removes four UI-vs-mechanics lies at once.",
    "stateChanges": "None new \u2014 decay.ts already reads existing Pet fields (vaccinated, sickness, energy, weeksAtZeroHealth). No migration.",
    "actions": "None (player-facing actions unchanged).",
    "tickHook": "Swap the tickPetsForWeek call at GameActionsContext.tsx:1128 for tickAllPets seeded from the same pre-rolled RNG (rollFor keyed by pet id); reconcile death threshold (decay uses weeksAtZeroHealth>=2 vs current >=3) and the food-cost/happiness side effects.",
    "uiSurface": "None \u2014 PetApp already renders health/happiness/energy/hunger, breed traits, and life-stage; they simply become accurate.",
    "content": "None \u2014 PET_BREEDS/PET_SICKNESSES already carry the data decay.ts consumes.",
    "tests": "Update the equivalence snapshots (subsystemEquivalence + petSicknessWrap encode the OLD flat behavior \u2014 intentional churn), add coverage that vaccinated pets fall sick less and that severe illness drains faster than mild.",
    "effort": "M",
    "risk": "Balance shift + snapshot churn: passive energy recovery + breed rates + 2-week death threshold change pet mortality and competition readiness. Mitigate by reconciling constants deliberately (keep 3-week zero-health death if desired) and reviewing death rate against the current baseline before landing."
   },
   {
    "name": "Bonding that actually pays",
    "pitch": "Apply bondingSummary() deltas in the weekly tick \u2014 well-cared pets grant the player happiness AND a small health boost, neglected pets sap happiness \u2014 so the 'Companion bonus' and per-pet 'Bond with you' the UI already advertises finally happen.",
    "fitsBecause": "bonding.ts is written, tested, and displayed; only the tick uses a cruder +2/pet calc. Deepens the existing care loop and closes a UI/mechanics gap with almost no new surface.",
    "stateChanges": "None new (reads existing pet happiness/health).",
    "actions": "None.",
    "tickHook": "In applyPetLivingSideEffects (contexts/game/actions/weekly/applyPets.ts:172), replace the flat +2/pet with bondingSummary(updatedPets): add playerHappinessDelta and playerHealthDelta to ctx.newStats (clamped 0-100); keep the $15/pet food cost.",
    "uiSurface": "None \u2014 the Companion bonus card and profile bond stats already show these numbers.",
    "content": "None.",
    "tests": "New unit test asserting the applied happiness/health deltas equal bondingSummary output and that a neglected pet reduces player happiness; update applyPetLivingSideEffects snapshots.",
    "effort": "S",
    "risk": "Happiness/health inflation with many pets. Mitigate with a hard weekly cap on the total pet-derived delta (e.g. clamp to [-5,+5] happiness, [0,+3] health) per the economy guardrails."
   },
   {
    "name": "Champion titles \u2014 turn competition wins into a progression",
    "pitch": "Give pets a visible title ladder (Novice -> Contender -> Champion -> Grand Champion) derived from the already-tracked competitionWins, unlock a locked 'Champions Circuit' top-tier show at a win threshold, and grant a one-time small player-reputation bump when a pet earns a new title \u2014 so wins mean something beyond the per-entry prize.",
    "fitsBecause": "competitionWins is already stored and shown as a medallion, and already feeds a tiny reputationBonus inside win-odds, but there is nothing to aim for. Adds a spend/goal target to the money loop without a new system.",
    "stateChanges": "Additive optional `petTitleAwarded?: string` (highest title already granted) on Pet, read via ??; bump STATE_VERSION with a no-op default migration (v22).",
    "actions": "Extend enterCompetition to compute petTitle(competitionWins) and, when it crosses a new tier, grant a small capped reputation bonus once (via the established money/stat helper with a reason).",
    "tickHook": "None (event-driven on competition win).",
    "uiSurface": "Title chip on the Compete arena hero + on the roster rail avatar; a locked 'Champions Circuit' competition card that unlocks at N cumulative wins (reuses the existing compCard + win-odds ProgressRing).",
    "content": "1-2 new PET_COMPETITIONS entries (high entry/prize, requirement 'all') + a pure petTitle(wins) helper in lib/pets/competition.ts.",
    "tests": "petTitle threshold boundaries; reputation granted exactly once per tier; new competition eligibility gating.",
    "effort": "M",
    "risk": "Reputation inflation. Mitigate: small (+2) one-time grants, a handful of tiers, gated behind real win counts (each win is once-per-week-per-pet)."
   },
   {
    "name": "Sickness severity that bites (standalone alt to proposal 1)",
    "pitch": "Make the live tick import PET_SICKNESSES and drain health by each illness's own healthDrain instead of a flat \u22125, so a Cold (2) is survivable and an Infection (5) is a real emergency \u2014 giving the vet's treatment/surgery services genuine stakes.",
    "fitsBecause": "The catalog already defines severity + healthDrain + treatmentCost per sickness; the tick just ignores it and duplicates the type list. Finishes a content system already displayed in the vet clinic chart. Fold into proposal 1 if that lands (decay.ts already does this).",
    "stateChanges": "None.",
    "actions": "None.",
    "tickHook": "In tickPetsForWeek (applyPets.ts:94,102), replace the hardcoded array with PET_SICKNESSES ids and drain by findSickness(sickness).healthDrain.",
    "uiSurface": "None \u2014 the vet chart already shows sickness name + severity.",
    "content": "Optionally expand PET_SICKNESSES beyond the current 4 for variety.",
    "tests": "Per-sickness drain matches catalog; type selection stays deterministic under pre-rolled RNG.",
    "effort": "S",
    "risk": "Deadlier severe illness. Mitigate: drains already sit in the safe 2-5 range; keep them there and ensure vet 'treatment'/'surgery' remain affordable counters."
   }
  ],
  "flawlessChecklist": [
   "Wire vaccination into the live tick (applyPets.ts:93) so the $200 vaccination service and the 'Vaccinated' shield actually reduce illness \u2014 today they are cosmetic.",
   "Reconcile the breed-traits card (PetApp.tsx:594-598) with reality: the tick uses flat \u22128 hunger, flat 2/6% illness, and no passive energy recovery, so 'Base hunger -12/wk', 'Rest +35/wk', 'Illness x/wk' and 'Age illness x2.5' are all false (ideally route through lib/pets/decay.ts which implements them).",
   "Align the 'Companion bonus'/per-pet 'Bond with you' figures (bondingSummary) with the tick's actual +2/pet-happiness, no-player-health, \u2212$15/pet rule (applyPets.ts:172) \u2014 the advertised player health-from-pets is never applied.",
   "Use PET_SICKNESSES.healthDrain in the tick (applyPets.ts:102) instead of a flat \u22125 for every illness, and stop duplicating the sickness-type list (applyPets.ts:94).",
   "Collapse the dead ownedToys duplicate into toys (types.ts:76-77) with a migration so old saves' ownedToys are preserved.",
   "Make the stage 'Feed' care button feed the selected pet with best owned food (or relabel it 'Shop') \u2014 today Play/Sleep act in place but Feed/Vet just switch tabs (PetApp.tsx:273,276)."
  ]
 },
 {
  "app": "EducationApp",
  "inventory": "Three tabs. CATALOG: 11 programs grouped into 5 display tiers (foundation->professional), each enrolled via EnrollModal offering cash or student-loan (routed through the banking system with a merit-scholarship quote). ENROLLED: course cards with a progress ProgressRing, GPA band chip, weeks-left/semester/exam chips, and always-visible Study/Pause/Withdraw. EARNED: transcript hero + credential rows, each opening a rich detail page (stat grid, classes list, linked student loan, scholarship rate, about). Actions (contexts/game/actions/EducationActions.ts): enrollInProgram (cash|loan; creates a real Loan named 'Student Loan: <name>'), studyExtra (\u221215 energy/\u22125 happiness, +1 week progress, capped 3/wk via weeklyStudySessions), togglePauseProgram (re-anchors exam/campus cadence on resume), withdrawFromProgram, clearCampusEvent (UNUSED). Education state (types.ts:555-574): weeksRemaining,paused,completed,gpa,examsPassed/Failed,enrolledClasses[],studyGroupActive,semesterNumber,lastExamWeek,lastCampusEventWeek; plus gameState.weeklyStudySessions{}, pendingCampusEventEducationId, and student loans in gameState.loans[]. Weekly tick: applyEducationStress (happiness/health/energy penalty scaled 1.0/1.3/1.6x by active count, capped) + applyEducationProgression (decrement weeksRemaining with Fast Learner x1.5; study-group +2 happy/\u22123 energy; student-loan weekly payment; exam every 13wk via runExam->updateGPA; campus-event roll sets pendingCampusEventEducationId; completion applies enrolledClasses statBonuses). lib/education/educationSystem.ts additionally defines 19 CLASS_TEMPLATES, 8 CAMPUS_EVENTS with choices, STUDY_GROUP_BENEFITS, getAvailableClasses, getRandomCampusEvent, calculateStudentLoan \u2014 all UNUSED by production (tests only).",
  "broken": [
   {
    "what": "The entire class system is dead: enroll() initializes enrolledClasses to [] and nothing ever populates it, so the 19 CLASS_TEMPLATES + getAvailableClasses are unused, the completion stat-bonus loop (applyEducationProgression.ts:144) always iterates an empty array, exam difficulty (getAverageDifficulty) always defaults to 2, and the detail 'Classes' section (EducationApp.tsx:928) never renders.",
    "evidence": "lib/education/operations.ts:53",
    "severity": "dead-feature"
   },
   {
    "what": "Campus events fire in the tick and set pendingCampusEventEducationId, but NO component reads that flag, getRandomCampusEvent + the 8 CAMPUS_EVENTS (with choices/effects/resultText) are never shown, and clearCampusEvent is referenced only inside a dead `{false && <Text>{String(clearCampusEvent)}</Text>}` block. The player never sees or resolves a campus event; the flag is set-but-never-read/cleared.",
    "evidence": "components/mobile/EducationApp.tsx:547",
    "severity": "dead-feature"
   },
   {
    "what": "studyGroupActive is read by the tick (+2 happy/\u22123 energy at applyEducationProgression.ts:93 and the +15% exam pass bonus at :110) and by the UI ('Study group' chip, 'study group active' subtitle), but NO code path ever sets it to true \u2014 operations.ts:57 sets it false at enroll and it stays false forever. A fully-built mechanic with no activation.",
    "evidence": "lib/education/operations.ts:57",
    "severity": "dead-feature"
   },
   {
    "what": "semesterNumber is set to 1 at enroll and never incremented anywhere, so every course displays 'Sem 1' forever in three places (card chip, detail chip, detail stat) \u2014 a frozen progression readout.",
    "evidence": "lib/education/operations.ts:58",
    "severity": "dead-end"
   },
   {
    "what": "getAvailableClasses, getRandomCampusEvent, STUDY_GROUP_BENEFITS, and calculateStudentLoan are exported and unit-tested but referenced ONLY by the stress test \u2014 zero production callers. A large, rich content/logic surface (19 classes, 8 events) sits unwired.",
    "evidence": "lib/education/educationSystem.ts:220",
    "severity": "dead-feature"
   },
   {
    "what": "calculateStudentLoan (educationSystem.ts:511, 4.5% / 260wk) is superseded by the banking amortization path in enrollInProgram (calculatePeriodicPayment, 6% / 520wk) \u2014 two divergent student-loan formulas, only the banking one live, inviting drift.",
    "evidence": "contexts/game/actions/EducationActions.ts:139",
    "severity": "dead-feature"
   }
  ],
  "proposals": [
   {
    "name": "Pick your classes each semester (finish the class system)",
    "pitch": "At enrollment (and each new semester) let the player choose 2-3 classes from getAvailableClasses(programId) and store them in the existing enrolledClasses \u2014 instantly lighting up exam difficulty, completion stat bonuses, and the detail 'Classes' section that are all ALREADY wired to that field.",
    "fitsBecause": "Everything downstream of enrolledClasses already consumes it (getAverageDifficulty in runExam, the completion bonus loop, the CourseDetail Classes list) \u2014 only the one population step is missing. Highest-value finish of a half-built system with 19 classes of ready content.",
    "stateChanges": "None new \u2014 Education.enrolledClasses and the EducationClass type already exist and are optional; default [] keeps old saves valid.",
    "actions": "Extend enrollInProgram to accept chosen class ids and map CLASS_TEMPLATES -> EducationClass[] (completed:false); add a selectSemesterClasses(educationId, ids) action for later semesters. Both pure via lib/education/operations.",
    "tickHook": "None new \u2014 applyEducationProgression.ts:144 completion bonuses and runExam difficulty already read enrolledClasses; they simply stop being no-ops.",
    "uiSurface": "Add a class-picker step to the existing EnrollModal (glass tiles per ClassTemplate showing category/difficulty/statBonuses); the detail 'Classes' section already renders the result.",
    "content": "Already present: CLASS_TEMPLATES (19 core/elective/lab/seminar entries with availableIn + statBonuses).",
    "tests": "enroll populates enrolledClasses from chosen ids; completion applies the summed statBonuses; higher average difficulty lowers exam pass odds; alreadyTaken filtering.",
    "effort": "M",
    "risk": "EnrollModal surface change + stat-bonus economy. Mitigate: cap classes per semester (2-3), keep statBonuses small (existing 1-6), default to auto-pick if the player skips so enroll never blocks."
   },
   {
    "name": "Campus events you actually attend (finish the event system)",
    "pitch": "Surface the campus event the tick already flags as an on-DNA modal using the existing getRandomCampusEvent + CampusEventChoice.effects, let the player pick a choice (apply effects), then clear it \u2014 turning a dead background flag into the app's signature interactive beat.",
    "fitsBecause": "The tick already fires and stores pendingCampusEventEducationId, and CAMPUS_EVENTS (8 events x 2 choices with effects + resultText) plus clearCampusEvent already exist unused. Pure finishing work.",
    "stateChanges": "Additive: store the chosen event id alongside the education id \u2014 add optional pendingCampusEventId?: string (keep the existing pendingCampusEventEducationId). Bump STATE_VERSION with a default-fill migration (v22).",
    "actions": "New resolveCampusEvent(educationId, choiceIndex) applying the choice.effects to stats/money via the established helpers, then clearing both pending fields; clearCampusEvent already exists for dismissal.",
    "tickHook": "In applyEducationProgression.ts:136, when an event fires, also select a CAMPUS_EVENTS index deterministically from a pre-rolled value (no Math.random in the updater) and store it in the new field.",
    "uiSurface": "A CampusEventModal (glass card, choice buttons, result text) shown when pendingCampusEventId is set \u2014 mirrors the existing EnrollModal DNA; also removes the dead `{false && ...}` clearCampusEvent block.",
    "content": "Already present: 8 CAMPUS_EVENTS; easy to add more as data.",
    "tests": "resolveCampusEvent applies each choice's effects and clears the flags; event selection is deterministic under pre-rolled RNG; migration is idempotent.",
    "effort": "M",
    "risk": "RNG determinism under React StrictMode + additive migration. Mitigate: pre-roll the event index like the existing pet/loan pre-rolls; guard the new field with ?? everywhere."
   },
   {
    "name": "Form a study group (finish the study-group system)",
    "pitch": "Add a per-course 'Join study group' action that sets studyGroupActive=true, activating the already-wired +2 happiness/\u22123 energy weekly bonus and +15% exam pass boost \u2014 the 'Study group' chip the UI already renders finally lights up.",
    "fitsBecause": "studyGroupActive is read by the tick, runExam, and the UI but never written true; STUDY_GROUP_BENEFITS already documents the numbers. Cheapest possible full-system finish.",
    "stateChanges": "None new \u2014 studyGroupActive already exists (optional bool).",
    "actions": "New toggleStudyGroup(educationId) in EducationActions (pure operations helper) that flips the flag; optionally require a small one-time cost or reputation gate so it is a tradeoff, not free.",
    "tickHook": "None new \u2014 applyEducationProgression.ts:93/110 already branch on studyGroupActive.",
    "uiSurface": "A 'Study group' toggle button in the CourseCard and CourseDetail action rows (next to Study/Pause/Withdraw), reflecting active state.",
    "content": "None (STUDY_GROUP_BENEFITS constant already present).",
    "tests": "Action sets/clears the flag; tick applies the weekly bonus only when active; exam pass odds rise by the study-group bonus.",
    "effort": "S",
    "risk": "Near-free happiness. Mitigate: the \u22123 energy/wk already offsets it; add a small join cost or a minimum-GPA/enrollment gate and keep the happiness bonus capped as today."
   },
   {
    "name": "Semesters that advance",
    "pitch": "Increment semesterNumber as a program progresses (each ~half-year of remaining weeks consumed) so the 'Sem N' chip and semester stat reflect real progress instead of a frozen 'Sem 1', and (with proposal 1) prompt a fresh class pick at each new semester.",
    "fitsBecause": "semesterNumber already exists and is displayed in three places but never moves. Small, satisfying progression polish on an existing readout.",
    "stateChanges": "None new \u2014 semesterNumber already optional.",
    "actions": "None required (tick-driven); pairs with selectSemesterClasses from proposal 1 for the class re-pick.",
    "tickHook": "In applyEducationProgression, derive the current semester from duration vs weeksRemaining and bump semesterNumber on boundary crossings \u2014 using progression, not wall-clock, so a paused program does not advance (reuse the cadence-anchor discipline from togglePauseProgram).",
    "uiSurface": "None new \u2014 EducationApp.tsx:696/908/920 already show semesterNumber.",
    "content": "None.",
    "tests": "Boundary crossings increment exactly once; paused programs do not advance; completed programs freeze the final semester.",
    "effort": "S",
    "risk": "Double-counting across pause/resume. Mitigate: compute semester purely from weeksRemaining/duration (idempotent), not an accumulator."
   },
   {
    "name": "Merit stipend \u2014 reward maintaining a high GPA",
    "pitch": "While actively enrolled with a best GPA >= 3.5, pay a small capped weekly merit stipend (scaled by meritRate) so the 'Academic standing' hero and the detail 'Scholarship rate %' the UI already shows translate into ongoing money \u2014 giving study sessions and study groups a payoff and offsetting tuition.",
    "fitsBecause": "meritRate + gpaBand already exist, drive the enrollment scholarship, and are displayed as a 'scholarship rate', but merit only applies once at enroll. Deepens the study loop and makes GPA matter every week, not just at hiring.",
    "stateChanges": "None new (derived each week from bestGpa + active enrollment).",
    "actions": "None (tick-driven).",
    "tickHook": "New contexts/game/actions/weekly/applyMeritStipend.ts (wired beside applyEducationProgression): while any non-paused education is active and highestGpa>=3.5, credit min(cap, meritRate(gpa) * base) to money with a reason; no-op otherwise. Idempotent per week, weeksLived-based.",
    "uiSurface": "A '+$X/wk merit' chip on the Enrolled hero stat strip; the detail already shows the scholarship rate.",
    "content": "None (meritRate table already present).",
    "tests": "Stipend paid only at GPA>=threshold and only while actively enrolled; hard weekly cap respected; zero when paused/withdrawn/completed.",
    "effort": "M",
    "risk": "Money printing. Mitigate: hard cap (e.g. <= $150/wk total across programs), require active enrollment (which already carries the happiness/health/energy stress penalty), and scale modestly so it offsets rather than exceeds tuition."
   }
  ],
  "flawlessChecklist": [
   "Populate enrolledClasses at enroll (operations.ts:53) so the completion stat-bonus loop (applyEducationProgression.ts:144) and the detail 'Classes' section (EducationApp.tsx:928) stop being permanently dead.",
   "Surface and resolve campus events: read pendingCampusEventEducationId (set at GameActionsContext.tsx:1711) in a modal and call clearCampusEvent, then delete the dead `{false && <Text>{String(clearCampusEvent)}</Text>}` guard (EducationApp.tsx:547).",
   "Give studyGroupActive a way to become true (operations.ts:57 hard-codes false) so the tick's study-group bonus and exam boost and the 'Study group' chip can ever occur.",
   "Advance semesterNumber (frozen at 1 in operations.ts:58) or remove the 'Sem 1' chip/stat that today lies on every course.",
   "Unify the two student-loan formulas: calculateStudentLoan (educationSystem.ts:511) is dead and diverges from the live banking amortization in enrollInProgram (EducationActions.ts:139) \u2014 adopt one to prevent drift.",
   "Either wire or remove getAvailableClasses/getRandomCampusEvent/STUDY_GROUP_BENEFITS (educationSystem.ts) \u2014 currently exported and tested but never called by the app, masking that the class/event/study-group features are unfinished."
  ]
 },
 {
  "app": "Hustle (business empire \u2014 components/mobile/Hustle/**, HustleActions, lib/business/*)",
  "inventory": "Player founds companies (5 industries via CreateCompanyScreen\u2192createCompany, canonical CompanyActions). Per company (CompanyDetailScreen + 5 modals): hire/fire GENERIC workers (addWorker/removeWorker\u2192headcount income multiplier) and NAMED hires (HireEmployeeModal\u2192hireCandidate/fireNamedHire; a named hire = +1 employee + sign-on/severance cash); launch/cancel 5 marketing campaign kinds (weekly spend + ROI revenue lift); IPO float 10/25/40% (\u2192cash + reputation); accept/decline procedural acquisition offers (\u2192market share + reputation); resolve scandals (5 methods); buy canonical company upgrades (ProgressRing tracks); notifications inbox. Dashboard = empire KPIs + per-company tiles + lifetime Milestones. State: gameState.hustleApp{companies:Record<id,HustleCompanyOverlay>, lifetimeStats, lastOpenedTimestamp}. Overlay: hiringPipeline{candidates,namedHires,weeksSinceLastHire,totalSeverance}, activeCampaigns, brand{score,trend}, activeScandal, scandalHistory, boardSeats, ipo{status,ownershipPercent,sharePrice,sharesOutstandingK,recentEarnings}, pendingAcquisitions, suppliers, marketSharePercent, notifications. Weekly tick (lib/business/hustleTick.ts): campaign spend/lift, brand drift toward 50, scandal decay+revenue-drag, hire morale/perf drift, market-share recompute, acquisition-offer gen (every 8w), IPO quarterly earnings (every 12w). brand + marketSharePercent scale each company's weeklyIncome via the passiveIncome factor 1+(brand-50)/200+share/200 clamped [0.75,1.6] (lib/economy/passiveIncome.ts:314).",
  "broken": [
   {
    "what": "Scandals never occur. triggerScandal() is defined but has ZERO callers anywhere; the weekly tick only DECAYS an existing activeScandal, it never spawns one. So the entire scandal subsystem \u2014 ResolveScandalModal, resolveScandal(), the sticky scandal banner, SCANDAL_HEADLINES/SCANDAL_BASE_SEVERITY content, scandalRevenueDrag, the scandal ledger, and the 'Scandals survived' milestone \u2014 is unreachable in normal play.",
    "evidence": "contexts/game/actions/HustleActions.ts:466 (only definition, no callers); lib/business/hustleTick.ts:148 (only decays, never creates)",
    "severity": "dead-feature"
   },
   {
    "what": "boardSeats is initialized to [] and never written by any code path. The 'Board of directors' roster section in CompanyDetailScreen only renders when boardSeats.length>0, so it never shows. HustleBoardMember type, BOARD_ROLE labels, voting/alignment/satisfaction, and the 'board_vote' notification type are all unused (the HustleActions header comment even claims 'board votes' coverage).",
    "evidence": "contexts/game/actions/HustleActions.ts:117 (init []), saveMigrations.ts:439 (init []); components/mobile/Hustle/screens/CompanyDetailScreen.tsx:439 (render gate never true)",
    "severity": "dead-feature"
   },
   {
    "what": "suppliers is initialized to [] and never written by any code path. The 'Suppliers' roster section in CompanyDetailScreen renders only when suppliers.length>0, so it never shows. HustleSupplier type (costPerWeek/reliability/contractEndWeek) and the 'supplier_issue' notification type are unused (header comment claims 'supplier deals' coverage).",
    "evidence": "contexts/game/actions/HustleActions.ts:120 (init []); components/mobile/Hustle/screens/CompanyDetailScreen.tsx:559 (render gate never true)",
    "severity": "dead-feature"
   },
   {
    "what": "Dashboard Milestones 'Founded' and 'Sold' tiles are permanently 0. totalCompaniesFounded / totalCompaniesSold are only ever assigned 0 \u2014 no += anywhere. createCompany never touches hustleApp, and sellCompany (company.ts:306) has no live caller (only a doc-comment reference), so companies cannot be sold at all.",
    "evidence": "contexts/game/initialState.ts:1603-1604 (only writes, =0); DashboardScreen.tsx:256-257 (reads for tiles); contexts/game/company.ts:306 (sellCompany defined, unreferenced by UI)",
    "severity": "unread-state"
   },
   {
    "what": "IPO is pure upside with a cosmetic share price. ownershipPercent is only displayed \u2014 the income factor in passiveIncome uses brand+marketShare only, never scales company income by ownership \u2014 so floating 25-40% costs nothing. No dividends are ever paid (the tick only moves sharePrice), and there is no way to sell your stake, so quarterly beat/miss and peakSharePrice track a number the player can never realize. HustleTickResult's comment promises 'quarterly dividends' that don't exist.",
    "evidence": "lib/economy/passiveIncome.ts:314 (factor ignores ownershipPercent); contexts/game/actions/HustleActions.ts:651 (sets ownershipPercent, display only); lib/business/hustleTick.ts:229-252 (earnings move sharePrice, credit no cash)",
    "severity": "dead-end"
   },
   {
    "what": "Named-hire quality is cosmetic. candidate.experience is generated but never read; named-hire performance/morale drift weekly and render as meters, but nothing consumes them economically \u2014 company income depends on headcount + brand + market share only. A star engineer and a demoralized operations hire produce identical revenue.",
    "evidence": "lib/business/hustleLogic.ts:82 (experience generated, unused); lib/business/hustleTick.ts:193-204 (perf/morale drift); lib/economy/passiveIncome.ts:314 (income factor ignores hires)",
    "severity": "unread-state"
   },
   {
    "what": "A newly founded company gets no Hustle overlay until the player opens a modal. createCompany does not seed hustleApp.companies[id]; the migration only seeds overlays for pre-existing companies. The weekly tick skips companies with no overlay, so a fresh company has no brand drift, no market-share evolution, and never receives acquisition offers until an action (e.g. opening the hire modal) lazily creates the overlay via ensureOverlay.",
    "evidence": "contexts/game/actions/CompanyActions.ts:98-109 (return omits hustleApp); lib/business/hustleTick.ts:94 (`if (!prevOverlay) continue`)",
    "severity": "no-op"
   }
  ],
  "proposals": [
   {
    "name": "Scandal event engine (activate the dead scandal system)",
    "pitch": "Roll organic scandals in the weekly tick so the fully-built resolution UI, headlines, revenue-drag and ledger finally fire.",
    "fitsBecause": "ResolveScandalModal, resolveScandal(), SCANDAL_HEADLINES/SCANDAL_BASE_SEVERITY, the scandal banner, scandalRevenueDrag and the scandalHistory ledger all already exist \u2014 only the trigger is missing (triggerScandal has zero callers). Highest leverage: turns ~4 already-written files into a live loop.",
    "stateChanges": "No new top-level state \u2014 reuse HustleActiveScandal + scandalHistory. Accumulate the weekly drag onto the active scandal, then write it into record.totalRevenueLoss/finalReputationLoss on resolve (both currently hardcoded 0). Optional additive overlay field scandalRiskModifier?:number read via ??.",
    "tickHook": "New contexts/game/actions/weekly/applyHustleScandalRoll.ts (matching the ~30 siblings) OR fold into hustleTick: per-company seeded roll on weeksLived, base ~2-3%/wk scaled by brand (low brand raises risk), company size and industry; respect the existing one-scandal-at-a-time guard and add a post-resolution cooldown.",
    "actions": "No new player action (resolveScandal already handles the response); add internal spawn logic reusing triggerScandal.",
    "uiSurface": "None new \u2014 existing scandal banner (CompanyDetailScreen), ResolveScandalModal and the Recent-alerts inbox already render it.",
    "content": "SCANDAL_HEADLINES ships 2 lines/kind \u2014 expand to 4-5/kind for variety (lib/business/hustleLogic.ts).",
    "tests": "Deterministic spawn under a seeded roll; never spawns a 2nd concurrent scandal; drag accumulates into the record; idempotent per week; cooldown respected.",
    "effort": "M",
    "risk": "Scandals could feel punishing \u2014 mitigate with brand/size gating, post-resolution cooldown, and the already-bounded severity/drag (scandalRevenueDrag caps at 30%)."
   },
   {
    "name": "IPO teeth: dilution, dividends & a sellable stake",
    "pitch": "Make going public a real trade-off \u2014 float dilutes your income share, quarterly earnings pay dividends on your stake, and you can sell shares to realize the share price.",
    "fitsBecause": "The IPO flow, ownershipPercent, sharePrice, sharesOutstandingK, recentEarnings and quarterly-earnings movement already exist but ownershipPercent is display-only and share price is unrealizable (a dead-end). This finishes the half-built IPO.",
    "stateChanges": "Additive \u2014 reuse ipo.ownershipPercent/sharePrice/sharesOutstandingK; no retypes. Optional ipo.dividendsPaidTotal?:number for the ledger.",
    "tickHook": "In the existing hustleTick IPO block (every 12w): credit a bounded dividend = company.weeklyIncome*12 * payoutRate * ownershipPercent/100 into cashDelta/cashReasons; and in passiveIncome.ts scale the company income factor by ownershipPercent/100 while public.",
    "actions": "New sellShares(setGameState, gameState, companyId, pct) in HustleActions \u2014 credits cash at current sharePrice, lowers ownershipPercent, uses applyMoneyDelta with a reason; keep launchIPO.",
    "uiSurface": "Extend the existing IPOModal public branch with a 'Sell shares' control and a dividend read-out (same sheet, on-DNA).",
    "content": "None.",
    "tests": "Dilution reduces player company income while public; dividends credited only when public and on the 12w cadence; sellShares bounded to owned float and atomic; peakSharePrice still tracked.",
    "effort": "M",
    "risk": "Dividends could over-pay \u2014 bound payoutRate and net it against the income dilution so IPO stays a genuine tradeoff, not free money."
   },
   {
    "name": "Board of directors governance",
    "pitch": "Populate the empty board roster at IPO and let board alignment/satisfaction pressure your decisions and buff/penalize revenue.",
    "fitsBecause": "HustleBoardMember, the boardSeats overlay field, BOARD_ROLE labels, the 'board_vote' notification type and the board roster UI already exist but boardSeats is never written (dead).",
    "stateChanges": "Additive \u2014 write boardSeats:HustleBoardMember[] inside launchIPO (investors take seats proportional to float%); drift member.satisfaction in the tick. No new top-level state.",
    "tickHook": "In hustleTick: update board satisfaction from brand + earnings beats; low aggregate satisfaction emits a 'board_vote' pressure notification (type already exists).",
    "actions": "New respondToBoardVote(companyId, voteId, choice) in HustleActions applying the vote effect once (guarded like the other actions).",
    "uiSurface": "Board roster already renders in CompanyDetailScreen; add a lightweight board-vote sheet via the existing HustleApp modal host.",
    "content": "Board-member name pool + a handful of vote templates in a new lib/business/board.ts data module.",
    "tests": "Seats created at IPO sum to float%; satisfaction drift bounded; vote resolution applies its effect exactly once; idempotent per week.",
    "effort": "L",
    "risk": "Biggest new surface (a decision loop) \u2014 scope strictly to IPO'd companies and ship vote templates as data so it stays additive and bounded."
   },
   {
    "name": "Supplier contracts & reliability",
    "pitch": "Give each company suppliers with a weekly cost and reliability; flaky suppliers dent revenue or seed product-defect scandals, and you can renegotiate.",
    "fitsBecause": "HustleSupplier, the suppliers overlay field and the supplier roster UI already exist but suppliers is never populated (dead). Pairs naturally with the scandal engine.",
    "stateChanges": "Additive \u2014 seed suppliers:HustleSupplier[] on founding/first tick; no new top-level state.",
    "tickHook": "In hustleTick: charge supplier costPerWeek via cashDelta/cashReasons; a seeded reliability roll applies a small revenue hit or (cheaply) raises scandal risk (ties to the scandal engine proposal).",
    "actions": "New signSupplier / cancelSupplier(companyId, supplierId) in HustleActions (money via applyMoneyDelta with a reason).",
    "uiSurface": "Supplier roster already renders in CompanyDetailScreen; add a supplier-marketplace sheet (on-DNA).",
    "content": "Per-industry supplier catalog in a new lib/business/suppliers.ts (name/cost/reliability).",
    "tests": "Costs debited weekly; reliability roll deterministic under seed; contract expiry handled; idempotent per week.",
    "effort": "M",
    "risk": "Adds a recurring cost \u2014 keep costPerWeek a small % of weekly income and default reliability high so it deepens rather than punishes."
   },
   {
    "name": "Named-hire productivity payoff",
    "pitch": "Let a company's key-hire performance/morale actually move revenue or brand, so hiring quality and retention finally matter.",
    "fitsBecause": "namedHires carry skill/performance/morale that are generated, drift weekly and render as meters, but nothing reads them for income (cosmetic). Cheapest way to make the whole hiring loop meaningful.",
    "stateChanges": "None new \u2014 read existing hiringPipeline.namedHires performance/morale.",
    "tickHook": "In hustleTick (or the passiveIncome factor): add a small bounded lift from average key-hire performance (e.g. a \u00b15-8% income nudge or a brand delta).",
    "actions": "None new \u2014 hireCandidate/fireNamedHire already exist.",
    "uiSurface": "None new \u2014 the meters are already shown in CompanyDetailScreen and HireEmployeeModal.",
    "content": "None.",
    "tests": "A high-performance roster raises income within the cap; empty roster is neutral; combined factor stays within the existing [0.75,1.6] clamp.",
    "effort": "S",
    "risk": "Could compound with the brand/share factor \u2014 reuse the existing passiveIncome clamp so the combined multiplier stays bounded."
   }
  ],
  "flawlessChecklist": [
   "Wire triggerScandal to a real weekly trigger (or gate the scandal UI so ResolveScandalModal/scandal banner aren't orphaned surfaces).",
   "Seed a per-company overlay in createCompany and increment totalCompaniesFounded, so new companies join the weekly tick and the 'Founded' milestone stops reading 0.",
   "Fix the campaign 'paused' path in hustleTick.ts:109 \u2014 it DELETES the campaign while the notification says 'paused'; either truly pause (keep it, set active=false, skip spend/lift) or change the message to 'cancelled'. The `camp.active ? '' : ' \u00b7 paused'` branch in CompanyDetailScreen.tsx:360 is currently dead because active is never set false.",
   "Reconcile projectedROI rendering: LaunchCampaignModal.tsx:110 shows it as a multiplier ('2.2\u00d7') but CompanyDetailScreen.tsx:357 shows Math.round(roi)% ('2% ROI') for the same field.",
   "Make scandalHistory.totalRevenueLoss/finalReputationLoss carry real values (or hide the always '\u2212$0 lost' ledger line at CompanyDetailScreen.tsx:604).",
   "Either wire sellCompany (company.ts:306) into a Hustle UI (incrementing totalCompaniesSold) or remove the permanently-0 'Sold' milestone tile."
  ]
 },
 {
  "app": "RealEstateApp (components/computer/RealEstateApp.tsx + RealEstateActions + lib/realEstate/*)",
  "inventory": "Player browses an 8-tier residential catalog (CATALOG inline in RealEstateApp) and buys with a mortgage (BuyPropertyModal\u2192buyPropertyWithMortgage: down-payment tiers, terms, cash option, as-residence; bridges banking quoteLoan/originateMortgage). Per owned property (ManagePropertyModal): set rent mode longTerm/airbnb/commercial + weekly rent (rent-ceiling clamped), stop renting, evict tenant, maintain (restore condition, cost scales with value), toggle laundering front (commercial only), sell (closing cost + capital-gains tax). Portfolio tab = equity dashboard (stacked equity/mortgage bar) + KPI strip + photo rows with condition bar/value trend; Listing detail sub-page = condition ProgressRing, value trend, tenant card, amenities; Activity tab = eventLog filtered to property keywords. State: gameState.realEstate:RealEstate[] with price/currentValue/purchasePrice/condition/status/rentMode/tenant{satisfaction,weeklyRent,movedInWeek}/marketCycle/cycleWeeksRemaining/weeksVacant/mortgageId/launderingFront/currentResidence/interior/rooms/upgradeLevel. Weekly tick (contexts/game/actions/weekly/applyRentAndHousing.ts \u2192 lib/realEstate/housing.processWeeklyHousing + lib/realEstate/weeklyTick.runRealEstateWeeklyTick): condition decay, appreciation, neighborhood cycle evolution (stable/gentrifying/hot/cooling), tenant satisfaction/move-out/find-tenant rolls, Airbnb rent variance, carrying costs (property tax + maintenance). Realized rent credited to cash via housingRentalIncome.",
  "broken": [
   {
    "what": "The Housing & Decoration System is fully unreachable. DECOR_ITEMS (16 items), ROOM_ADDITIONS (6), and UPGRADE_TIERS (3) in lib/realEstate/housing.ts are only consumed by components/RealEstateManager.tsx, which is imported NOWHERE (orphaned \u2014 the live app mounts RealEstateApp). ManagePropertyModal exposes only rent-mode/maintain/laundering/sell \u2014 no decorate/add-room/upgrade action. So RealEstate.interior[]/rooms[]/upgradeLevel can never be populated for properties bought in the app, and the detail chips 'X furnishings / X rooms added / Upgrade tier N' never appear. processWeeklyHousing computes happiness/rent bonuses from decor that can never exist.",
    "evidence": "components/RealEstateManager.tsx:73 (sole DECOR_ITEMS consumer, component unmounted); components/realEstate/ManagePropertyModal.tsx:19-24 (action props: no decor/room/upgrade); components/computer/RealEstateApp.tsx:879-881 (detail chips that never populate)",
    "severity": "dead-feature"
   },
   {
    "what": "The Activity tab starves. It reads gameState.eventLog filtered to property keywords, but the rich weekly tenancy/cycle events the tick generates (tenant moved in/out, neighborhood shift, property alert) are pushed to weeklyCtx.notifications (one-frame toasts), never to eventLog. eventLog is written only by event-CARD choices, so the dedicated Activity timeline is near-empty in practice while the events it was built for flash once and vanish.",
    "evidence": "components/computer/RealEstateApp.tsx:332 (Activity reads eventLog); contexts/game/actions/weekly/applyRentAndHousing.ts:98-100 (tick events \u2192 ctx.notifications only); contexts/game/GameActionsContext.tsx:2250 (eventLog written only from event choices)",
    "severity": "content-starved"
   },
   {
    "what": "Legacy housing rental income is computed then thrown away. applyRentAndHousing sets housingRentalIncome from processWeeklyHousing (which includes upgradeTier.rentBonus), then unconditionally overwrites it with the tenant-model figure from runRealEstateWeeklyTick. So the legacy rent path \u2014 including any upgrade rent bonus \u2014 is dead in production; only the tenant model pays rent.",
    "evidence": "contexts/game/actions/weekly/applyRentAndHousing.ts:73 (assigns housingRentalIncome) then :96 (overwrites it)",
    "severity": "no-op"
   },
   {
    "what": "Catalog is thin and static \u2014 8 residential properties, all filtered out of Browse once owned, so Browse permanently empties to 'You already own every property in the catalog!'. No commercial/retail/warehouse/multi-unit variety despite commercial rent mode and laundering fronts existing, so late-game has nothing to buy.",
    "evidence": "components/computer/RealEstateApp.tsx:220-229 (8-item CATALOG); components/computer/RealEstateApp.tsx:683 (permanent empty state)",
    "severity": "content-starved"
   },
   {
    "what": "Rental listings advertise a benefit they don't deliver. Every listing card shows '+N comfort/wk' and '+N energy/wk', but calculatePropertyHappiness returns 0 unless the property is the player's currentResidence \u2014 so a rented-out property's advertised comfort/energy never pays out.",
    "evidence": "components/computer/RealEstateApp.tsx:527-528 (chips on every listing); lib/realEstate/housing.ts:121 (returns 0 when !currentResidence)",
    "severity": "no-op"
   }
  ],
  "proposals": [
   {
    "name": "Bring decoration, rooms & upgrades into the app",
    "pitch": "Port the orphaned decor/room/upgrade catalog into the manage flow so interior, rooms and upgradeLevel become buyable and the value/happiness math they already feed goes live.",
    "fitsBecause": "DECOR_ITEMS(16)/ROOM_ADDITIONS(6)/UPGRADE_TIERS(3), calculatePropertyHappiness, appreciatePropertyValue and the detail chips ('furnishings/rooms/upgrade tier') already exist; only the unmounted RealEstateManager ever exposed them. Highest leverage \u2014 unlocks a whole dead content pool.",
    "stateChanges": "Additive \u2014 write the EXISTING RealEstate.interior[]/rooms[]/upgradeLevel fields; no new fields, no migration needed.",
    "tickHook": "None new \u2014 processWeeklyHousing already consumes interior/rooms/upgradeLevel for happiness/appreciation; additionally route upgradeTier.rentBonus through the tenant-model realized rent so it isn't discarded (see the double-compute finding).",
    "actions": "New RealEstateActions installDecor / addRoom / upgradeProperty (debit cash, trackBudgetSpend 'housing', reject if unaffordable \u2014 same pattern as maintainProperty).",
    "uiSurface": "New 'Improve' section in ManagePropertyModal or an Improve sub-sheet \u2014 Slate Glass tokens + emerald identity, matching the existing Rental/Maintenance/Sell sections.",
    "content": "Reuse the existing housing.ts catalogs (already data-driven).",
    "tests": "Affordability gating; happiness/value recompute after install; upgrade rent bonus reaches realized rent; budget 'housing' tracked.",
    "effort": "M",
    "risk": "Rent/appreciation could inflate \u2014 keep upgrade bonuses modest and keep the existing rent-ceiling clamp in setPropertyRentMode."
   },
   {
    "name": "Real portfolio activity timeline",
    "pitch": "Feed the Activity tab the weekly tenancy/cycle events it was built for, instead of leaving them as one-frame toasts.",
    "fitsBecause": "runRealEstateWeeklyTick already emits tenant-moved-in/out and neighborhood-shift notifications and the Activity tab UI already exists \u2014 but it reads eventLog, which never receives them.",
    "stateChanges": "Additive \u2014 new optional slice gameState.realEstateActivity?:{week;type;propertyId?;text}[] (capped ~40) added to initialState with a STATE_VERSION bump + defaulting migration (the N:(state)=>{...} pattern).",
    "tickHook": "In applyRentAndHousing, append reTick.notifications (and housing condition alerts) to realEstateActivity stamped with nextWeeksLived; keep the eventLog fallback for event-card items.",
    "actions": "None new.",
    "uiSurface": "Point the RealEstateApp Activity tab at the new slice (it already renders a timeline).",
    "content": "None \u2014 messages are already generated by the tick.",
    "tests": "Tick appends entries with the right week; cap enforced; migration defaults old saves to []; idempotent per week.",
    "effort": "S",
    "risk": "Save growth \u2014 cap the array and reuse the existing slice-cap pattern (as scandalHistory does)."
   },
   {
    "name": "Commercial & multi-unit catalog",
    "pitch": "Extend Browse past 8 homes with office/retail/warehouse and multi-unit assets so commercial rent mode and laundering fronts finally have real inventory.",
    "fitsBecause": "commercial rentMode and launderingFront already exist but every catalog entry is residential, and Browse permanently empties once the 8 homes are owned.",
    "stateChanges": "None new \u2014 same RealEstate shape.",
    "tickHook": "None new \u2014 the tenancy/cycle tick is already property-type-agnostic.",
    "actions": "None new \u2014 buyPropertyWithMortgage already handles any catalog descriptor.",
    "uiSurface": "The Browse list already renders any catalog array; add a type filter chip row (on-DNA).",
    "content": "New lib/realEstate/catalog.ts exporting the property catalog (move CATALOG out of the component; add commercial + multi-unit tiers with photos in assets/images/Real Estate).",
    "tests": "New entries buyable via the mortgage flow; commercial-only laundering gating still holds; DTI/mortgage preflight still applies.",
    "effort": "S",
    "risk": "Balance \u2014 price commercial tiers against the existing mortgage/DTI guardrails and the rent ceiling."
   },
   {
    "name": "Refinance & principal prepayment",
    "pitch": "Add manage-flow actions to refinance a mortgage at today's APR or pay down principal early, deepening the existing loan integration.",
    "fitsBecause": "Properties already carry mortgageId \u2192 loans[], and quoteLoan/originateMortgage/calculatePeriodicPayment already power buying \u2014 this reuses all of it.",
    "stateChanges": "None new \u2014 mutate the linked Loan in loans[] (remaining/rateAPR/weeklyPayment).",
    "tickHook": "None new \u2014 applyLoanAutopay already services the loan each week.",
    "actions": "New refinanceMortgage(propertyId) and prepayPrincipal(propertyId, amount) in RealEstateActions (money via the established helpers with a reason).",
    "uiSurface": "New rows in ManagePropertyModal alongside Maintenance/Sell.",
    "content": "None.",
    "tests": "Refi requotes APR and rewrites weeklyPayment; prepay reduces remaining and can close the loan; cash debited atomically; rejects when unaffordable.",
    "effort": "M",
    "risk": "Refi could be free money if APR only drops \u2014 charge closing costs (reuse the 6% pattern from operations.sellProperty)."
   },
   {
    "name": "Macro-driven market shocks (cross-app)",
    "pitch": "Let the global economy events already bannered on the Portfolio screen actually swing neighborhood cycles and property values.",
    "fitsBecause": "EconomyEventBanner is already mounted on the Portfolio screen and economy.economyEvents.modifiers already move other incomes; marketCycle + appreciation are the natural hook. Cheap cross-app tie-in.",
    "stateChanges": "None new \u2014 read economy modifiers, write the existing marketCycle/currentValue.",
    "tickHook": "In runRealEstateWeeklyTick, bias the nextCycle transition probabilities and appreciation by the active economy modifier (boom\u2192gentrifying/hot, recession\u2192cooling).",
    "actions": "None new.",
    "uiSurface": "The existing EconomyEventBanner + neighborhood cycle chips already convey it.",
    "content": "None.",
    "tests": "Boom skews cycles up and recession down under seeded rolls; neutral when no event active; appreciation bounds preserved.",
    "effort": "M",
    "risk": "Double-counting macro effects \u2014 apply a single bounded modifier and keep the existing appreciation clamp."
   }
  ],
  "flawlessChecklist": [
   "Reconnect the Activity tab to real weekly events (tenant moved in/out, neighborhood shift) \u2014 today it reads eventLog, which never receives them, so the tab is near-empty.",
   "Surface decoration/rooms/upgrades in-app OR drop the 'furnishings / rooms added / upgrade tier' detail chips that can never populate for app-bought properties.",
   "Stop advertising '+comfort/wk' and '+energy/wk' on rental listing cards \u2014 those only pay out for your currentResidence (calculatePropertyHappiness returns 0 otherwise).",
   "Remove the dead double-assignment of housingRentalIncome in applyRentAndHousing.ts (computed from processWeeklyHousing, then immediately overwritten by the tenant model), or actually use it.",
   "Give Browse a graceful 'you own everything / new listings coming' state, or expand the 8-item catalog so late-game doesn't dead-end.",
   "Delete or re-mount the orphaned components/RealEstateManager.tsx so the only DECOR_ITEMS consumer isn't dead code drifting out of sync with RealEstateApp."
  ]
 },
 {
  "app": "GamingApp / YouVideo",
  "inventory": "Player loop: Record tab uploads a video (title + one of 6 cosmetic topic chips) for 15 energy, capped 5/week (videosThisWeek/lastVideoWeek). projectVideoOutcome (algorithm.ts) sets views from subscribers*0.05 + gear quality multiplier (starter0.5/budget1.0/pro1.6/elite2.5) + ~5% viral 5-20x; videoEarnings (monetization.ts) pays immediate RPM cash; subscribers/totalViews/experience accrue. Channel tab shows featured video, monetization + analytics grids, recent-video rows -> video detail sub-page. Videos tab: sortable thumbnail cards. Studio tab: buy 5 flat-price accessories + upgrade 9 tiered PC components (cost = base*2^tier) via buyAccessory/upgradePCComponent, raising computeQuality 0-100. Residual: calcGamingStreamingIncome decays each video 5%/wk to 0 by 20wk at flat $0.01/effective-view, folded into weekly passive income (shared $75k/wk cap). LIVE state fields written: videos, subscribers, totalViews, totalEarnings, experience, equipment, pcUpgradeLevels, videosThisWeek, lastVideoWeek. Never-written-but-shown: level, paidMembers, totalSubEarnings, averageViewers, membershipRate; unused hooks: trendBonus, videoTitleCounters, unlockedGames/ownedGames/gamesPlayed.",
  "broken": [
   {
    "what": "XP accrues on every upload (experience += views/100) but the channel `level` field is never recomputed from it, so the prominent 'Lv N' badge and XP stat are a progression that never moves and unlocks nothing.",
    "evidence": "contexts/game/actions/ContentActions.ts:141",
    "severity": "dead-end"
   },
   {
    "what": "Channel Memberships are fully non-functional: the Monetization card renders 'Members' (paidMembers) and 'Members/wk' (membershipWeeklyRevenue), but paidMembers is never incremented anywhere and membership revenue is never added to income (calcWeeklyPassiveIncome omits it) \u2014 both display forever as 0 / $0.",
    "evidence": "components/computer/GamingApp.tsx:485",
    "severity": "dead-feature"
   },
   {
    "what": "'Sub earn' stat reads channel.totalSubEarnings, which is initialised to 0 and never written by publishVideo or runStream \u2014 the stat is permanently $0.",
    "evidence": "components/computer/GamingApp.tsx:497",
    "severity": "unread-state"
   },
   {
    "what": "The hot-topic `trendBonus` multiplier is threaded end-to-end (publishVideo arg -> projectVideoOutcome, pre-clamped 0..0.5) but handlePublish never supplies it, so the trending-topic reach boost is a wired-but-dead hook.",
    "evidence": "contexts/game/actions/ContentActions.ts:104",
    "severity": "dead-feature"
   },
   {
    "what": "Topic pool is 6 hardcoded cosmetic tags with no library/unlock; unlockedGames, ownedGames, gamesPlayed and videoTitleCounters are initialised but never read or written in real gameplay (only a sim harness touches ownedGames).",
    "evidence": "components/computer/GamingApp.tsx:185",
    "severity": "content-starved"
   }
  ],
  "proposals": [
   {
    "name": "Creator Level & Perks",
    "pitch": "Turn the already-tracked XP into a real Creator Level ladder that advances the existing 'Lv N' badge and unlocks small, bounded reach/RPM perks.",
    "fitsBecause": "experience is already incremented on every upload/stream and a level field + 'Lv N' badge + XP stat already render (GamingApp.tsx:263,498-499) \u2014 nothing consumes them today.",
    "stateChanges": "Keep existing gamingStreaming.level & experience; write level = creatorLevel(experience) inside the publishVideo/runStream setGameState updaters. Optional additive perkTier?:number (bump STATE_VERSION 21->22 with a migration defaulting it). Read via ?? so old saves are safe.",
    "actions": "New pure lib lib/content/progression.ts (creatorLevel(xp), levelPerks(level)); ContentActions publishVideo/runStream set level and apply a clamped perk factor into the outcome (reuse the existing trendBonus/quality plumbing, don't add a new money path).",
    "uiSurface": "Existing Channel hero 'Lv N' badge + a ProgressRing (already imported) for level progress; a small perks strip on the Channel tab reusing MoneyStat/AnalyticStat tiles.",
    "tests": "lib/content/__tests__/progression.test.ts: monotonic thresholds, perk hard-cap; extend contexts/game/actions/__tests__ ContentActions test to assert level advances after enough XP and perk stays bounded.",
    "effort": "M",
    "risk": "Economy inflation \u2014 cap perk at ~+2%/level with a hard ceiling and a superlinear XP curve so late levels are slow; perk multiplies views only (already-capped income).",
    "tickHook": "None \u2014 derived at action time.",
    "content": "Level threshold table + perk table as a data module in lib/content/."
   },
   {
    "name": "Channel Memberships (shared with Streamly)",
    "pitch": "Make the Memberships numbers real \u2014 a bounded share of subscribers converts to paid members weekly and pays recurring membership revenue that is actually credited.",
    "fitsBecause": "paidMembers, membershipRate and membershipWeeklyRevenue() already exist and render ('Members' / 'Members/wk') but are never written or paid; this finishes a half-built monetization surface shown in BOTH apps.",
    "stateChanges": "Write paidMembers (bounded % of subscribers, hard cap); accumulate the existing totalSubEarnings; optional additive lastMemberWeek?:number. Bump STATE_VERSION 21->22 with a migration defaulting lastMemberWeek. All reads via ??.",
    "actions": "Optional one-off 'Promote membership' in ContentActions (energy 10, cooldown via lastMemberWeek, bounded conversion bump) crediting via applyMoneyDelta with a reason string.",
    "uiSurface": "Existing Monetization card Members / Members-wk tiles go live; add a 'Promote' CTA + a members-of-subs progress bar on the Channel tab.",
    "tests": "weekly module test: conversion cap, revenue cap, idempotent per weeksLived; action test for cooldown + affordability.",
    "effort": "M",
    "risk": "Unbounded recurring income \u2014 cap members at e.g. <=5% of subscribers with an absolute ceiling and reuse the existing $75k/wk gamingStreaming per-source cap.",
    "tickHook": "NEW contexts/game/actions/weekly/applyContentMemberships.ts, wired in GameActionsContext.tsx near applyMiningCryptos (~line 968); converts <=X%/wk subscribers -> paidMembers (capped), credits membershipWeeklyRevenue(paidMembers) to money + totalSubEarnings using weeksLived (no Date.now). ONE module serves YouVideo and Streamly."
   },
   {
    "name": "Trending Topics",
    "pitch": "A weekly-rotating 'hot topic' grants the already-wired trendBonus (+up to 50% views, pre-clamped) to uploads whose topic matches.",
    "fitsBecause": "publishVideo + projectVideoOutcome already accept and clamp trendBonus (algorithm.ts:40); only the composer omits it \u2014 this lights a dead extension point already in the pipeline.",
    "stateChanges": "Additive optional trendingTopic?:string and trendingWeek?:number on gamingStreaming; bump 21->22 with migration defaulting undefined; read via ??.",
    "actions": "handlePublish passes trendBonus (e.g. 0.5) when selectedGame === trendingTopic \u2014 no action-signature change since the param already exists.",
    "uiSurface": "A 'Trending: <topic>' chip in the Record composer and a highlight ring on the matching topic chip (reuse existing chip styles).",
    "tests": "weekly module determinism test (same weeksLived -> same topic, no wall-clock); publish test that a matching topic raises views vs non-matching at a fixed viral roll.",
    "effort": "S",
    "risk": "Low \u2014 bonus is already clamped to 0..0.5 inside projectVideoOutcome; deterministic pick keeps ticks idempotent.",
    "tickHook": "NEW contexts/game/actions/weekly/applyTrendingTopic.ts choosing a topic deterministically from weeksLived.",
    "content": "Reuse the existing GAME_OPTIONS topic list as the trend pool."
   },
   {
    "name": "Pulse fame -> reach (cross-app hook)",
    "pitch": "A high Pulse follower count / influence tier gives a small bounded reach boost to new uploads \u2014 cheap cross-app synergy the program prizes.",
    "fitsBecause": "passiveIncome.ts already imports getSocialMediaData(state) and lib/social/socialMedia.ts exposes getInfluenceLevel(followers); publishVideo already has a bonus channel (trendBonus) to fold a fame factor into.",
    "stateChanges": "None \u2014 derive from existing state.socialMedia via getSocialMediaData(state).followers/engagementRate. No new fields, no migration.",
    "actions": "publishVideo computes a bounded fameBonus from influence tier and folds it into the existing trendBonus sum (keep the combined value clamped <=0.5); optionally add an explicit fameBonus?:number param mirroring trendBonus.",
    "uiSurface": "A 'Pulse boost +X%' line in the Projected reach card on the Record tab.",
    "tests": "publish test: higher followers -> higher projected views, combined bonus clamp respected; zero-followers path unchanged.",
    "effort": "S",
    "risk": "Low \u2014 clamp the combined bonus so fame can't stack past the existing viral/quality ceilings.",
    "tickHook": "None."
   },
   {
    "name": "Play-button milestones",
    "pitch": "Silver/Gold play-button one-off rewards at the 1k / 100k subscriber marks the game already tracks \u2014 a strong YouTube-fantasy payoff for the currently dead-end subscriber count.",
    "fitsBecause": "lib/statistics/milestones.ts already fires '1k+ subscribers' and '100k+ subscribers' at exactly these thresholds (milestones.ts:101,108); nothing in-app rewards them.",
    "stateChanges": "Additive optional claimedPlayButtons?:string[] on gamingStreaming; bump 21->22 with migration defaulting []; read via ??.",
    "actions": "New claimPlayButton action in ContentActions granting a bounded one-off cash reward via applyMoneyDelta (reason string), gated on subscriber threshold and once-per-tier.",
    "uiSurface": "A play-button shelf on the Channel tab with a claim CTA that appears when a threshold is met (reuse GearTile/badge styles).",
    "tests": "action test: claim gated by subscribers, credits once, second claim is a no-op; migration defaults [].",
    "effort": "S",
    "risk": "Bounded one-off windfall (rare, tied to real milestones); idempotency via claimedPlayButtons prevents re-claim.",
    "tickHook": "None (optionally surface a prompt on tick when newly eligible)."
   }
  ],
  "flawlessChecklist": [
   "Persist level from experience (new creatorLevel helper) in publishVideo/runStream so the 'Lv N' badge and XP stat actually advance \u2014 contexts/game/actions/ContentActions.ts:141.",
   "Make 'Members/wk' real or hide it: paidMembers is never written so it renders $0 forever \u2014 components/computer/GamingApp.tsx:485.",
   "Wire or stop showing 'Sub earn' (totalSubEarnings) \u2014 it is permanently $0 \u2014 components/computer/GamingApp.tsx:497.",
   "Feed trendBonus from the composer (even a static 0) or remove the dead param path so the hook isn't silently unreachable \u2014 contexts/game/actions/ContentActions.ts:104 vs GamingApp.tsx:331.",
   "Reconcile residual income with the displayed RPM tier: calcGamingStreamingIncome pays a flat $0.01/effective-view and ignores qualityMultiplier, so Studio upgrades never visibly help long-term income \u2014 lib/economy/gamingStreamingIncome.ts:55."
  ]
 },
 {
  "app": "GamingStreamingApp / Streamly",
  "inventory": "Player loop: Go Live tab picks a category (5 fixed box-art tiles) + duration (30/60/180 min at 15/25/60 energy), capped 5/week (streamsThisWeek/lastStreamWeek). projectStreamOutcome (algorithm.ts) sets concurrent viewers from followers*0.015 + gear quality multiplier + duration diminishing-returns past 90min + ~8% hype train (2.5x); streamEarnings pays viewer+duration+donation cash; followers/subscribers/totalDonations/totalEarnings/streamHours/experience accrue, bestStream tracked, stream pushed to streamHistory. Dashboard: live-preview hero, Stream health ring (computeQuality), dense Revenue grid, box-art category directory (statsFor aggregates history), best stream. History: VOD cards + summary. Category & broadcast detail sub-views. Shop: SAME shared accessories + PC upgrades as YouVideo. Shares the gamingStreaming slice + lib/content + ContentActions with YouVideo; residual streaming income decays 5%/wk to 0 by 20wk (flat $0.005/effective-viewer + $0.02/min). Never-written-but-shown here: currentStream/isLive, averageViewers, totalSubEarnings, paidMembers, level, membershipRate.",
  "broken": [
   {
    "what": "The LIVE state is unreachable: isLive = !!channel.currentStream, but runStream completes synchronously and never sets currentStream, so the dashboard hero's LIVE pill branch never renders \u2014 the channel is always 'OFFLINE'.",
    "evidence": "components/computer/GamingStreamingApp.tsx:217",
    "severity": "dead-feature"
   },
   {
    "what": "averageViewers is shown in the dashboard hero ('X avg') and the History summary ('Avg viewers') but is initialised to 0 and never written by runStream \u2014 both read 0 forever.",
    "evidence": "components/computer/GamingStreamingApp.tsx:436",
    "severity": "unread-state"
   },
   {
    "what": "The Revenue grid's 'Sub earnings' reads totalSubEarnings, which is never written by any action \u2014 permanently $0.",
    "evidence": "components/computer/GamingStreamingApp.tsx:521",
    "severity": "unread-state"
   },
   {
    "what": "The Revenue grid shows 'Members' (paidMembers) and 'Members/wk' but paidMembers is never incremented and membership revenue is never credited \u2014 the whole membership monetization is inert.",
    "evidence": "components/computer/GamingStreamingApp.tsx:517",
    "severity": "dead-feature"
   },
   {
    "what": "'Level N Partner' badge and the Stream-health level ring never change: experience is incremented but level stays 1 forever (no level-up logic anywhere).",
    "evidence": "components/computer/GamingStreamingApp.tsx:449",
    "severity": "dead-end"
   },
   {
    "what": "The Go Live console prints a hardcoded 'Hype-train chance ~8%' hint with no way to influence it, and membershipRate is initialised to 4 (dead, and mismatched with the lib's BASE_MEMBERSHIP_RATE 4.99).",
    "evidence": "components/computer/GamingStreamingApp.tsx:725",
    "severity": "unread-state"
   }
  ],
  "proposals": [
   {
    "name": "Channel vitals: live avg viewers",
    "pitch": "Compute and persist a rolling averageViewers each stream (and a lightweight last-live marker) so the dashboard hero and History summary stop reading 0.",
    "fitsBecause": "averageViewers and currentStream/isLive already render across the Dashboard hero (436), History summary (782) and best-stream row but are never written \u2014 this finishes fields the UI already depends on.",
    "stateChanges": "Write existing averageViewers (rolling mean of recent streamHistory viewers) inside the runStream setGameState updater; optional additive lastLiveWeek?:number for the pill (bump 21->22 defaulting it). No required new field.",
    "actions": "runStream updater derives averageViewers from streamHistory + the new stream; sets the last-live marker to weeksLived.",
    "uiSurface": "Existing hero 'X avg' pill (line 436), History 'Avg viewers' StatCell (782), and best-stream row \u2014 no new surface, just real data.",
    "tests": "Extend ContentActions runStream test to assert averageViewers equals the mean of recorded stream viewers and updates each stream; unrelated decay math untouched.",
    "effort": "S",
    "risk": "Negligible \u2014 pure derived stat from data already stored; no new money path.",
    "tickHook": "None."
   },
   {
    "name": "Subscriber tiers & recurring sub revenue (shared Memberships)",
    "pitch": "Stream-won subs partly become recurring paid subscribers that pay weekly into totalSubEarnings, making Streamly's 'Sub earnings' and 'Members' real.",
    "fitsBecause": "runStream already produces newSubs and increments subscribers, and paidMembers/totalSubEarnings already render on the Revenue grid (517,521) but are never written or paid; the same applyContentMemberships module serves both apps.",
    "stateChanges": "Write paidMembers (bounded % of subscribers, hard cap) and accumulate totalSubEarnings; optional additive lastMemberWeek?:number. Bump 21->22 with migration; read via ??. SHARED with YouVideo 'Channel Memberships' \u2014 one build covers both.",
    "actions": "Shared weekly module (below); no per-stream action change required beyond the existing subscriber accrual.",
    "uiSurface": "Existing Streamly Revenue card (Members / Sub earnings) and category-detail Subs cells go live.",
    "tests": "Shared weekly test: conversion cap, revenue cap, idempotent per weeksLived; assert Revenue-grid-backed fields grow within caps.",
    "effort": "M",
    "risk": "Unbounded recurring income \u2014 cap members <=5% of subscribers with an absolute ceiling; reuse the $75k/wk gamingStreaming per-source cap.",
    "tickHook": "Shared NEW contexts/game/actions/weekly/applyContentMemberships.ts (see YouVideo proposal), wired in GameActionsContext.tsx near applyMiningCryptos, using weeksLived (no Date.now)."
   },
   {
    "name": "Hype Train streak meter",
    "pitch": "Consecutive weekly streams build a hype streak that raises hype-train odds toward a bounded ceiling, replacing the static ~8% and the hardcoded console hint.",
    "fitsBecause": "projectStreamOutcome already rolls hypeTrain at a flat 0.08 and the Go Live console already prints a static 'Hype-train chance ~8%' hint (line 725) \u2014 this makes an existing loop responsive.",
    "stateChanges": "Additive optional hypeStreak?:number on gamingStreaming (reuse lastStreamWeek for gap detection); bump 21->22 defaulting 0; read via ??.",
    "actions": "runStream increments/resets hypeStreak by week gap and passes a streak-scaled hype chance into projectStreamOutcome via a new optional hypeChance?:number param (default 0.08, so existing callers/tests are unchanged).",
    "uiSurface": "A dynamic hype meter + live '% chance' in the Go Live publish console, replacing the static hint at line 725.",
    "tests": "algorithm.ts test: higher hypeChance -> hypeTrain true at a fixed roll; runStream test: streak accrues on consecutive weeks and resets after a gap; default 0.08 unchanged.",
    "effort": "M",
    "risk": "Income stays bounded \u2014 hype is only 2.5x and streams are capped 5/week; cap the streak-boosted chance (e.g. <=25%).",
    "tickHook": "None (streak updated at action time); optional streak decay could live in a weekly module later."
   },
   {
    "name": "Category mastery",
    "pitch": "Repeatedly streaming a category builds mastery that modestly lifts its viewer pull, giving Streamly's signature box-art directory real progression.",
    "fitsBecause": "The category directory + per-category aggregates (statsFor, Dashboard 'Browse categories', category detail) are Streamly's signature surface but are purely cosmetic today; this deepens an existing loop rather than adding a system.",
    "stateChanges": "Additive optional categoryMastery?:Record<string,number> on gamingStreaming; bump 21->22 defaulting {}; read via ??.",
    "actions": "runStream increments categoryMastery[args.game] (bounded per stream); projectStreamOutcome gains an optional masteryBonus?:number (clamped) folded into viewers, default 0 so existing callers are unaffected.",
    "uiSurface": "A mastery bar on each category tile (Dashboard directory) and the category-detail header, reusing the existing meter/progress styles.",
    "tests": "runStream test: mastery accrues for the streamed category only; algorithm test: masteryBonus clamp respected and default path unchanged.",
    "effort": "M",
    "risk": "Bounded multiplier (e.g. <=+25% viewers) with a per-category cap so it can't outgrow the quality/hype ceilings or the weekly income cap.",
    "tickHook": "None."
   }
  ],
  "flawlessChecklist": [
   "Write averageViewers in the runStream updater \u2014 the dashboard hero 'X avg' and History 'Avg viewers' read 0 forever \u2014 components/computer/GamingStreamingApp.tsx:436,782.",
   "Wire the LIVE state or remove the pill: currentStream/isLive is never set, so the hero is permanently 'OFFLINE' and the LIVE branch is dead \u2014 components/computer/GamingStreamingApp.tsx:217,423.",
   "Persist the shared level from experience so 'Level N Partner' and the Stream-health level ring advance \u2014 components/computer/GamingStreamingApp.tsx:449.",
   "Make the Revenue grid's 'Members' and 'Sub earnings' real or hide them \u2014 paidMembers/totalSubEarnings are never written ($0/0 forever) \u2014 components/computer/GamingStreamingApp.tsx:517,521.",
   "Replace the hardcoded 'Hype-train chance ~8%' hint with the real (or streak-scaled) number \u2014 components/computer/GamingStreamingApp.tsx:725.",
   "Fix membershipRate init (4) vs lib BASE_MEMBERSHIP_RATE (4.99): either read the field in monetizationSummary or drop it so the value isn't dead and misleading \u2014 contexts/game/initialState.ts:1443 vs lib/content/monetization.ts:20."
  ]
 },
 {
  "app": "TravelApp",
  "inventory": "Player loop: browse 17 destinations (list->detail sub-page) -> book a trip (deducts cash via updateMoney, writes travel.currentTrip, logs a travelHistory stamp, budgets as 'entertainment') -> wait N weeks -> Return home (returnFromTrip rolls 0-2 seeded events, applies benefits, unlocks a business on first visit). Also: buy passport ($500) to unlock international destinations; invest in per-destination business opportunities for weekly passive income; view a passport/history stamp book. Trip cost/duration are modified by transportationMods (active-vehicle speedBonus shortens trips; politics transport policy cuts fares/duration) -- a real existing cross-app hook. Key state (types.ts:2502 TravelState): currentTrip{destinationId,returnWeek,startWeek}, visitedDestinations[], passportOwned, businessOpportunities Record<id,BusinessOpportunity{cost,weeklyIncome,unlocked,invested}>, travelHistory[]{destinationId,week,year}. Passive business income IS paid via calcWeeklyPassiveIncome (passiveIncome.ts:395-409, capped $50k/wk). lifetimeStatistics.totalTravelDestinations is incremented on first visit. Destination benefits: happiness/health/energy/intelligence/stress/reputation (destinations.ts:11).",
  "broken": [
   {
    "what": "Trip stress-relief AND intelligence benefits are advertised on every destination but never applied. buildTripReturnSummary computes totals.stressDelta and totals.intelligenceDelta, but returnFromTrip's updateStats call passes ONLY happiness/health/energy/reputation -- stress and intelligence are dropped. There is also no `stress` or `intelligence` field in GameStats (only health/happiness/energy/fitness/money/reputation/gems), so the deltas have nowhere to land even if passed. Every one of the 17 destinations carries a `stress` value (the single most consistent 'benefit', e.g. Maldives -40, Bali -30) rendered prominently as a Sparkles chip, plus 8 carry `intelligence` (Tokyo +10) rendered as a Brain chip -- the entire 'de-stress / broaden the mind' fantasy of travel is purely cosmetic.",
    "evidence": "lib/travel/operations.ts:179-180 (stressDelta/intelligenceDelta computed into totals); contexts/game/actions/TravelActions.ts:167-172 (updateStats omits stress+intelligence); contexts/game/types.ts:23-31 (GameStats has no stress/intelligence)",
    "severity": "dead-feature"
   },
   {
    "what": "Per-destination curated event pools are dead data. TravelDestination has an `events?: string[]` field and 3 destinations populate it (london_theatre, dubai_luxury, rome_colosseum, etc.), but nothing reads destination.events -- rollTripEvents filters the fixed 10-event TRAVEL_EVENTS pool by minTripCost ONLY. The referenced ids (london_theatre...) don't even exist in TRAVEL_EVENTS. Consequence: the detail 'What could happen' list and the on-return roll are identical for any two same-priced destinations (Paris and a generic $2.5k trip show the same events), and the 10-event pool is generic/repetitive with no destination flavor.",
    "evidence": "lib/travel/destinations.ts:19 (events field declared), :144/:163/:182 (populated with non-existent ids); lib/travel/events.ts:59 (rollTripEvents keys off minTripCost only, never destination.events)",
    "severity": "content-starved"
   },
   {
    "what": "Business opportunities are a flat, risk-free dead-end. Every unlock uses the same formula (cost = destination.cost*2, weeklyIncome = destination.cost*0.05), giving an identical 40-week payback for every destination with zero differentiation. Once invested it is permanent: income never decays, never has a risk event, can never be divested/sold, and cannot be upgraded -- so there is nothing to aim for or manage after the one tap. 'First visit unlocks a local business deal' is teased on every unvisited destination but the reward is always the same generic '<Name> Business Opportunity'.",
    "evidence": "contexts/game/actions/TravelActions.ts:202-224 (unlockBusinessOpportunity fixed cost*2 / cost*0.05); :299-328 (investInBusinessOpportunity sets invested=true with no reverse; no divest action exists in the file)",
    "severity": "dead-end"
   }
  ],
  "proposals": [
   {
    "name": "Destination-flavored event pools",
    "pitch": "Turn the dead events[] field into real, curated per-destination events so 'What could happen' and the return roll feel specific to Paris vs Bali vs a safari.",
    "fitsBecause": "Finishes an already half-built system (the events[] arrays and TravelDestination.events field already exist and are shown in the detail 'What could happen' card) and repairs broken finding #2. Reuses rollTripEvents + summarizeEvents + the existing TripReturnModal verbatim.",
    "stateChanges": "None to saved state. Data-only: add ~2 events per destination to TRAVEL_EVENTS (lib/travel/events.ts) matching the ids already referenced in destinations.ts, each tagged with a destinationId (or add optional `destinationId?: string` to TravelEventDef).",
    "actions": "Extend rollTripEvents(tripCost, rollFor, destinationId?) to merge destination-specific eligible events with the generic cost-tier pool before rolling; no action-layer change (returnFromTrip already calls buildTripReturnSummary).",
    "uiSurface": "No new surface -- the detail 'What could happen' card (TravelApp renderDetail) and the return modal already render whatever the pool returns.",
    "content": "~2 authored events per destination (34 total) reusing the flavor already named in destinations.ts (london_theatre, dubai_desert_safari, rome_romance, safari_lion...).",
    "tests": "lib/travel/__tests__/events.test.ts: destination-specific event is eligible only for its destination; seeded roll is deterministic; generic pool still fires for destinations with no curated events.",
    "tickHook": "None (fires inside returnFromTrip).",
    "effort": "M",
    "risk": "Low. Balance risk mitigated by keeping event stat/money deltas within the existing magnitude band; deterministic seed already prevents reroll exploits."
   },
   {
    "name": "Living businesses: ROI tiers, business events + divest",
    "pitch": "Make invested travel businesses a managed asset -- varied ROI by destination, occasional boom/bust/expansion events, and a sell/divest option -- instead of a fire-and-forget flat annuity.",
    "fitsBecause": "Repairs the dead-end (broken #3), deepens an existing loop, and plugs into the already-wired passive-income path (calcWeeklyPassiveIncome reads businessOpportunities.weeklyIncome).",
    "stateChanges": "Additive optional fields on BusinessOpportunity: healthPct?, lastEventWeek?, tier? (all read via ?? with safe defaults). New optional divested businesses stay in the record with invested=false. Bump STATE_VERSION (currently 21 in initialState.ts) with a no-op migration if defaults needed.",
    "actions": "New divestBusinessOpportunity in TravelActions.ts (returns ~40-60% of cost, sets invested=false, follows the existing setGameState/{success,message}/applyMoneyDelta pattern with a reason string).",
    "uiSurface": "Business tab StorefrontBody already has an Invest CTA -- add a secondary 'Sell business' chip and a small health/tier badge on the storefront awning (on-DNA).",
    "content": "3-4 business-event templates (supply-chain boom, local downturn, expansion offer) in a new lib/travel/businessEvents.ts data module.",
    "tests": "New applyTravelBusinesses.test.ts (idempotent per week, capped, uses weeksLived); divest action test in TravelActions test.",
    "tickHook": "New contexts/game/actions/weekly/applyTravelBusinesses.ts wired beside applyVehiclesForWeek in GameActionsContext.tsx (~line 1133); rolls rare capped events off weeksLived, never Date.now.",
    "effort": "L",
    "risk": "Economy. Mitigated by the existing $50k/wk businessOps per-source cap (passiveIncome.ts:575) and by making downside events (bust) as likely as upside."
   },
   {
    "name": "Trip class selection (Economy / Business / First)",
    "pitch": "Let the player pick a travel class at booking that trades cash for outcomes -- higher class = pricier but skews the event roll positive and grants a small reputation bump.",
    "fitsBecause": "The boarding-pass DNA already computes and displays classForCost on the pass and detail hero; this promotes a cosmetic label into a real choice. Reuses quoteTrip + transportationMods.",
    "stateChanges": "Additive: currentTrip.tripClass?: 'economy'|'business'|'first' (read via ?? 'economy' so old saves default). No migration required (optional sub-field).",
    "actions": "quoteTrip gains an optional class param that scales adjustedCost; returnFromTrip passes class into rollTripEvents to weight event categories and adds a small reputation delta for first/business.",
    "uiSurface": "A 3-segment class picker in the booking Alert flow / detail hero (on-DNA with the existing classChip).",
    "content": "None (reweights existing event pool).",
    "tests": "operations.test.ts: class scales cost monotonically; higher class reduces negative-event probability; reputation delta bounded.",
    "tickHook": "None.",
    "effort": "M",
    "risk": "Low/economy -- class surcharge is a money sink, not a printer; reputation delta capped to a few points."
   },
   {
    "name": "Passport milestones (frequent-flyer tiers)",
    "pitch": "Give the passport an aim: hitting stamp/country milestones grants one-off bounded rewards (a happiness milestone, then a standing fare discount at high tiers).",
    "fitsBecause": "Uses counters that already exist and are already displayed (visitedDestinations, travelHistory, lifetimeStatistics.totalTravelDestinations) -- the Passport tab already shows Stamps/Countries stats with nothing to reach for.",
    "stateChanges": "Additive optional travel.claimedTravelTiers?: number[] (read via ?? []). Bump STATE_VERSION with a trivial migration if initialization is desired.",
    "actions": "claimTravelMilestone (or auto-grant on return in returnFromTrip) that awards a bounded happiness/reputation one-off and, at the top tier, sets a small standing fare discount surfaced through transportationMods-style multiplier.",
    "uiSurface": "A tier progress strip on the Passport (history) tab beside the existing BoardStat row (on-DNA).",
    "content": "3-4 tier definitions (data) in lib/travel/milestones.ts.",
    "tests": "Milestone tier is derived correctly from counts; reward granted exactly once (claimed guard); old saves default cleanly.",
    "tickHook": "None (evaluate on return) or a light check in an existing weekly module.",
    "effort": "S",
    "risk": "Low. One-off bounded rewards; the standing discount is small and capped, consistent with the existing fare-reduction band."
   }
  ],
  "flawlessChecklist": [
   "Apply the dropped trip benefits: fold totals.stressDelta into happiness/energy relief (stress is modeled elsewhere as happiness/health/energy, e.g. applyEducationStress.ts + meditation), and either route intelligence to a real stat/counter or stop displaying it (operations.ts:179-180, TravelActions.ts:167-172).",
   "New York's `stress: 5` is a stress INCREASE yet renders with the same positive Sparkles/teal chip as relief -- once stress is wired, style positive stress as a penalty and label it clearly (destinations.ts:106, benefitDescriptors TravelApp.tsx:174).",
   "Wire TravelDestination.events into rollTripEvents (author the referenced event ids) or delete the dead events[] arrays so the data doesn't lie (destinations.ts:19/144/163/182).",
   "Differentiate business opportunities: vary ROI and/or add a divest path so investing isn't an identical risk-free 40-week clone across all 17 destinations (TravelActions.ts:202-224).",
   "Verify the on-return events match the detail 'What could happen' preview (the seed makes this deterministic today -- keep a regression test so future event changes don't desync preview vs outcome)."
  ]
 },
 {
  "app": "VehicleApp",
  "inventory": "3-tab loop (Garage / Dealership / Insurance) with a vehicle-detail sub-page. Player can: get a driver's license ($500 gate); buy vehicles from a 15-template catalog via the BuyVehicleModal (financed with an auto loan -> Bank Loan type='auto', or cash), reputation-gated for premium models; set active vehicle; refuel (calculateFuelCost); repair (calculateRepairCost, insurance-discounted); sell (calculateVehicleSellPrice); buy/cancel insurance (basic/comprehensive/premium, 6-month prepaid premium, prorated cancel refund). Garage surfaces fleet value, weekly upkeep, insured count, auto-debt. Weekly tick (applyVehiclesForWeek, wired at GameActionsContext.tsx:1133) deducts maintenance+fuel, decays condition ~1%/wk, adds ~200 mi/wk, rolls accidents (minor/moderate/severe), and nudges reputation +1/wk toward a cap. Active vehicle's speedBonus feeds travel duration (transportationMods) -- a real cross-app hook. Key state: gameState.vehicles[] (Vehicle: condition,fuelLevel,mileage,speedBonus,reputationBonus,insurance,lastServiceWeek,weeklyMaintenanceCost,weeklyFuelCost,maxSpeed,fuelCapacity,fuelEfficiency), activeVehicleId, hasDriversLicense, loans[] (type='auto').",
  "broken": [
   {
    "what": "The live financed-purchase path grants NO reputation, despite every dealer card and the detail spec grid advertising '+X rep'. The UI buys exclusively through purchaseVehicleWithAutoLoan (via BuyVehicleModal.onConfirm), which adds the vehicle but never applies template.reputationBonus -- only the legacy, UI-unused purchaseVehicle applies an instant bonus. The only rep a car actually gives is the weekly tick's +1/wk nudge capped at reputationBonus*3, so an exotic supercar's '+40 rep' badge massively overstates the real, slow effect.",
    "evidence": "contexts/game/actions/VehicleActions.ts:883-890 (purchaseVehicleWithAutoLoan returns stats with money only, no reputation) vs :164-166 (legacy purchaseVehicle grants +reputationBonus); UI label components/computer/VehicleApp.tsx:796 ('+{t.reputationBonus} rep'); weekly nudge weekly/applyVehicles.ts:95-101",
    "severity": "no-op"
   },
   {
    "what": "Every owned vehicle shares identical hardcoded maxSpeed/fuelCapacity/fuelEfficiency (120 mph / 50 gal / 25 mpg) because createVehicleFromTemplate stamps constants -- the VehicleTemplate has no such fields. Yet the detail Specifications grid, the garage hero SpecChip ('120 mph'), and the fleet card ('25 mpg') display these prominently as differentiating specs, so a sport motorcycle, an economy sedan, and a 250k exotic supercar all read 120 mph top speed / 25 mpg / 50 gal.",
    "evidence": "lib/vehicles/vehicles.ts:262-264 (maxSpeed:120, fuelCapacity:50, fuelEfficiency:25 hardcoded for all); shown at components/computer/VehicleApp.tsx:643-645 (spec tiles) and :370 (hero chip)",
    "severity": "content-starved"
   },
   {
    "what": "A fully-built, unit-tested accident model is never wired into gameplay. lib/vehicles/accidents.ts (accidentChance with condition/mileage/active/vehicle-type factors, pickAccidentSeverity including 'total', healthLossForSeverity where insurance reduces injury) has zero importers, and processAccident -- the only path that handles 'total' loss (removing the vehicle) -- is called only by stress tests. The live weekly tick uses its own inline block that caps at 'severe' (60% damage), can never total a car, and ignores insurance when computing the player's health loss. So 'total loss', mileage/active-vehicle accident scaling, and insurance-reduced injury are all unreachable in real play.",
    "evidence": "lib/vehicles/accidents.ts (entire module, no production importers found); contexts/game/actions/VehicleActions.ts:525 (processAccident, only referenced by __tests__); weekly/applyVehicles.ts:63-87 (inline minor/moderate/severe, no 'total', insurance only applied to repair cost not health)",
    "severity": "dead-feature"
   },
   {
    "what": "lastServiceWeek is written and displayed but mechanically inert. It is set on purchase and on repair and shown in the detail spec grid as 'Last service: N wk ago', but no tick or action ever reads it -- deferred/overdue service has no consequence, so the 'Last service' readout is decorative.",
    "evidence": "written at lib/vehicles/vehicles.ts:272 and VehicleActions.ts:315 (repair); displayed components/computer/VehicleApp.tsx:534 & 651-655; never read in weekly/applyVehicles.ts or anywhere in the tick",
    "severity": "unread-state"
   }
  ],
  "proposals": [
   {
    "name": "Wire the real accident system + total loss",
    "pitch": "Replace the inline weekly accident block with the tested accidents.ts model so accidents scale with mileage/condition/active-use, insurance actually reduces injury, and a rare 'total loss' can remove a car.",
    "fitsBecause": "Finishes a half-built, already-unit-tested system (accidents.ts + processAccident's total-loss handling) and repairs broken finding #3. Reuses existing Vehicle fields and the existing accident notification.",
    "stateChanges": "None new -- reads existing condition/mileage/insurance/type. Total loss reuses processAccident's existing vehicle-removal + activeVehicleId reassignment logic.",
    "actions": "Have applyVehiclesForWeek call accidentChance/pickAccidentSeverity/healthLossForSeverity (from the pre-rolled arrays it already holds) and route a 'total' outcome through the existing removal path; keep processAccident as the shared applier.",
    "uiSurface": "None new -- the accident push notification already exists; totaled-car removal is already handled by processAccident.",
    "content": "None (logic wiring).",
    "tests": "Extend weekly/__tests__ (or subsystemEquivalence) to assert insurance lowers injury, high mileage/low condition raises chance, and a 'total' roll removes the vehicle + reassigns active; accidents.test.ts already covers the pure math.",
    "tickHook": "contexts/game/actions/weekly/applyVehicles.ts (already wired at GameActionsContext.tsx:1133) -- swap inline math for accidents.ts calls, keep pre-roll arrays for StrictMode determinism.",
    "effort": "M",
    "risk": "Balance/economy. Mitigate by keeping total-loss probability low (accidents.ts already floors/caps chance at 0.5%) and preserving current repair-cost caps; gate behind the existing constants."
   },
   {
    "name": "Per-template vehicle specs",
    "pitch": "Give each template its own top speed, fuel efficiency, and tank size so the spec grid actually distinguishes a motorcycle from a supercar.",
    "fitsBecause": "Repairs broken finding #2 with near-zero risk; the SpecGrid/SpecChip UI already renders these fields, they just need real data. Optional feed into fuel realism reuses calculateFuelCost.",
    "stateChanges": "Additive optional fields on VehicleTemplate (maxSpeed?, fuelEfficiency?, fuelCapacity?); createVehicleFromTemplate reads them with the current constants as ?? fallbacks, so existing owned vehicles are unaffected. No migration needed (owned vehicles keep their stored values).",
    "actions": "None to the action layer beyond createVehicleFromTemplate reading the new template fields.",
    "uiSurface": "None new -- detail Specifications grid, hero SpecChip, and fleet card already display maxSpeed/fuelEfficiency.",
    "content": "Per-template numbers for all 15 templates (bikes: high mpg/small tank; supercars: high speed/low mpg).",
    "tests": "vehicles.test.ts: createVehicleFromTemplate copies template specs when present and falls back to defaults when absent; a bike and a supercar differ.",
    "tickHook": "None (unless optionally deriving weeklyFuelCost from efficiency -- keep out of scope to stay S).",
    "effort": "S",
    "risk": "None for display. If efficiency later drives fuel cost, bound it against the existing weekly-cost band."
   },
   {
    "name": "Scheduled maintenance (make lastServiceWeek matter)",
    "pitch": "Turn the inert 'Last service' readout into a real upkeep loop: skipping service past an interval accelerates condition decay and accident risk; a Service action resets the clock and restores reliability.",
    "fitsBecause": "Repairs broken finding #4 by reading state that is already written and displayed, and deepens the existing repair/condition loop without new UI scaffolding.",
    "stateChanges": "None new -- reads existing lastServiceWeek and weeksLived. Optionally reuse repair to also set lastServiceWeek (already does).",
    "actions": "Optional serviceVehicle action (or extend repair) that resets lastServiceWeek and grants a small condition buff following the existing setGameState/{success,message} pattern with the money helper + reason.",
    "uiSurface": "An 'Overdue service' warning chip on the fleet/detail card (on-DNA with the existing FootChip/warning bubble) and a Service action chip beside Refuel/Repair.",
    "content": "One tunable interval constant in gameConstants.ts.",
    "tests": "applyVehicles.test.ts: condition decays faster past the service interval; servicing resets the interval and normalizes decay; idempotent per week.",
    "tickHook": "weekly/applyVehicles.ts reads currentWeek - lastServiceWeek to scale VEHICLE_WEEKLY_CONDITION_DECAY.",
    "effort": "M",
    "risk": "Economy -- another upkeep sink. Mitigate with a forgiving interval and a modest decay multiplier so it adds texture, not punishment."
   },
   {
    "name": "Used-car market + trade-in",
    "pitch": "Add a rotating used-inventory row (discounted price, random age/mileage/condition) and a trade-in credit when buying so the Dealership stays interesting after the catalog is owned.",
    "fitsBecause": "Deepens the existing buy/sell/finance loop and reuses createVehicleFromTemplate, calculateVehicleSellPrice, and the auto-loan origination path verbatim; the Dealership already filters/lists templates.",
    "stateChanges": "Additive optional gameState.usedVehicleListings?: {templateId,mileage,condition,priceMultiplier,seedWeek}[] regenerated weekly (read via ?? []). Bump STATE_VERSION with a trivial migration.",
    "actions": "purchaseVehicleWithAutoLoan gains an optional used-listing override (price/condition/mileage); new applyTradeIn credit that subtracts an existing vehicle's sell price from a new purchase's down payment.",
    "uiSurface": "A 'Used inventory' section + filter chip in the Dealership tab (on-DNA dealer cards) and a 'Trade in' toggle in BuyVehicleModal.",
    "content": "Weekly-seeded listing generation in lib/vehicles/usedMarket.ts.",
    "tests": "usedMarket.test.ts (deterministic per weeksLived, priced below sticker, condition in range); trade-in reduces down payment correctly and cannot go negative.",
    "tickHook": "A light refresh in an existing weekly module or lazy regen keyed on weeksLived (no Date.now).",
    "effort": "L",
    "risk": "Economy. Mitigate by pricing used stock off calculateVehicleSellPrice (already market-bounded) and capping trade-in credit at the vehicle's real resale value."
   }
  ],
  "flawlessChecklist": [
   "Grant the advertised reputationBonus on the live financed-purchase path (purchaseVehicleWithAutoLoan) once at purchase, or relabel the dealer/detail '+X rep' as a passive weekly perk -- today buying a car via the UI delivers none of the badged reputation (VehicleActions.ts:883-890 vs :164-166; label VehicleApp.tsx:796).",
   "Give templates real maxSpeed/fuelEfficiency/fuelCapacity so the spec grid stops showing identical 120mph/25mpg/50gal for every vehicle (vehicles.ts:262-264).",
   "Wire the tested accident model + total-loss into the live tick, or delete accidents.ts/processAccident, so the built-and-tested system and the running inline logic stop diverging (accidents.ts, VehicleActions.ts:525, applyVehicles.ts:63-87).",
   "Make lastServiceWeek mechanically matter or drop the 'Last service' tile -- it is written and displayed but never read (VehicleApp.tsx:651-655).",
   "Fix the Insurance 'Weekly premium' summary: the premium is prepaid upfront for 26 weeks and nothing is charged weekly, yet the strip divides monthlyCost/4 -- amortize as monthlyCost*6/26 or relabel as 'prepaid, N wks left' (VehicleApp.tsx:159-162).",
   "Retire the superseded processVehicleWeekly (VehicleActions.ts:592) now that applyVehiclesForWeek is the wired path, so it can't be called accidentally and double-charge upkeep."
  ]
 },
 {
  "app": "PoliticalApp",
  "inventory": "Once careerLevel>0 (won via runForOffice, gated on age/reputation/education/prev-term/cash in PoliticalActions.ts:146), the player can: run for the next office up; Fund a campaign push (campaign() \u2192 approval, diminishing, min $ via CAMPAIGN_MINIMUM_AMOUNT); raise PAC clean from cash 1:1 (raisePACClean) and dirty from BTC (raisePACDirty, adds lifetimeDirtyUSD \u2192 permanent scandal risk); spend PAC \u2192 approval at 1.5x efficiency (spendPACOnCampaign); enact policies from an 80-policy catalog gated by requiredLevel + cost (enactPolicy via EnactPolicyModal); suppress active scandals (suppressPoliticalScandal); join a party (joinParty, +5 approval). Weekly tick runPoliticsWeeklyTick (wired GameActionsContext.tsx:1470): rolls a fresh scandal from heat+dirty-PAC+karma+contentiousPolicies+office, drains approval + decays each active scandal, drifts approval toward 50, and RESOLVES re-election at nextElectionWeek (low approval can lose the seat). Key state (PoliticsState types.ts:2570): careerLevel(1-based:1=Council..6=President), approvalRating, policyInfluence, electionsWon, policiesEnacted[], activePolicies[], lobbyists[], alliances[], campaignFunds, pac{cleanUSD,dirtyUSD,lifetimeDirtyUSD}, scandals[], nextElectionWeek, party. UI is 3 tabs (Office/Policies/Influence) + local list\u2192detail sub-views (ladder/policy/lobbyist).",
  "broken": [
   {
    "what": "hireLobbyist, formAlliance and lobby actions are fully implemented but imported by ZERO components \u2014 the Influence tab renders lobbyists[] and alliances[] (PoliticalApp.tsx:637,654) yet gives no way to hire a lobbyist or form an alliance, so both sections are permanently empty in normal play. getAvailableLobbyists (lib/politics/lobbyists.ts:145, a 15-entry catalog) is likewise never called.",
    "evidence": "contexts/game/actions/PoliticalActions.ts:698 (hireLobbyist), :597 (formAlliance), :518 (lobby) \u2014 no component imports them; roster rendered at PoliticalApp.tsx:627-655",
    "severity": "dead-feature"
   },
   {
    "what": "policyInfluence is write-only: enactPolicy adds +5 (PoliticalActions.ts:508), lobby/hireLobbyist and event effects also write it, and it is the headline 'Influence' stat card, but NO election, policy, or tick math ever reads it \u2014 runForOffice successChance uses approval+reputation+karma, re-election uses approval+electionsWon, enact gates on careerLevel+cash. Pure vanity number.",
    "evidence": "contexts/game/actions/PoliticalActions.ts:508 (write); PoliticalApp.tsx:445 (display); no mechanical read anywhere",
    "severity": "unread-state"
   },
   {
    "what": "activePolicies[] and expiresWeek are written on every enact but never read or processed by any tick, and 0 of the 80 policies set a `duration`, so expiresWeek is always undefined and the 'temporary policy' system is entirely vestigial (policy detail always shows 'Permanent').",
    "evidence": "contexts/game/actions/PoliticalActions.ts:500-506 (write); grep shows no reader of .activePolicies; no `duration:` in lib/politics/policies.ts",
    "severity": "unread-state"
   },
   {
    "what": "campaignFunds accumulates on every campaign() call but is never read by any mechanic and never displayed anywhere in PoliticalApp \u2014 dead legacy counter superseded by the PAC pool.",
    "evidence": "contexts/game/actions/PoliticalActions.ts:690 (write); no read outside default-object initializers",
    "severity": "unread-state"
   }
  ],
  "proposals": [
   {
    "name": "Finish the Influence Market",
    "pitch": "Wire the existing hireLobbyist/formAlliance/lobby actions into the Influence tab and give policyInfluence a real effect, so the roster you can see is a roster you can build.",
    "fitsBecause": "hireLobbyist (PoliticalActions.ts:698), formAlliance (:597), lobby (:518) and the 15-entry AVAILABLE_LOBBYISTS catalog already exist and are imported by zero components; the Influence tab already renders lobbyists[]/alliances[] with no add path.",
    "stateChanges": "None new \u2014 populates existing lobbyists[]/alliances[]. Add one mechanical read of existing politics.policyInfluence: discount enactPolicy implementationCost by min(25%, policyInfluence%) and add up to +10 to the re-election successChance in runPoliticsWeeklyTick. No migration.",
    "actions": "Wire existing hireLobbyist + formAlliance; add HireLobbyistModal (EnactPolicyModal DNA) driving hireLobbyist; alliance picker over gameState.relationships driving formAlliance; modify enactPolicy cost calc + weeklyTick reelection to read policyInfluence.",
    "uiSurface": "New HireLobbyistModal + 'Hire lobbyist'/'Form alliance' CTAs on the Influence tab; the Influence stat card (PoliticalApp.tsx:445) gets a subtitle naming its now-real effect.",
    "content": "AVAILABLE_LOBBYISTS (lib/politics/lobbyists.ts, exists, 15 entries).",
    "tickHook": "runPoliticsWeeklyTick reelection successChance += min(10, policyInfluence*0.1).",
    "tests": "hireLobbyist adds lobbyist + raises influence; fireLobbyist recomputes; enactPolicy discount clamps at 25%; reelection successChance reflects influence.",
    "effort": "M",
    "risk": "Economy: uncapped influence discount could zero policy costs \u2014 clamp discount \u226425% and reelection bonus \u2264+10 (successChance already clamped 25-92)."
   },
   {
    "name": "Perks & Government-Contracts dashboard",
    "pitch": "Show the player the economic payoff of holding office \u2014 unlocked perks and live weekly government-contract income \u2014 which today is applied invisibly.",
    "fitsBecause": "getCombinedPerkEffects/getActivePerks and calculateGovernmentContractBonus/getTotalGovernmentContractBonus already drive real business-income %, real-estate tax breaks, follower bonuses and contract $ in lib/economy/passiveIncome.ts:165,275,292 \u2014 but PoliticalApp never surfaces any of it.",
    "stateChanges": "None \u2014 read-only surface over existing computed data.",
    "actions": "None.",
    "uiSurface": "New 'Perks of office' card in renderOffice listing the active perks for the current office plus a 'Government contracts +$X/wk' line from getTotalGovernmentContractBonus(gameState).",
    "content": "POLITICAL_PERKS (lib/politics/perks.ts, exists).",
    "tickHook": "None.",
    "tests": "Perk list matches office level; contract-bonus number equals passiveIncome's; the displayed value matches what is actually paid (guards the careerLevel off-by-one).",
    "effort": "S",
    "risk": "Perk indexing: passiveIncome calls getCombinedPerkEffects with 1-based careerLevel while POLITICAL_PERKS.requiredLevel is 0-based \u2014 mirror that exact call so the card shows what is actually paid, and fix the off-by-one in the same pass (flawless item)."
   },
   {
    "name": "Campaign promises / term platform",
    "pitch": "On winning an office pick 1-2 campaign promises (by policy type); fulfil them before re-election for an approval+reputation boost, break them for an approval hit and a policy-flip scandal.",
    "fitsBecause": "Reuses PolicyType (lib/politics/policies), the existing 'policy-flip' scandal category (scandals.ts:20), and the now-working re-election resolution in runPoliticsWeeklyTick.",
    "stateChanges": "Additive (migration v22): politics.termPromises?: {id; policyType; madeWeek; fulfilled:boolean}[] default []; politics.currentTermStartWeek?: number. All read via ?? [].",
    "actions": "New chooseTermPromises(setGameState, promises) fired on election win; enactPolicy marks a matching-type promise fulfilled.",
    "uiSurface": "Promise picker sheet after runForOffice success; a 'Term promises' progress card on the Office tab reusing the bill-vote-card DNA.",
    "content": "New lib/politics/promises.ts template module keyed by PolicyType.",
    "tickHook": "In the reelection branch of runPoliticsWeeklyTick: +approval/+rep per fulfilled promise; per broken promise an approval penalty + a policy-flip scandal via existing createScandal.",
    "tests": "Fulfilled promise boosts reelection; broken promise penalizes + can spawn a scandal; migration defaults [] on old saves.",
    "effort": "L",
    "risk": "Scope/economy \u2014 cap at 2 promises, bound total deltas (\u2264+8 approval), reuse existing approval clamps."
   },
   {
    "name": "PAC donor base (weekly fundraising cadence)",
    "pitch": "Give the PAC a living donor base: a small weekly clean inflow scaled by approval + office, plus occasional big-donor events offering clean or dirty money, so PAC isn't only a manual funnel.",
    "fitsBecause": "PAC state + pac.ts raiseClean/raiseDirty + lastRaiseWeek already exist; approvalRating and careerLevel are already tracked and consumed by the tick.",
    "stateChanges": "None new \u2014 writes existing pac.cleanUSD and uses pac.lastRaiseWeek. No migration.",
    "actions": "Optional 'hold fundraiser' action (energy cost + cooldown via lastRaiseWeek, larger one-off clean inflow).",
    "uiSurface": "PACCard shows a 'donor inflow +$X/wk' line; big-donor offers surface via the existing EconomyEventBanner / Alert.",
    "content": "Small donor-event data module (clean vs dirty offer with scandal tradeoff).",
    "tickHook": "runPoliticsWeeklyTick: pac.cleanUSD += clamp(careerLevel * approval * k) capped ~$2k/wk, only when careerLevel>0.",
    "tests": "Inflow scales with approval/office and respects the weekly cap; zero when out of office; dirty-donor event raises lifetimeDirtyUSD.",
    "effort": "M",
    "risk": "Money printing \u2014 hard weekly cap + office/approval gate; magnitude sanity-checked against $500-$5k salaries."
   },
   {
    "name": "Recurring policy budget (make 'Weekly income' true)",
    "pitch": "Turn enacted-policy money into the recurring weekly income the UI already claims, instead of a one-time payment at enactment.",
    "fitsBecause": "calculatePolicyEffects(policiesEnacted).money is already computed and labelled 'Weekly income' in the Policies hero and policy detail, but enactPolicy pays it once (PoliticalActions.ts:481) and no tick ever pays it weekly.",
    "stateChanges": "None new \u2014 derive the weekly delta from existing policiesEnacted via calculatePolicyEffects; remove the one-off effects.money add in enactPolicy.",
    "actions": "enactPolicy: drop the one-time money add so income becomes purely the weekly stream.",
    "uiSurface": "Existing 'Weekly income' labels (Policies hero + describeEffects) become accurate; add the line to the weekly summary.",
    "content": "None \u2014 uses the existing 80-policy catalog.",
    "tickHook": "In runPoliticsWeeklyTick (or passiveIncome): money += calculatePolicyEffects(policiesEnacted).money when careerLevel>0.",
    "tests": "Weekly tick pays the aggregate; enact no longer double-pays; net matches the hero figure; losing office stops the income; old saves unaffected.",
    "effort": "M",
    "risk": "Shifts lump-sum to stream \u2014 audit catalog magnitudes and gate on holding office so a voted-out player stops collecting."
   }
  ],
  "flawlessChecklist": [
   "Fix the office-perk off-by-one: getCombinedPerkEffects is called with 1-based careerLevel (1=Council) in passiveIncome.ts:165,275 but POLITICAL_PERKS.requiredLevel and governmentContracts.ts:50-59 contractMultipliers are 0-based (0=Council), so a first-term Council Member already receives Mayoral perks + tier-1 government contracts. Align the index.",
   "Relabel the Policies 'Weekly income' figure (PoliticalApp.tsx describeEffects + Policies hero) or make it recurring \u2014 policy.effects.money is applied once at enact (PoliticalActions.ts:481), never weekly.",
   "Either wire hireLobbyist/formAlliance into the Influence tab or hide the empty 'Lobbyist roster'/'Alliances' sections so they aren't permanent dead-ends (PoliticalApp.tsx:627-655).",
   "Give policyInfluence a mechanical read or stop presenting it as the headline 'Influence' stat (PoliticalApp.tsx:445) \u2014 it is currently write-only.",
   "Drop or process activePolicies/expiresWeek (PoliticalActions.ts:500) \u2014 written on every enact but never read, and no policy sets a duration."
  ]
 },
 {
  "app": "OnionApp",
  "inventory": "Player can: browse a weekly-rotating marketplace (7 categories \u00d7 3 tiers) posted by 4 seeded vendors, and BUY a listing (buyMarketListing \u2192 BTC debited, buyer-rep + OPSEC xp on success, scam risk by vendor rep, +heat on success); browse the vendor directory + vendor/listing detail sub-views; START multi-stage jobs (4 templates, skill-gated) and RUN each stage (runJobStage \u2014 energy-gated, skill-rolled via stageSuccessProbability, heat on success/fail, payout\u2192dirtyBtc on completion, 3-fail burn); LAUNDER dirtyBtc through 3 mixer tiers (submitMixerTransaction \u2014 fee/delay/failProbability, reduced by laundering skill + restaurant/bank/real-estate fronts via countLaunderingFronts); CASH OUT cleanBtc \u2192 cryptos.btc (cashOutCleanBtc); ACQUIRE NEW IDENTITY (0.5 BTC + 80% of discharged unsecured-loan principal \u2192 resets heat/rep/credit-score to 580, drops active jobs, closes unsecured loans + credit cards). Weekly tick runDarkWebWeeklyTick (wired GameActionsContext.tsx): heat decay (base 3 + 0.5/opsec-level), marketplace refresh (top up to 3/vendor), laundering settlement at readyWeek, job expiry, police events at heat (sting/raid/informant/surveillance), partner discovery \u226550 heat. 4 skills (hacking/social/opsec/laundering) level via xp (level cap 20). Key state (DarkWebState types.ts:379): heat, dirtyBtc, cleanBtc, playerReputation, vendors[], listings[], activeJobs[], jobHistory[], laundering[], skills{}, recentEvents[].",
  "broken": [
   {
    "what": "Purchased marketplace goods are inert: attemptPurchase removes the listing and grants only buyer-rep + OPSEC xp + heat, but the item itself is never added to any inventory and grants none of its implied effect \u2014 gear ('Encrypted Phone','Lockpicks'), hackingTools ('Zero-Day Exploit'), fakeIds ('New Identity Kit') and services ('DDoS-for-Hire') are pure flavor. A legacy DarkWebItem type with riskReduction/rewardBonus fields sits unused, confirming the payoff was intended but never wired.",
    "evidence": "lib/darkweb/operations.ts:138-203 (attemptPurchase \u2014 no inventory write); contexts/game/types.ts:273-281 (unused DarkWebItem)",
    "severity": "dead-feature"
   },
   {
    "what": "The core Jobs loop and vendor pool are thin: only 4 job templates and 4 seeded vendors exist, so once skills clear the corp-breach / exchange-drain gates there is nothing new to attempt and the marketplace rotates the same ~35 title strings across the same 4 handles.",
    "evidence": "lib/darkweb/jobs.ts:71-133 (4 JOB_TEMPLATES); contexts/game/initialState.ts:1138-1143 (4 vendors)",
    "severity": "content-starved"
   },
   {
    "what": "Buy outcome is not surfaced: confirmBuy calls buyMarketListing then queueSave with no result Alert, so a scam silently debits BTC and the only feedback is a forum-log line on the next render \u2014 unlike runJobStage which Alerts success/fail/complete. The BUY button can feel like it did nothing (or robbed you) with no acknowledgement.",
    "evidence": "components/computer/OnionApp.tsx:401-416 (confirmBuy, no outcome Alert)",
    "severity": "no-op"
   }
  ],
  "proposals": [
   {
    "name": "Gear / loadout \u2014 finish the market's payoff",
    "pitch": "Make bought goods matter: gear/tools/fake-IDs/services grant bounded, category-specific effects that plug into the existing job and heat math instead of vanishing after purchase.",
    "fitsBecause": "attemptPurchase (operations.ts:138) currently discards the item after granting rep + xp + heat, and a legacy DarkWebItem type with riskReduction/rewardBonus (types.ts:273) already exists unused \u2014 the payoff was designed but never wired.",
    "stateChanges": "Additive (migration v22): darkWeb.inventory?: {id; category; tier; effect; consumable}[] default []. Read via ?? [].",
    "actions": "attemptPurchase pushes the bought item into inventory; attemptJobStage accepts owned gear to modify stageSuccessProbability / heat; consumables are removed on use; a 'use service' path can auto-pass one stage.",
    "uiSurface": "A terminal 'ls ~/gear' sub-view (OnionApp SubView routing already supports local pages) + an applicable-gear line on the job-detail stage view.",
    "content": "New lib/darkweb/gear.ts mapping category+tier \u2192 a bounded effect.",
    "tickHook": "None \u2014 gear is consumed/applied at job time, not per week.",
    "tests": "Purchase adds an inventory item; passive gear raises a matching stage's success within cap; consumable is one-shot; migration defaults [] on old saves.",
    "effort": "L",
    "risk": "Power creep \u2014 one gear per stage, cap the bonus (e.g. \u2264+15% success or \u2264\u221250% stage heat), consumables single-use; enforce with tests."
   },
   {
    "name": "Vendor relationships (trust + disputes)",
    "pitch": "Turn one-off buys into a vendor relationship: repeat business builds per-vendor trust that lowers YOUR scam odds and earns a small discount; a scam lets you dispute/flag.",
    "fitsBecause": "Vendors already carry reputation + reviewCount and updateVendorAfterPurchase (marketplace.ts:90) already moves them; the vendor-detail sub-view (OnionApp renderVendorDetail) already exists to host the standing.",
    "stateChanges": "Additive (v22): darkWeb.vendorStanding?: Record<vendorId,{buys;scams;trust}> default {}. Read via ?? {}.",
    "actions": "attemptPurchase updates the buyer's standing with that vendor; new disputePurchase(vendorId) after a scam adjusts standing/flag.",
    "uiSurface": "Vendor-detail sub-view gains a 'your standing' trust bar and a [ DISPUTE ] bracket-button after a scam.",
    "content": "None.",
    "tickHook": "Optional: refreshMarketplace favors high-standing vendors when topping up listings.",
    "tests": "Successful buys raise trust and lower the effective scam prob (capped); scam enables dispute; discount clamps \u226415%.",
    "effort": "M",
    "risk": "Low \u2014 cap the discount and trust-driven scam reduction so a favored vendor can never become fully risk-free."
   },
   {
    "name": "Expand job catalog + weekly leads",
    "pitch": "Grow the 4-template job pool to ~10 and rotate a weekly subset of 'available leads', so the core Jobs loop stays fresh and gated progression has more rungs.",
    "fitsBecause": "JOB_TEMPLATES (jobs.ts:71) is plain data and StartJobModal already renders the list; the weekly listing-refresh pattern (refreshMarketplace) is a ready template for lead rotation.",
    "stateChanges": "Additive (v22): darkWeb.availableLeads?: {templateId; postedWeek; expiresWeek}[] default [] (or derive deterministically per week).",
    "actions": "startJob validates the chosen template is currently an available lead.",
    "uiSurface": "StartJobModal filters to current leads with an 'expires in Nw' line; the jobs@onion header shows the lead count.",
    "content": "6-10 new DarkWebJobTemplate entries spanning categories/difficulties in jobs.ts.",
    "tickHook": "runDarkWebWeeklyTick refreshes availableLeads (prune expired, top up), mirroring refreshMarketplace.",
    "tests": "Lead rotation is deterministic per rollFor; expired leads drop; new templates' payouts sit in the 0.04-4.5 BTC band; skill gates hold.",
    "effort": "M",
    "risk": "Economy \u2014 keep new payouts within the existing band and tied to difficulty/skill gates; unit-test payout vs difficulty."
   },
   {
    "name": "Heat tools: safehouse upgrades + lay-low",
    "pitch": "Give heat an active lever and clean BTC a second sink: buy safehouse/OPSEC upgrades that raise weekly decay, and a 'lay low' action that spends clean BTC + energy to cut heat now.",
    "fitsBecause": "decayHeat (heat.ts:54) already scales with opsecLevel so an upgrade tier slots straight in; today heat only decays passively or via risky police events, and clean BTC only cashes out.",
    "stateChanges": "Additive (v22): darkWeb.safehouseTier?: number default 0; darkWeb.lastLayLowWeek?: number.",
    "actions": "New buySafehouseUpgrade(setGameState) (clean-BTC cost, escalating) and layLow(setGameState) (clean BTC + energy, one-shot \u2212heat, cooldown via lastLayLowWeek).",
    "uiSurface": "The threat.monitor panel (renderMarket) gains a [ LAY LOW ] bracket-button + an upgrade line showing the decay bonus.",
    "content": "Small upgrade tier table (tier \u2192 extra decay/wk, cost).",
    "tickHook": "tickHeatDecay reads safehouseTier and adds bounded extra weekly decay.",
    "tests": "Upgrade raises weekly decay within cap; lay-low reduces heat once then respects cooldown; both debit cleanBtc.",
    "effort": "M",
    "risk": "Trivializing heat \u2014 cap total decay bonus, gate lay-low behind a cooldown and scale its cost so burning heat stays expensive."
   },
   {
    "name": "Surface the heat\u2192scandal cross-wire",
    "pitch": "Warn dark-web politicians that heat feeds political scandal risk, closing the loop between the two audited apps.",
    "fitsBecause": "runPoliticsWeeklyTick already consumes darkWebTick.darkWeb.heat as a scandal driver (GameActionsContext.tsx:1481 \u2192 scandals.ts scandalProbability) \u2014 the mechanic exists, only the warning is missing.",
    "stateChanges": "None \u2014 read-only surface.",
    "actions": "None.",
    "uiSurface": "The threat.monitor panel adds a conditional line when politics.careerLevel>0: 'heat feeds political scandal risk' with the current contribution.",
    "content": "None.",
    "tickHook": "None \u2014 already wired in the politics tick.",
    "tests": "Line renders only when holding office and heat>0; the shown value matches scandalProbability's heat term.",
    "effort": "S",
    "risk": "None meaningful \u2014 purely presentational; guard on politics slice existing."
   }
  ],
  "flawlessChecklist": [
   "Surface the BUY outcome: confirmBuy (OnionApp.tsx:409) should Alert scam/success like runJobStage does, instead of silently debiting BTC with only a next-render forum-log line.",
   "Make purchased goods non-inert (see Gear proposal) or relabel the market as 'reputation building' so buying gear/tools/fake-IDs isn't a no-payoff BTC sink (operations.ts attemptPurchase).",
   "Give clean BTC a second use and heat an active lever \u2014 today cleanBtc only cashes out and heat only decays passively, leaving the wallet loop shallow.",
   "Retire or hide the legacy wantedLevel police system (contexts/game/actions/weekly/applyCrimeTick.ts) now that heat is the source of truth \u2014 it still decays wantedLevel and can jail the player via a random encounter with no UI anywhere reading wantedLevel, so the jailing is unexplained.",
   "Expand the 4-vendor seed (initialState.ts:1138) alongside any job-catalog growth so marketplace variety keeps pace with the deeper Jobs loop."
  ]
 },
 {
  "app": "StatisticsApp",
  "inventory": "READ-ONLY analytics dashboard (no player actions, no writes \u2014 it never calls setGameState). 4 tabs: Overview / Systems / Milestones / Planning, plus local list->detail routing (DetailView kinds: vitals, records, career, metric, system, life) via useState. Overview: vitals rings (stats.health/happiness/fitness), net-worth hero + SVG sparkline, Records medal strip, per-metric trend cards (net worth / weekly earnings), Career summary, Lifetime money, Life-snapshot counter grid, Open IOUs. Systems: cross-system cards (buildCrossSystemSummary over banking/crypto/stocks/realEstate/darkWeb/politics/gamingStreaming/travel/pets/vehicles/contacts + favorLedger). Milestones: buildMilestones timeline with category filter. Planning: calculateLifeExpectancy, calculateFIRETracker, calculateRetirementPlanning(65), previousLives. Primary state slice: gameState.lifetimeStatistics (LifetimeStatistics \u2014 20 fields incl. peakNetWorth/Week, totalWeeksWorked, highestSalary, careerHistory[], netWorthHistory[], weeklyEarningsHistory[], and ~12 total* counters). Weekly writer contexts/game/actions/weekly/applyLifetimeStatistics.ts updates only 9 of them (jail, children, weeksWorked, highestSalary, careerHistory, peakNetWorth+week, netWorthHistory, weeklyEarningsHistory). lifetimeStatistics is initialized at initialState.ts:1635 so the weekly module's `if(!ls) return` guard passes. STATE_VERSION=21; migrations follow the `N:(state)=>{}` pattern up to 21.",
  "broken": [
   {
    "what": "totalRelationships is read by the UI (Life-snapshot counter + a Record chip) but has NO production writer, so it is permanently 0 \u2014 the counter and record never appear. trackNewRelationship exists but is never called.",
    "evidence": "components/computer/StatisticsApp.tsx:403 & :210-211 read it; initialState.ts:1641 is the only value source; statisticsTracker.ts:215 defines trackNewRelationship (grep: zero callsites outside tests)",
    "severity": "dead-feature"
   },
   {
    "what": "totalPropertiesOwned is read by the Life-snapshot grid and by the 'first-property' milestone but is never written (trackNewProperty never called), so it stays 0 and the milestone never fires even after buying property.",
    "evidence": "components/computer/StatisticsApp.tsx:402 and lib/statistics/milestones.ts:49-54 read it; statisticsTracker.ts:245 trackNewProperty has no callsites; RealEstateActions never increments it",
    "severity": "dead-feature"
   },
   {
    "what": "totalPostsMade / totalViralPosts are read by the Life-snapshot 'Posts' counter and the 'viral' milestone but are never written (trackPost never called), so both stay 0 forever.",
    "evidence": "components/computer/StatisticsApp.tsx:408 and lib/statistics/milestones.ts:77-82 read them; statisticsTracker.ts:285 trackPost has no callsites",
    "severity": "dead-feature"
   },
   {
    "what": "totalHobbiesLearned is a defined LifetimeStatistics field with no writer AND no reader anywhere in StatisticsApp \u2014 fully dead state.",
    "evidence": "types.ts:2670 defines it; initialState.ts:1654 defaults 0; statisticsTracker.ts:296 trackHobbyLearned never called; no read in StatisticsApp.tsx",
    "severity": "unread-state"
   },
   {
    "what": "Three complete statistics lib modules are wired to nothing: enhancedStatistics.ts (~11.6KB), achievementAnalytics.ts (calculateAchievementAnalytics), and trends.ts slopeOf \u2014 all built + (partly) tested but never imported by any component/context.",
    "evidence": "grep for importers returns none outside tests: lib/statistics/enhancedStatistics.ts, lib/statistics/achievementAnalytics.ts (calculateAchievementAnalytics), lib/statistics/trends.ts:54 slopeOf",
    "severity": "dead-feature"
   },
   {
    "what": "~17 exported helpers in statisticsTracker.ts (startJobTracking, endJobTracking, updateJobStats, updateWeeklyStatistics, addNetWorthSnapshot, updatePeakNetWorth, addWeeklyEarningsSnapshot, trackNew*/trackCrime/trackJailTime/trackTravelDestination) are never called in production \u2014 superseded by applyLifetimeStatistics.ts and inline action writes. Latent double-count: updateJobStats and endJobTracking each do totalWeeksWorked+1 if ever rewired.",
    "evidence": "statisticsTracker.ts:58-350 define them; only trackMoneyEarned/trackMoneySpent/getCareerSummary/getAchievementProgress/getDefaultStatistics/formatStat*/calculateNetWorth are actually imported; double-inc at statisticsTracker.ts:181 & :207",
    "severity": "unread-state"
   },
   {
    "what": "Records 'Top salary' is labeled annual but the stored value is a weekly figure: applyLifetimeStatistics stores highestSalary = max(existing, careerSalary) where careerSalary is the weekly salary (summed with passiveIncome to form weeklyIncome at GameActionsContext.tsx:1161), while the old tracker annualized it \u2014 so the chip shows a weekly number tagged 'annual'.",
    "evidence": "applyLifetimeStatistics.ts:94 stores weekly careerSalary; StatisticsApp.tsx:192 renders it with sub:'annual'; contrast statisticsTracker.ts:208 which multiplied by WEEKS_PER_YEAR",
    "severity": "dead-feature"
   },
   {
    "what": "Milestones tab is a dead-end: buildMilestones only emits thresholds ALREADY crossed \u2014 there are no upcoming/next-target milestones or progress bars, so the tab offers nothing to aim toward.",
    "evidence": "lib/statistics/milestones.ts:27-150 push only when a threshold is already met; StatisticsApp.tsx renderMilestones (458-482) renders only that list",
    "severity": "dead-end"
   }
  ],
  "proposals": [
   {
    "name": "Wire the dead lifetime counters",
    "pitch": "Light up the Life-snapshot grid, Records, and 3 milestones by incrementing the five always-zero counters at their real event sites.",
    "fitsBecause": "The UI and milestones already RENDER totalRelationships/totalPropertiesOwned/totalPostsMade/totalViralPosts/totalHobbiesLearned \u2014 only the writers are missing. This is finishing a half-built system, the charter's top priority, and needs zero new state or UI.",
    "stateChanges": "None new \u2014 increment existing LifetimeStatistics fields. Reuse the guarded inline pattern already proven at JobActions.ts:436 (crime) and TravelActions.ts:156 (travel): `prev.lifetimeStatistics ? {...prev.lifetimeStatistics, totalX: (\u2026?? 0)+1} : prev.lifetimeStatistics`.",
    "actions": "DatingActions (on new marriage/partner) += totalRelationships; RealEstateActions (on purchase) += totalPropertiesOwned; the content/social post action (gamingStreaming/YouVideo) += totalPostsMade and += totalViralPosts when viral; the skill/education level-up path += totalHobbiesLearned. No money/energy changes.",
    "uiSurface": "No new surface \u2014 existing counters (StatisticsApp.tsx:401-409) and milestones (world-traveler/viral/first-property) start reflecting reality.",
    "tests": "Unit test per action asserting the counter increments exactly once per event and is guarded when lifetimeStatistics is undefined; extend milestones.test.ts for viral/property/world-traveler firing.",
    "tickHook": "None (event-driven, not weekly).",
    "content": "None.",
    "effort": "M",
    "risk": "Low \u2014 additive, idempotent per event, guarded by the existing `prev.lifetimeStatistics ?` check. Cross-app (touches 3-4 action files) so each write needs its own small test; mitigate by copying the exact JobActions/Travel precedent."
   },
   {
    "name": "Vitals trend history (Apple-Health sparklines)",
    "pitch": "Give the signature vitals rings a history so health/mood/fitness get the same trend sparklines net worth and earnings already have.",
    "fitsBecause": "Net-worth and earnings already sample history + draw sparklines; the vitals rings are the app's Apple-Health silhouette yet show only the current instant. Historizing them completes the DNA and reuses the existing Sparkline component.",
    "stateChanges": "Additive optional field on LifetimeStatistics: `vitalsHistory?: { week:number; health:number; happiness:number; fitness:number }[]`, initialized [] in initialState.ts and seeded [] via a v22 migration in saveMigrations.ts (follow v14/16/18 precedent). Read with `?? []`.",
    "actions": "None.",
    "uiSurface": "renderVitalsDetail (StatisticsApp.tsx:611) gains three Sparkline rows (reusing the existing Sparkline at line 993); optional mini-spark under the Overview vitals card.",
    "tests": "Extend applyLifetimeStatistics.test to assert a vitals sample is appended on the same 10-week gate and capped at 100; add a saveMigrations test that v22 seeds vitalsHistory:[] on an old save.",
    "tickHook": "Extend applyLifetimeStatistics.ts to push a vitals sample under the existing `shouldSampleHistory` (nextWeeksLived % 10 === 0) branch, capped like netWorthHistory (slice(-99) then push).",
    "content": "None.",
    "effort": "M",
    "risk": "Low \u2014 additive field with ?? guards and a standard migration; no economy impact. Old saves without the field render an empty sparkline (already handled by Sparkline's <2-point fallback)."
   },
   {
    "name": "Trajectory projection (revive slopeOf)",
    "pitch": "Turn the static Planning tab forward-looking: project net worth and 'weeks to next milestone / to FIRE' from the real series slope.",
    "fitsBecause": "slopeOf (trends.ts:54) is built and unit-tested but unused; the Planning/Overview tabs show only current + past. A dashed forecast is a natural, cheap deepening that reuses the existing sparkline and fireTracker output.",
    "stateChanges": "None.",
    "actions": "None.",
    "uiSurface": "Append a dashed 'projected tail' to the net-worth Sparkline (add an optional projected-points prop) and a line like 'On pace: $X in 26wk \u00b7 FIRE in Ny' using slopeOf(netWorthSeries) with fire.fireNumber (already computed at StatisticsApp.tsx:167).",
    "tests": "New pure helpers in trends.ts \u2014 projectSeries(series, steps) and weeksToTarget(series, target) \u2014 with unit tests (flat/declining slope -> Infinity/none; rising -> finite), mirroring the existing trends.test.ts style.",
    "tickHook": "None.",
    "content": "None.",
    "effort": "S",
    "risk": "Low \u2014 pure presentation over existing arrays; guard divide-by-zero/negative-slope to avoid Infinity in the UI (return a 'not on pace' string)."
   },
   {
    "name": "Achievements analytics detail page",
    "pitch": "Make the Records 'Achievements' chip open a real breakdown: completion %, category split, incomplete list, and recommendations.",
    "fitsBecause": "The whole achievementAnalytics.ts module is dead, and the Records 'Achievements' chip (StatisticsApp.tsx:213) currently dead-ends into the flat Records list. A dedicated detail page reuses the module and the app's existing list->detail DNA.",
    "stateChanges": "None (reads existing achievement state).",
    "actions": "None.",
    "uiSurface": "New DetailView kind 'achievements' rendering calculateAchievementAnalytics output (completionRate, categoryBreakdown, incompleteAchievements, recommendations); route the Records 'Achievements' chip to it instead of the Records list.",
    "tests": "Unit test calculateAchievementAnalytics against the chosen source; snapshot the detail rows for a partial-completion fixture.",
    "tickHook": "None.",
    "content": "None.",
    "effort": "M",
    "risk": "Medium \u2014 data-source mismatch must be reconciled first: getAchievementProgress reads state.achievements (types.ts:2196, Achievement[].completed) while calculateAchievementAnalytics reads state.progress?.achievements (types.ts:1547) plus totalAchievementsUnlocked is a third store (GameActionsContext.tsx:2492). Mitigation: confirm which store the Achievements app actually writes, point the analytics fn at that one, so the chip can't disagree."
   },
   {
    "name": "Next-milestone targets",
    "pitch": "Add ghosted upcoming milestones with progress bars so the Milestones tab gives the player concrete goals, not just a trophy log.",
    "fitsBecause": "buildMilestones only lists thresholds already crossed (a dead-end). Emitting the next tier with current/target/pct turns the timeline into an aim-for board, reusing the existing FilterChip/TimelineItem DNA and ProgressRing.",
    "stateChanges": "None \u2014 derive from the same thresholds already encoded in milestones.ts against live values (peakNetWorth, totalTravelDestinations, subscribers, etc.).",
    "actions": "None.",
    "uiSurface": "A 'Next up' section in renderMilestones showing e.g. '$10M net worth \u2014 42%', 'Visit 5 destinations \u2014 3/5' with a slim progress bar.",
    "tests": "Unit test a new buildUpcomingMilestones(state) that excludes already-achieved tiers and computes pct = min(100, current/target*100), following milestones.test.ts.",
    "tickHook": "None.",
    "content": "Extend milestones.ts with the paired target thresholds (largely already implicit in the existing if-conditions).",
    "effort": "S",
    "risk": "Low \u2014 pure derivation; ensure a milestone at 100% moves from 'upcoming' to the achieved list to avoid duplication."
   }
  ],
  "flawlessChecklist": [
   "Wire the five always-zero counters (relationships, properties, posts/viral, hobbies) at their event sites \u2014 or at minimum derive relationships/properties live from state.relationships.length and state.realEstate.filter(owned) \u2014 so the Life-snapshot grid stops showing permanent zeros (StatisticsApp.tsx:401-409).",
   "Fix the highestSalary unit/label mismatch: either annualize the stored value or change the Records 'Top salary' sub from 'annual' to 'weekly' (applyLifetimeStatistics.ts:94 stores weekly; StatisticsApp.tsx:192 labels it annual).",
   "Reconcile the three achievement sources \u2014 state.achievements (types.ts:2196), state.progress.achievements (types.ts:1547), and totalAchievementsUnlocked (GameActionsContext.tsx:2492) \u2014 so the Records chip can't disagree with the Achievements app.",
   "Retire or wire the dead statistics code (enhancedStatistics.ts, achievementAnalytics.ts, slopeOf, and the ~17 unused statisticsTracker.ts helpers) to establish applyLifetimeStatistics.ts as the single source of truth and remove the latent totalWeeksWorked double-count in updateJobStats/endJobTracking (statisticsTracker.ts:181,207).",
   "Resolve the weeklyEarningsHistory semantics: it samples only every 10 weeks (applyLifetimeStatistics.ts:81) yet the UI calls it 'Weekly earnings' and derives 'Best week' from those sparse snapshots (StatisticsApp.tsx:194) \u2014 either sample every week or relabel to '10-week samples'.",
   "Verify sub-week-10 empty states: before the first snapshot the net-worth sparkline, 'Best week', earnings trend, and metricRange all read empty arrays \u2014 confirm they render the intended flat-line/'\u2014' fallbacks with no NaN (Sparkline handles <2 points; metricRange handles []; spot-check the hero peak fallback at StatisticsApp.tsx:304)."
  ]
 }
]